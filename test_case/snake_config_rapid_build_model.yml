engine:
  build_config: config/defaults/wflow_build_model.yml
  waterbodies_config: config/defaults/wflow_update_waterbodies.yml
observations:
# The model RUN period, a subset of historical_window: WF1's wflow time
# scales with it, and nothing downstream reads more than it covers.
  river discharge: test_case/test_data/observations_timeseries.csv
simulation_window:
  start: 2010
  end: 2016
