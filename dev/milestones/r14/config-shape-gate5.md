Gate 5 — R14 config shape, the pre-merge gate

**RELEASED 2026-08-30**, on `feat/r14-p1-loader`. The gate asks two things
(`config-shape-master-brief.md`, human gate 5): *full validation ladder green,
and the four data baseline targets unchanged*. Both halves were already
recorded; this file adds the third cross-cutting check the brief schedules at
Gate 5 and nothing had run — `semantic_tree_diff.py` — and states the gate
released.

### 1. Ladder — green

`pixi run test-full`: 3199 passed. CI green on both legs. Recorded in
`9bfdda5c` and again in `3348c6ef`; not re-run here.

**Not re-run is a claim, so here is what moved after it.** Four commits:
`3348c6ef` (`dev/baseline/manifest.json`, `provenance.md`), `baa28390` (a
board note), this record, and one clause in `AGENTS.md` §Repo Map that still
named R13's `shared:` heading after R14 dissolved it. Of those paths exactly
one is READ by a test — the baseline manifest, by `test_check_baseline_*.py`
and `test_advanced_settings.py` — and those five files were re-run here: **82
passed in 8.6 s**. `AGENTS.md` appears in a dozen test files as a citation in
comments and in no assertion.

### 2. Numbers — the four data targets did not move

`3348c6ef`. Full rebuild of `test_case/test_local` (WF1 with `--notemp` 5m07s,
WF2 2m10s, WF3 9m51s), then `check_baseline.py check`. Three targets differ and
they are the three the gate declares excluded by design — the
`project_config_*.yml` snapshots, which a config migration renames and
rewrites (D-14.1). **The four DATA targets matched**, and both tolerance
reference tables regenerated byte-identical, so the pass says *nothing moved*
rather than *nothing moved by more than the tolerance*.

### 3. Tree shape — CLEAN, with 24 declared exceptions

`check_baseline.py` fingerprints seven targets. `semantic_tree_diff.py` is the
un-manifested slice: it walks the whole `project_dir` and compares every file
under a per-type comparator. It is what can say the milestone moved no file it
was not supposed to move.

**Reference tree.** `C:/Users/taner/workspace/blueearth_cst/test_case/test_local`
— the primary worktree's fixture, last written 2026-08-22 11:25. It identifies
its own code state: its `run_record.yml` carries
`toolbox.commit: 0d256a41`, the same commit `dev/baseline/provenance.md` names
as the baseline recording. The fixture is per-worktree and untracked, which is
why one survived: this branch's copy was rebuilt for Gate 5 and the primary's
was not.

**Why it is a legitimate pre-R14 counterfactual.** The run-path diff from
`0d256a41` to the branch's merge-base `4d0a78dc` is **empty**:

    git diff --stat 0d256a41 4d0a78dc -- blueearth_cst/ '*.smk' scripts/ config/ test_case/
    (no output)

so nothing capable of moving a file changed on `main` between the reference run
and the branch point. Every difference below is R14's or run-to-run.

**Known drift in the reference, enumerated.** The reference tree is an
INCREMENTAL tree, not a rebuild: its experiment directory dates to 2026-08-16
and only parts of it were refreshed on 08-22. One artifact survives there from
code that no longer exists —
`experiments/experiment/config/catalogs/data_catalog_run_stress_test.yml`, the
generated per-realization climate catalog written by rule 3.13
`write_climate_data_catalog`, **removed 2026-08-18, before this branch
existed** (`run_stress_test.smk` still carries the tombstone comment). It is
the single allowlisted MISSING and the only reason the file counts are 276
against 275.

**Invocation.** `--no-path-map`, because both trees are already in the current
layout and the built-in p31 map has nothing to translate; the three renames
R14 performs are declared explicitly instead.

```bash
REF="C:/Users/taner/workspace/blueearth_cst/test_case/test_local"   # run at 0d256a41
pixi run python dev/scripts/semantic_tree_diff.py --ref "$REF" --cur test_case/test_local \
  --no-path-map \
  --map "config/runs/snake_config_analyze_projections.yml=config/runs/project_config_analyze_projections.yml" \
  --map "config/runs/snake_config_build_model.yml=config/runs/project_config_build_model.yml" \
  --map "experiments/experiment/config/snake_config_run_stress_test.yml=experiments/experiment/config/project_config_run_stress_test.yml" \
  --allow "experiments/experiment/config/catalogs/data_catalog_run_stress_test.yml" \
  --allow-content …   # one per adjudicated difference; 23 of them
```

The 23 `--allow-content` paths are not reconstructable from the grouped table
below, so the exact command is committed beside this file as
`gate5-tree-diff.sh` — run it to reproduce the CLEAN, or drop its
`--allow-content` lines to reproduce `gate5-tree-diff.txt`.

**Result.**

    CLEAN: 253 files compared, 0 failed, 0 missing, 0 extra, 24 allowlisted

253 of 275 compared; the remainder are `logs/`, `benchmarks/` and `.snakemake/`,
which the tool compares as absent by design (wall times and Snakemake
bookkeeping). **0 missing and 0 extra is the load-bearing number**: after the
three declared renames, no file appeared, disappeared or moved. The
un-adjudicated report — the actual differences, before they were classified —
is `gate5-tree-diff.txt`; run it without `--allow-content` to reproduce it.

#### The 23 content differences, classified

Every one was read, not assumed. No row landed in an "unexplained" bucket.

| # | Files | What differs | Class |
|---|---|---|---|
| 1 | the 3 config snapshots (`config/runs/project_config_{analyze_projections,build_model}.yml`, `experiments/experiment/config/project_config_run_stress_test.yml`) | v1 keys → v2 keys, `shared:` → kind sections, `schema_version: 2` | **R14, its declared purpose.** D-14.1 |
| 2 | the 3 `run_record.yml` + `config/runs/journal.jsonl` | the same key renames, plus `source_config.path`/`.sha256`, `effective_config_sha256`, `projection:`, `referenced_inputs[].origin`, `toolbox.commit` | **R14.** These records quote the configuration by construction |
| 3 | `experiments/experiment/config/experiment.yml` | 8 rows: `realizations_num`→`n_realizations`, `stress_test`→`climate_perturbations`, `run_length`+`horizontime_climate`→`simulation_window`, `run_historical` gone | **R14.** Consequence X1, the frozen record migrated forward |
| 4 | 2 × `config/README.md` (generated) | `snake_config_<workflow>.yml` → `project_config_<workflow>.yml` in two lines | **R14.** `C-85` |
| 5 | 2 × `run_metadata.json` (`experiments/.../results/`, `models/hydrology/wflow/evaluation/`) | `configuration_inputs_sha256` and `effective_config_sha256`, **and nothing else** | **R14.** The digest break, paid once (X1) |
| 6 | `experiments/experiment/config/model_reference.yml` | `inputs.forcing/inmaps_historical.nc` sha256, and the `digest` derived from it | **netCDF container non-determinism.** The file itself is in the compared set and **PASSED element-wise** — same dims, coordinate labels and order, NaN masks, values to 1e-9, non-volatile attrs. Different bytes, identical data; distinguishing those two is what this tool is for |
| 7 | 9 × `data/climate/projections/cmip6/scalar/*.nc`, plus `summary/provenance.json` | `cst_reducer_module_hash` and the `cst_series_digest` derived from it. `cst_raw_digest` — the same identity minus the reducer hash — is **identical in all nine**, and the arrays compared equal | **NOT R14.** See below |

**Row 7 is worth its own paragraph, because it looks like a numerical change
and is not.** `REDUCER_HASH` is `kernel_hash(REDUCER_KERNEL, env_fingerprint=
file_digest(pixi.lock))`. None of the six enumerated kernel functions is
touched by this branch (`git diff main..HEAD -- blueearth_cst/projections/`
reaches `change_factor_table.py`, `dry_month.py`, `variable_spec.py` and no
others). The env fingerprint moved instead — and `pixi.lock` is **unchanged in
git** on this branch. The two worktrees hold the same lock file with different
LINE ENDINGS: this worktree's copy is CRLF (664,259 bytes,
`ca888ef1…`), the primary's is LF (646,447 bytes, `d2b1e08e…`, byte-equal to
the committed blob), and `tr -d '\r'` makes them identical. So a checkout
line-ending difference re-keys every CMIP6 series.

**Reconstructed rather than argued.** "No kernel function changed" is an
inference from a diff; the hash itself settles it. Importing the six symbols
exactly as `analyze_projections.smk` does, at HEAD, and varying only the lock
file:

    session-1 (CRLF)   3f549e3d26166828e81d485ff658049ed4db5db479d5983680589a9de71da34f
    primary   (LF)     b1245b016889b0f72476a281867a652c4aa63d7c22e771cb2c4797eeee60ee2e
    cur tree recorded  3f549e3d…      ref tree recorded  b1245b01…

One code state reproduces BOTH recorded hashes. The kernel source contributes
nothing to the difference; the env fingerprint is the whole of it. Probe:
`gate5-kernel-hash-probe.py`.

Boarded as `t2608301524`; it is a pre-existing defect, unrelated to config
shape, and it does not touch a value anywhere.

#### X5 was predicted and did not fire

The design (§16, X5) expected `semantic_tree_diff.py` to report a tree-shape
change, because `C-61` renames WF2 figure directories. It reported none — 0
missing, 0 extra. `C-61` made naming a future window OPTIONAL rather than
mandatory, and the baseline config names its windows, so no directory moved for
this project. X5 stands as written for a config that omits the names; it is
**unobserved here**, which is a narrower statement than "did not happen".

### 4. What this gate does NOT cover

- **One project tree.** `test_local` only. `rapid`, `baseline_linux` and
  `wf2_fast` have no tree comparison; P4's resolved-config equivalence covers
  their CONFIGS, not their outputs.
- **The reference's own drift.** One stale artifact, enumerated above. A
  second one could only hide as a content difference in a file both trees
  carry, and all 23 were read.
- **`logs/`, `benchmarks/`, `.snakemake/`** — excluded by the tool.
- **The stale-spelling sweep is RED and stays red.**
  `dev/scripts/sweep_stale_spellings.py` at this commit: 25 hits classified,
  **117 unclassified**, exit 1. Not a defect list — the populations are the
  migration machinery, tests that assert a v1 spelling is REFUSED,
  `dev/scripts/` old→new path maps, and `clim_project` as a Python parameter
  name. `t2608290620` owns giving the classifier those allowance classes and
  measured 113 at `bedadddd`; the four added since are `docs/migration-config-tiers.md`,
  R13's migration record, which holds v1 keys for the same reason the other
  migration files do. So this gate's ladder claim is `test-full` + the targeted
  re-run above, and **not** a green sweep. Reading it still requires filtering
  by path, which is exactly what that board item exists to fix.
- **The experiment-freeze defect** (`t2608290250`) is out of scope by
  construction: R14 found it, briefed the repair, and did not fix it. Gate 5
  does not assert the freeze fires.

### 5. Release

Gate 5 is **RELEASED**. The bundle is green: ladder, numbers, tree shape.

**The merge already happened, and it happened first.** R14 landed on `main` as
`5e048c00` (PR #1) on 2026-08-29 15:24, one bundle as K3 requires, with CI
green on both legs at `baa28390`. That merge cites Gate 5's ladder and baseline
halves — which were the two that had run. **The tree-shape half is the one this
record adds, and it ran the day after the merge.** It changes nothing about the
outcome: it CONFIRMS what the merge asserted, and it would have been the gate's
cheapest falsifier had it run before.

Recorded because the ordering is the kind of thing a milestone record exists to
preserve, not to smooth over: a gate is only a gate when it precedes the thing
it gates. What lands after this record is the record itself.

### 6. The gate found one defect after all, and not with the tree diff

The pre-push `test-fast` run for this record went red: **1 failed, 3124
passed**. `tests/test_interchange_contracts.py::test_wg1_integration` derives
the historical-store key from the fixture's own WF1 snapshot and read it as
`yaml.safe_load(...)["shared"]` — the section R14 dissolved. Both keys behind
it moved too (`shared.clim_historical` → `climate.selected`, `C-44`;
`shared.historical_window`'s ISO pair → `climate.window`'s inclusive years,
`C-70`). Fixed by routing through `historical_window_bounds`, the same helper
`climate_store_rule` uses, so the key has one implementation.

**Why nine phases, `test-full` at 3199, and two green CI legs all missed it.**
The test reads a FIXTURE and skips when the fixture is absent — so on CI it has
never run at all. Locally it read a *pre-migration* snapshot that still had
`shared:` in it, and passed. The Gate 5 rebuild replaced that snapshot with a
v2 one; the test failed on the next run. It was green through the entire
migration for the same reason it was worthless: it was reading v1 both sides.

That is the general shape worth keeping: **a fixture-gated test passes through
a migration by reading a fixture the migration has not touched yet**, and CI
cannot see it. The tree diff found nothing here; the check that did was running
the suite in the one worktree whose fixture had caught up.
