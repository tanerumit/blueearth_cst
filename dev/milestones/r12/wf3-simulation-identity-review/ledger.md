# Disposition ledger — wf3 simulation identity, revision r1

Append-only. One row per original finding ID (31 total: `domain-1`..`domain-10`,
`arch-1`..`arch-10`, `risk-1`..`risk-11`). Severities are as filed by each
reviewer; deliberate divergences between lenses are NOT harmonised — `arch-6` /
`risk-11`, `arch-7` / `risk-8` and `risk-1` / `arch-8` are each one defect filed
at two severities, and each ID is dispositioned at its own.

**Split: 30 accepted · 0 rejected · 1 deferred · 0 withdrawn.**

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| `domain-1` | r1 | blocking | accepted | **Owner ruling R-2** (with R-1 on the semantics). Class C is declared `grain: run` with a **required** `reference`, not `grain: bundle`. §5.4.1a states the correction and its evidence: `_category_month` is called once, globally, outside the member loop from the `st_0` baseline (`export_wflow_results.py:361-364`), so v1's stated reason ("different realizations select different months") describes a mechanism absent from the code and, under R-1, one the owner has now rejected as a method. `_month_mean` (`:173-175`) is linear in years, so per-run values exist, are finer, and average back exactly. `bundle_by` and `reference` are separated as two ideas: sharing a reference is not pooling. Accepted cost, ruled at the gate: the results file gains rows, HM-7's pinned surface and the baseline both move (§5.4.2 C-12) | v2 |
| `domain-2` | r1 | major | accepted | **Owner ruling R-3.** The family registry gains a declared `PAIRING` field (`paired_across_design_points` / `independent` / `none`), stage 3 records it beside the surface, and §5.2.2 states that `derived_from` carries common random numbers for any family that populates it (`run_stress_test.smk:1020`). Prose-only was offered at the gate and declined. **Added beyond the ruling, from CFS §4:** a family declaring a paired relation with an all-empty `derived_from` is refused at parse time — a declared pairing is only true if the edges exist, and without the refusal the field is a claim with nothing behind it. Falsifier GF-18 | v2 |
| `domain-3` | r1 | major | accepted | **Owner ruling R-4.** `min_blocks` becomes `blocks_per_return_period`, a ratio, so the declaration references the extrapolation and distinguishes a 20-year level from a 200-year level on the same sample (`RETURN_PERIOD_PEAK_YR = 10`, `RETURN_PERIOD_LOW_YR = 2`, `indicator_tables.py:186-187`). **The ratio is picked here, not deferred**, per the ruling: `1.0` — the block sample must be at least as long as the period estimated — with a separately-argued `GEV_IDENTIFIABILITY_FLOOR = 10` because the ratio alone would demand 2 blocks for a 3-parameter fit. §5.4.4 tabulates what each shipped config gets (baseline 32, rapid 18, both pass; 1 rlz × 9 yr refuses) and states plainly that the floor binds for both shipped periods while the ratio binds from `T > 10`. The stricter `2.0` reading is weighed and rejected as A12, because it refuses `snake_config_rapid` | v2 |
| `domain-4` | r1 | major | accepted | **Not discharged by R-4, and treated as its own obligation.** A ratio the milestone must pick is a claim that needs a falsifier. **GF-15** added: a unit test invoking the reducer on a synthetic bundle one block below `required`, asserting the refusal and each of the five facts the message must name (metric, unit, return period, required, actual). Deliberately synthetic: §5.4.4's table states that no shipped config reaches the threshold, so the design claims a *reachable* refusal (1 rlz × 9 yr) without claiming a *shipped* one, and GF-15 does not depend on either | v2 |
| `domain-5` | r1 | major | accepted | **A working instrument is specified** (§9, "GF-9's instrument"). Confirmed against the code: `_indicator_key_columns` (`dev/scripts/check_baseline.py:788`) keys on every non-value column and `compare_indicator_table` FAILs structurally on a column-set mismatch and an unequal row-key set before any numeric comparison (`:790-797`, `:861-874`). R-2 sharpens it — column set *and* row count now change. GF-9 is therefore restated as **three claims** with a one-off crosswalk comparator: (A) Class-A rows 1:1 through `(rlz, st_id) → run_id → unit_id`; (B) Class-B pooled rows 1:1 onto bundle units; (C) the old Class-C pooled value against the **mean over `rlz`** of the new per-run values, which tests R-2's own averaging premise rather than restating it; (D) coverage, so a dropped row is a failure. **The honest statement is made explicitly:** this change cannot be gated by `check_baseline.py check` in its current form, and §8.2 says so instead of citing a command that cannot evaluate the claim. Depends on `risk-7`'s pinned row order, without which the crosswalk cannot be written | v2 |
| `domain-6` | r1 | major | accepted | §5.4.1a clause 2: **a declared `reference` unit MUST be `evaluated: true`, refused at parse time** alongside §5.6's other refusals, with a message naming the metric, the reference grouping and `run_historical`. The live silent drop is reported in the design with its evidence — `if q_locations and 0 in runs:` (`export_wflow_results.py:362`) leaves the months `None` and two configured metrics vanish from the deliverable with no log line, an `AGENTS.md` "no silent caps" violation in the exact configuration §5.1.5 adds no-silent-cap language to. Falsifier GF-14 | v2 |
| `domain-7` | r1 | major | **deferred** | **The finding is correct and the substantive gap is not closed.** §5.4.4a states the position as a position — *the toolbox reports admissible point estimates and carries no fit uncertainty across the seam* — notes that after R-2 the residual is Class B alone (A and C now carry per-run rows from which spread is recoverable), and names the honest cost: a surface reader cannot tell whether a gradient between two design points exceeds the estimator's noise. It is **not** fixed: no interval travels. Recorded as Q9 with the route that stays open at zero schema cost — `metric` is a free vocabulary under W2, so a companion metric name admits an interval later as a declaration-and-reducer change, not a contract migration. **Trigger:** a stated requirement to read the surface for threshold crossings *with* the interval. Filed `deferred` rather than `accepted` because the finding's alternative ("state the position") is the cheaper half of what it asked for, and dressing that as a fix would be exactly what this column exists to prevent | v2 |
| `domain-8` | r1 | minor | accepted | §5.4.1a clause 1: the reference parameter is **resolved once per table, not per location**, normatively, citing ruling Q5 as its authority. Confirmed in code: `_category_month` sums monthly flow across gauges, takes `idxmax()`, and returns `int(chosen.iloc[0])` — the first gauge column (`export_wflow_results.py:159-170`), documented there as preserving pre-R11 behaviour. Stated because a re-implementer working from v1's text would reasonably compute it per location, which is a behaviour change smuggled into a migration claiming output-neutrality | v2 |
| `domain-9` | r1 | minor | accepted | §5.1.2's completeness predicate now asserts the cross-product against the **configured** axes (`RLZ_NUM`, `ST_NUM`), both available at the minter, rather than over observed values — which detected raggedness and missed uniform truncation, i.e. held only where the question does not arise. Added: **a family that declares no cardinality is reported UNCHECKABLE by name, not passed**, since for a supplied family the expected cardinality is not derivable and silence is not a pass | v2 |
| `domain-10` | r1 | minor | accepted | **Owner ruling R-6**, and the finding proposed the instrument. `candidate-family-schema.md` is written as a G2 precondition: two variants (a *factorial* GCM-downscaled set, deliberately, and a ragged supplied set), checked against §5.1.2's core, the `FAMILY_COLUMNS` registry, `derived_from`, and the seam's column count. **E18 is CONFIRMED** — on the `st_id`-foreign-key and CRN-pairing grounds, which reach the factorial case the intake's raggedness argument does not. E19 is confirmed by the same exercise. It ships no producer, no downscaling path and no config surface, per the non-goal. It also returned three things nothing in the panel named: the one-table-one-family rule, the pairing-consistency refusal, and the correction to Goal 1's wording | v2 |
| `arch-1` | r1 | major | accepted | **§5.2.1a replaces all three statements with one normative sentence, states the exception once, and respecifies the instrument.** The rule: *a stage-2 rule MUST NOT branch on, filter by, compare, or derive a path or an id from any family-block column; a family-block value may travel as an OPAQUE `params:` payload, obtained only from `family_payload(row)`, interpreted only by the family-specific script.* §5.3.3 and GF-11 now cite it rather than restating it at new scopes. **The exception is stated as real and its cost named** — rule 3.12 must hand the generator a design point's multipliers, so the honest rule is "stage 2 makes no *decision* on anything family-specific", and the envelope is a hole in the checkable surface, bounded by the addressing being by `run_id`. **GF-11's instrument gets a referent:** an AST test over rule bodies declared stage 2 by a module-level `STAGE` mapping, not over modules — §2.2 makes splitting the `.smk` a binding non-goal, so "stage-2 module" named nothing. §5.6's rule-3.12 row is rewritten so it no longer names `st_id` | v2 |
| `arch-2` | r1 | major | accepted | Filed with `risk-2`; resolved once. **`forcing_uri` is removed from the universal core** (§5.1.2), taking the mutual-exclusivity rule with it. The two options were implement-fully or remove; implementing ships an ingest path for a family the intake's non-goal forbids building (recorded and argued as A11). The removal is made accountable rather than evasive by **CFS §4**, which prices the column's return exactly — one core column, one rule 3.11b `stage_supplied_forcing`, one WG-4 producer clause, one falsifier — and establishes it is **family-blind**, so the seam contract survives its addition. §5.2.3's seam table and derived-object list are now the same set, closing the "break 1" mismatch. Recorded as Q8 | v2 |
| `arch-3` | r1 | major | accepted | Filed with `risk-4`; both halves fixed, per the index's "both, or neither". **Confirmed by reading the function:** `validate_wg5` selects entries by `k.startswith("rlz_")` and appends a diagnostic when there are none (`interchange_contracts.py:554-560`), with the key pattern also in its docstring (`:525`, `:536-537`) — so under the design's own `run_<run_id>` key it reports a contract violation on every well-formed catalog. §5.8 replaces the "untouched" clause: **the entry selector and the no-entries diagnostic move to `run_`, and the docstring with them; per-entry FIELD validation is what is unchanged** — the true statement v1 meant. The commit inventory (§8.2a) names `interchange_contracts.py` explicitly for this | v2 |
| `arch-4` | r1 | major | accepted | §5.9 enumerates `validate_hm7`'s complete assertion set in five numbered items, restoring the two v1 dropped: the **id-domain check** re-pointed (replacing `allowed = {POOLED_REALIZATION} | set(range(1, rlz_num + 1))`, `:947-953`) as "every `unit_id` resolves in the index, every `member_run_id` in the scenario table", stated rather than left to the argument swap; and **design-point completeness with the `st_0` partition** (`:995-1038`) re-expressed over `unit_id → member_run_id → st_id → lookup.st_id`, gated to families declaring an `st_id` block column. The design states why this is not §5.4.3's invariant 6: invariant 6 checks against the minter's namespace, this against the configured design, and without it the guarantee is *weaker than today's* — the opposite of what the migration claims. The existing diagnostic text is preserved verbatim | v2 |
| `arch-5` | r1 | major | accepted | **Intake E2 and §5.1.5 are now connected, which is the finding's core.** §5.1.5 replaces the pull-semantics argument: rule 3.15 is loop-generated with **static** output lists (`run_stress_test.smk:1226-1231`), so every `_k_members` entry is an unconditional declared output and pull semantics exclude nothing; what excludes the baseline today is `ST_START` filtering `_k_members` at `:1120`. v1's citation of `:1247` licensed a claim about a different rule. §5.6's rule-3.15 row now states **`_k_members` is built from `EVALUATED_IDS`, never `RUN_IDS`** — without which §5.5.2's "ids are minted for every row" would have simulated the unevaluated baseline and broken output-neutrality on the one config it claimed to preserve. GF-4's falsifier is strengthened to assert over each batch rule's **declared outputs**, not the job list | v2 |
| `arch-6` | r1 | **major** | accepted | Dispositioned at the architecture lens's severity, not `risk-11`'s. §8.2a is a new subsection carrying the complete inventory. Six targets added, all verified present: `dev/reference/indicator-glossary.md` (documents grain as `rlz_id = 0` at `:101-104`, pinned by `tests/test_indicator_glossary.py`), `shared/surface_axes.py` (`read_indicators` reads `dtype={"st_id": str}` at `:295-305`), `dev/scripts/check_baseline.py` (hardcoded `TARGETS` at `:237`, `_indicator_key_columns` at `:788`, the `st_id`-is-text note at `:771`), `dev/baseline/indicator_ref/74ed83c06b2e7e6c.csv`, `docs/notebooks/Climate Stress Test.ipynb` (in-repo, so C-7 does not cover it), plus `shared/indicator_tables.py` as a **semantic** change under R-2. §5.4.3's "free **now**" claim is corrected to "requires adding one row to `check_baseline.TARGETS`". The notebook and the contract documents are named as targets of the landing, not written here, per §8's authority note | v2 |
| `arch-7` | r1 | **major** | accepted | Dispositioned at the architecture lens's severity, not `risk-8`'s. Two fixes. **(a)** Rule 3.09's `params:` gains the grain-declaration digest, matching what §5.3.4 does for 3.16 — necessary because 3.09's config inputs are `ancient()` and `params.stress_test_cfg` is otherwise its only trigger (`:882-887`, whose own comment records the trap). Falsifier **GF-16**. **(b)** The digest's misdiagnosis is removed at the source: §5.5.3 now digests the scenario table's **semantic content only** (family-block values and edges), blind to `run_id` spellings, width and column names, so a width change moves no digest and cannot be reported as a changed scenario set. §5.5.2's "loud" claim is corrected — v1 presented a wrong-cause refusal costing the whole sweep as a benefit — and §5.5.2 now states accurately that the minter **evaluates stage-3 groupings over stage-1 rows**. Carried into §7 as C-11 | v2 |
| `arch-8` | r1 | **minor** | accepted | Dispositioned at the architecture lens's severity, not `risk-1`'s. `<exp>/config/.scenario_table.sha256` is added to the refusal message's remedy **and** to §8.3's deletion list, so following the documented remedy actually clears the refusal — which matters because the refusal is deliberately not silenceable by a flag. And **the sidecar is declared authoritative; `run_metadata.json`'s copy is a record**, which v1 left unstated while writing to both | v2 |
| `arch-9` | r1 | minor | accepted | §5.6's rule-3.11 row restores `temp(...)`, matching §5.7b's "the `temp()` lifecycle is unchanged" and the live rule (`run_stress_test.smk:990`). The row now says why: an implementer following v1 literally would stop the baseline NCs being temporary, which `AGENTS.md` names as the change that explodes disk usage on large `RLZ_NUM × ST_NUM` runs | v2 |
| `arch-10` | r1 | minor | accepted | §5.9 replaces "zero-padded text at the namespace width" with the WG-2 formulation already used for `st_id`: **every `unit_id` in one results set shares one width, the width is discovered from `unit_index.csv` rather than computed, and a set mixing widths is malformed.** v1 pinned a value the contract does not define — §5.5.2 makes the width a function of a repository-internal declared bundle count — while C-7 requires HM-7 to be implementable standalone | v2 |
| `risk-1` | r1 | **major** | accepted | Dispositioned at the risk lens's severity, not `arch-8`'s; the framing half is where the value is. §5.5.3a is a new subsection reconciling with the guard already here. Verified: `check_not_frozen` (`write_experiment_config.py:113-140`; `has_run_successfully` `:55`, `_frozen_differences` `:63`) refuses a change to the recorded `workflows.run_stress_test` section once results exist, and `realizations_num` and the grid are both inside it (`run_stress_test.smk:94`, `:186`) — **so for the only family that exists, the in-place case is already refused.** *(Note: the review's `:191-239` citation points at the `__main__` block; the function is at `:113-140`.)* Four corrections: the digest check is keyed to the **same `has_run_successfully` marker**, so it cannot fire on the failed-run case the freeze deliberately permits (`:13-17`); precedence between the two refusals is stated; the digest covers **semantic content only**, removing the `FAMILY_COLUMNS`-rename false positive; and the justification is narrowed to what the freeze genuinely cannot see — a supplied table changing under an unchanged config section. v1's claim that "without it, W4's narrowing is not a narrowing but a removal" is **withdrawn as unsupported**, and §5.10.1's C24-reason-2 disposition is amended to attribute the safety jointly. **GF-12 is rewritten** because v1's falsifier could not show the digest was load-bearing; it now has a freeze case and a supplied-table case. GF-17 covers the three false positives | v2 |
| `risk-2` | r1 | major | accepted | Filed with `arch-2`; resolved once — see that row. On the finding's secondary point (the exclusivity, acyclicity and chain-depth machinery serving only an unbuilt family): the **exclusivity rule is deleted** with the column, and the **acyclicity/forest rule is deliberately kept** — it is a cheap parse-time refusal that converts a `CyclicGraphException` raised deep inside Snakemake into a named diagnosis, which is a benefit to the family that exists today, not only to a future one. The "a chain two deep" elaboration is retained only as an illustration in §5.2.2, not as a specified capability | v2 |
| `risk-3` | r1 | major | accepted | **Owner ruling R-6** took the timing question. §5.2.2 specifies **P2b** as a milestone precondition: the (i-c) + (iv) + `derived_from` composition, on P2's existing standalone harness, in **three** states — subtree resolvable, subtree unresolvable, and `DERIVED_FROM` **empty** (which also settles GF-2's `[assumed]` empty-alternation semantics and is the CFS-variant case). Run before §8.2 step 1; GF-1 remains the in-workflow gate and explicitly must not be the first observation. §7's C-5 and §8.3 are rewritten to say why: a failed GF-1 after the landing is a redesign (§4: "the seam must move"), and the rollback machinery is written for a defect | v2 |
| `risk-4` | r1 | major | accepted | Filed with `arch-3`; both fixed. §5.8's cross-artifact invariant **drops the second clause**: the entry-key set equals the `evaluated: true` run set, full stop. Verified unsatisfiable as v1 wrote it — the per-run catalog is a `temp()` output of rule 3.14 (`run_stress_test.smk:1085-1088`), pull-scheduled from 3.15's demand, so under `run_historical: false` no catalog exists for the unevaluated root while v1's invariant demanded one. The asymmetry is stated **once** and cross-referenced from §5.4.3's invariant 6, as the finding asked. Additionally the **latent defect is removed rather than pinned**: `validate_wg5_catalog_grid` builds its expected keys as `m in range(0, st_num + 1)` unconditionally (`interchange_contracts.py:1327-1331`), assuming `ST_START = 0`; the migration is the moment to fix it since the validator is being rewritten anyway | v2 |
| `risk-5` | r1 | major | accepted | **Answered explicitly: the empty `st_id` IS a valid grouping key**, stated normatively in §5.1.2 in three parts — it is a key like any other, a grouping MUST NOT drop null or empty keys (`dropna=False` is contract, not choice), and the minter mints its bundle. Restated in §5.4.1b, where it bites: after R-2 the only bundle-grained metrics are the two Class-B return levels, and today's reducer does emit them for the baseline design point (`export_wflow_results.py:373-427`). The design states why neither of v1's instruments would have caught it — GF-9's comparator fails structurally first (`domain-5`), and invariant 6 compares the emitted set to a namespace that would be short in the same way. **Invariant 7** added and falsifier **GF-13** | v2 |
| `risk-6` | r1 | major | accepted | **Owner ruling R-5:** W4 is direction, not a ruling; the single mixed sequence stays, but the rejection may not rest on deference. §6's A3 entry is rewritten. The deference is **withdrawn** and the inconsistency acknowledged (v1 refined W4 in §5.5.1 while treating it as dispositive in §6). A3's real case is stated first and includes W2's own *"distinguishable on sight"* clause, which v1 omitted — plus the two couplings A3 pays off, including the one v1 understated. **Rejected on merits:** a prefixed id is a parsed id, and `if unit_id.startswith("b_")` is `_MEMBER_IN_STEM` one level up, which makes the mandatory index optional in practice; and a reserved high block re-incurs A5's own reserved-range objection. "What would change the decision" is stated, and A3 is named as *preferred over A2* if that evidence appears. §5.5.2 now states the coupling accurately (the minter **evaluates** stage-3 groupings over stage-1 rows), so C-4 and C-11 can be judged for what they are | v2 |
| `risk-7` | r1 | minor | accepted | §5.1.2 pins the scenario table's row order as a **normative property**: rlz-major, baseline row first within a realization, matching `_k_members` (`run_stress_test.smk:1120-1121`). §5.5.2's minting clause cites it. The design states the two things that depend on it — baseline churn across six fingerprinted filenames, and the `(rlz, st_id) → run_id` crosswalk that `domain-5`'s GF-9 instrument is built from and cannot be written without. Noted, as the finding does, that this has no live-tree consequence today because the experiment freeze prevents an in-place set change (`risk-1`) | v2 |
| `risk-8` | r1 | **minor** | accepted | Dispositioned at the risk lens's severity, not `arch-7`'s. §5.6 states the change space as **two cases in a table** — a metric naming a different `bundle_by` (a string, so `params:` sees it) versus a grouping function's **body** changing (code, so it does not) — and closes the second by carrying a **digest of the grouping registry's source** through rule 3.16's `params:`. GF-6 is reworded as two commands, one per case, saying which mechanism closes which; v1's single falsifier ("edit a `bundle_by`") could pass while the hazard it was written for was live | v2 |
| `risk-9` | r1 | minor | accepted | The taxonomy is applied to itself. A fourth label **`[cited]`** — verified by reading a named file at a named line, reproducible by opening it, nothing run — is defined in the front matter, and roughly a dozen `[measured — read the function]` / `[measured — read the constant]` / `[measured — grep -n]` / `[measured — the two docstrings]` claims are relabelled. `[measured]` now appears only on P1/P2 rows and commands actually run. The front matter states why this is not cosmetic: the labels are how a reader allocates scrutiny, and both §5.10.1's C24 supersession and §7's own risk pricing rest on the distinction between "P2 ran six mechanisms twice" and "someone read line 79" | v2 |
| `risk-10` | r1 | minor | accepted | §5.7b writes out **replacement clauses** for HM-2, HM-4, HM-5 and HM-6b — path pattern, cardinality, producer and what is explicitly unchanged in each — rather than the instruction v1 supplied, which reached the implementer as a derivation against a success criterion reading "normative text, not sketch". The validator-index rows at `hydrological-model-seam.md:744-763` and `:792-795` are named as targets with their new spellings, including `validate_wg5_catalog_grid` → `validate_wg5_catalog_runs` | v2 |
| `risk-11` | r1 | **minor** | accepted | Dispositioned at the risk lens's severity, not `arch-6`'s. §8.2b separates the two requirements: **reference atomicity** (`naming.md` §7, `AGENTS.md` — no state in which a live document names a dead path) and tree-shape atomicity, both satisfied by a **branch squashed at merge**, which single-commit granularity was never required for. Decision recorded: the landing may be a squashed branch, buying reviewability and bisectability on the milestone's largest change at no cost to either obligation. §8.3 is rewritten to separate three things v1 fused: reverting the **code** (one operation), restoring the **project tree** (a delete-and-re-run, which no revert performs), and a **structural refutation** (not revertible in the section's sense at all — which is why `risk-3`'s P2b exists) | v2 |

## Notes on the disposition pattern

**Zero rejections is a result, not a default.** Every finding was checked against
the repository before disposition, and the code confirmed each one that made a
claim about code — `validate_wg5`'s prefix selector, `check_baseline`'s structural
comparator, `check_not_frozen`'s freeze and its `has_run_successfully` gate, rule
3.15's static output lists, `_category_month`'s single global call and its
first-column collapse, and `validate_wg5_catalog_grid`'s `range(0, st_num + 1)`.
Manufacturing a rejection for balance would have been the dishonest move; the one
place the design pushes back is inside `risk-2`, on the acyclicity rule, and that
is recorded in its row rather than promoted to a disposition it does not deserve.

**One deferral, marked as one.** `domain-7` — fit uncertainty across the seam —
is right and is not closed. §5.4.4a states the position and the route that stays
open; the gap remains.

**Three severity divergences preserved.** `arch-6`/`risk-11`, `arch-7`/`risk-8`
and `risk-1`/`arch-8` each appear twice, at their own filed severities, with
different resolution text, because they are different halves of one defect and the
index records the divergence as deliberate.

**Four things this revision changed that no finding named**, listed so the
external round can see they are not smuggled: grain was already a declared surface
(`METRIC_CLASSES`, `metric_grain`), which reframes §5.4.1 as a refinement and adds
three migration targets; the one-table-one-family rule; the pairing-consistency
refusal; and Goal 1's wording, which was unachievable by any design. The last
three come from `candidate-family-schema.md`.

## V3 refreshed domain-review dispositions — v4 author revision

Append-only addition dated 2026-09-09. These seven rows disposition every
original `domain-v3-*` finding from `internal-review-domain-v3.md` part by part.
They do not alter the 31 historical rows or the separate deferred `domain-7`
fit-interval ruling. **This addition: 7 accepted · 0 rejected · 0 deferred ·
0 withdrawn.** The author-proposed scientific thresholds in `domain-v3-3` await
Astra review and execution; acceptance here means the design now carries the
required criterion, not that the criterion or provisional GEV values have passed.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| `domain-v3-1` | v3-domain | major | accepted | **Adopted, each part.** (1) §5.1 requires every production stochastic perturbed row to derive directly from the unique empty-`st_id` root with the same `rlz`. (2) Roots have no ancestor. (3) Distinct realizations have distinct root `run_id` values. (4) The universal forest rule remains. (5) GF-18 adds cross-realization ancestry, one missing perturbed edge, and a perturbed-parent chain, all refused before generation. (6) An instrumented transform must prove it consumes the declared ancestor. (7) Distinct root ids are explicitly not treated as empirical proof of independent draws. No part rejected. | v4 |
| `domain-v3-2` | v3-domain | major | accepted | **Adopted, each part.** (1) §§6.6–6.7 persist neutral `location_ordinal` from native gauge order; §7.2 resolves and persists `reference_location_id` from the current first-native-q-gauge convention before reduction, so neutral iteration order cannot choose it. (2) §§7.1–7.2 persist reference member ids, monthly-sum wet/dry statistic, current first-label tie rule, exact analysis coverage/effective counts, and resolved wet/dry months. (3) §8.4 puts resolved reference provenance in metric identity. (4) GF-9 uses two gauges with opposite seasonal peaks and reverses native-artifact enumeration and neutral-series iteration while retaining persisted ordinals; reference and months must remain fixed while the numerical crosswalk still passes. (5) R-1/R-2 remain one shared reference month and per-run Class C; no per-location selection is introduced. No part rejected. | v4 |
| `domain-v3-3` | v3-domain | major | accepted | **Adopted, each part under the G1-v3 ruling.** (1) §7.5 separates minimum-sample screening, fit validity, and bounded scientific adequacy. (2) Usable blocks are counted per metric, bundle and location, with member counts, coverage, and preserved partial-block/missingness behavior recorded. (3) Named refusals cover constant samples, estimator failure/non-convergence, invalid/non-finite parameters or scale, and non-finite quantiles using the unchanged supported estimator outputs. (4) GF-15 adds constant-block, location-specific missingness, injected failure, invalid-parameter and non-finite-quantile fixtures. (5) §7.5 supplies a finite seeded GEV matrix and explicit pass criteria; the grid/tolerances are author proposals awaiting Astra, and failure leaves `1.0`/`10` limited screening heuristics pending a separate owner ruling. (6) Ratio `2.0`, estimator change, partial-year removal, and fit intervals are explicitly not automatic fixes. (7) Historical `domain-7` interval deferral remains separate for G2. No part rejected; no unrun adequacy is asserted. | v4 |
| `domain-v3-4` | v3-domain | major | accepted | **Adopted, each part.** (1) §7.4 adds canonical `metric_environment.json` for the actual stage-3 execution. (2) It covers the response reader/reducer numerical stack, including xclim, SciPy, pandas, NumPy, xarray, load-bearing transitive numerical libraries, and lock/environment revision, not only repository bytes. (3) §8.4 includes its digest in `metric_set_id` and persists path/digest in `metrics.json`; the old stage-2 environment remains only stage-2 provenance. (4) GF-6 changes the descriptor with collection, simulation and source bytes fixed and requires a new metric set only. The alternative of forcing exact historical stage-3 environment reuse is rejected because the adopted descriptor-and-new-identity path preserves auditable recomputation without forbidding dependency updates. | v4 |
| `domain-v3-5` | v3-domain | major | accepted | **Adopted, each part.** (1) §9.4 defines the explicit `generation-seed-material/1` projection. (2) It excludes namespace capacity, id width/spelling/order, storage/retention/readiness choices and other non-climatic execution settings. (3) Capacity remains in `collection_id`, as approved, so a headroom change creates a separate namespace without selecting a different random draw. (4) GF-27 requires capacity-only and id-width-only variants to preserve `resolved_seed` and forcing values under a run crosswalk while collection identity changes. (5) Migration of old explicit/default/auto seeds to explicit old integers remains. No part rejected. | v4 |
| `domain-v3-6` | v3-domain | minor | accepted | **Adopted, each part.** (1) §§5.3, 6.6–6.7 name source, prepared-forcing and response calendars separately. (2) They record the existing noleap-CFTime to `datetime64`/standard conversion, configured-window clip, actual endpoints and TOML endpoint refresh. (3) GF-28 covers leap-year, right-labelled terminal endpoint and non-January water-year cases, including partial blocks and annual reductions. (4) Any existing mismatch is reported for a separate method decision; the migration must not silently repair calendar or annual-block semantics. No part rejected. | v4 |
| `domain-v3-7` | v3-domain | minor | accepted | **Adopted, each part.** (1) §14.1a retains separate qualifications and settling observations for E6 (failure association remains needed), E13 (current source is WG-4), E17 (renumbering is conditional), and E19 (representability is not coordinate comparability). (2) The frozen intake is unchanged. (3) E19 stays outside current production scope; its future settling observation requires an example with explicit reference period, aggregation, units, and comparable surface coordinates, while projections remain terminal. (4) No extra scientific run is required for E13 attribution or E17 algebra. No part rejected. | v4 |

## External round 1 dispositions — v5 author revision

Append-only addition dated 2026-09-09, against `external-review-r1.md` on v4
SHA-256 `5511344AE8D0EA818723734A986B119A80EE524DFEA61C397975613205B5AE55`.
**4 accepted · 0 rejected · 0 deferred · 0 withdrawn; all filed major.**
Acceptance records proposed design resolutions, not reviewer approval of v5 or
empirical passage. The `domain-v3-3` v4 row remains historical; `ext1-1` below
refines its insufficient benchmark-to-policy inference. No old row is altered.
The historical `domain-7` Class-B fit-interval gap remains for owner ratification
or overturning at G2; it is not closed by these dispositions.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| `ext1-1` | external-r1 | major | accepted | **Adopted, each part.** (1) §7.5 separates benchmark validation from count-policy applicability: even a matrix pass leaves `1.0`/`10` provisional operational screening; no actual-bundle adequacy is inferred. (2) Adds generating-scale-normalized signed/absolute errors and proposed pass criteria; translated paired samples explicitly test location invariance. (3) Justifies relative-error ratios through `rho=q_p/sigma={0,0.05,0.5,2,10}`, chosen to expose sensitivity, not as representative discharge frequencies; all cells and fit-failure denominators are reported. (4) Includes zero/near-zero low-flow diagnostics, records undefined relative error at zero, excludes these from validated relative-error claims, and persists the limitation, exact benchmark domain/status/report and unestablished actual-bundle applicability in `metrics.json`; the evidence annotation enters metric identity. (5) Retains the current estimator, ratio/floor, annual-block treatment and no fit intervals; every benchmark tolerance including translation tolerance is an author proposal requiring Astra/owner review before execution. GF-15 is updated and a future policy-validation claim requires separately approved applicability/dependence evidence. No suggested part rejected. | v5 |
| `ext1-2` | external-r1 | major | accepted | **Adopted, each part.** (1) §5.4a inventories every existing preparation argument and assigns generation/collection or simulation/model ownership, grounded in baseline rule 3.14 and both preparation scripts. (2) Selects durable collection packaging: current catalog reader semantics, CHIRPS/CHIRPS-global source-grid orography and the non-CHIRPS catalog-selected ancillary closure are copied/resolved under collection-relative paths; original catalogs/store are provenance, not consumption dependencies. (3) Adds scenario-neutral `PreparationContext` to `RunForcing`, with resolved PET method, reader and elevation metadata; source labels no longer choose stage-2 behavior. Schemas, validation, source inventory, context digest in collection intent/revision and migration are explicit. (4) GF-31 prepares after the original store/catalogs are unavailable and compares against the existing path, with missing/stale ancillary negative cases and other current source/PET branches. Separately inventoried simulation auxiliaries are not chosen because collection packaging preserves the approved portable handoff with fewer consumer inputs; this adopts the review's offered packaging alternative rather than rejecting the finding. | v5 |
| `ext1-3` | external-r1 | major | accepted | **Adopted, each part.** (1) §5.2's source checkpoint completes generation-owned historical extraction and inventories actual bytes before final collection intent; a pre-source request key is only scheduling state and excludes unresolved seed. Pure scenario-row enumeration survives. (2) §8.4a's metric-plan checkpoint waits for responses, resolves reference provenance and the final content id, then exposes declared metric output targets through public checkpoint input functions. (3) Both content-derived scientific identities remain; no hidden runtime publication directory, third workflow, latest pointer or general run-control system is added. (4) §9.5 makes runner/direct behavior one invocation per entry point, with exact success paths and honest partial fresh dry-runs. (5) GF-29 begins without historical extraction/model, GF-30 without responses/metric plans; each must publish and identify the correct final directory in one direct and one runner command, with stale-plan/mode/direct-filename cases. These bounded mechanisms fill previously unspecified scheduling within approved independent execution; private-API or changed-invocation fallbacks require escalation, not silent broadening. No suggested part rejected. | v5 |
| `ext1-4` | external-r1 | major | accepted | **Adopted, each part.** (1) §7.4 derives expected `(metric,location,unit_id)` keys independently from selected declarations, declared grains, exact bundle memberships, evaluated runs and required locations from the immutable response request; missing location requirements cannot shrink applicability. (2) Publishes only when emitted keys are globally unique and exactly equal that independently rederived set; expected keys/resolution/digest are persisted and HM-7 checks all of them on later consumption. (3) Adds per-metric `value_validity` declarations and separate checking; invalid/omitted Class-B results refuse the whole ready marker. (4) GF-10 removes one return-level metric-location-unit row and duplicates another while preserving index/design coverage, and requires refusal in fresh and metrics-only cases; extras and prohibited values are covered separately. No suggested part rejected. | v5 |

## Owner-directed interface simplification — v6

Append-only addition dated 2026-09-10. This is an owner change, not a reviewer
finding or disposition, and it does not alter the finding counts above.

| Change ID | Authority | Resolution | Doc version |
|---|---|---|---|
| `owner-v6-interface` | Owner simplification ruling in `status.md`, 2026-09-10 | Routine simulation omits collection fingerprints, manifest paths, and a selector. Default resolution verifies the one request-plan-named ready collection without scanning or fallback; advanced reuse may supply an explicit manifest. Metrics-only reopens the immutable simulation selected by `experiment_name`, treats generation/model/simulation settings as retained provenance, and forbids switching its recorded collection. Internal collection/simulation identities, source and metric-planning checkpoints, readiness/content checks, stale refusal, unit-capacity safety, and every scientific/validation obligation remain unchanged. | v6 |

## G2 owner adjudication — 2026-09-10

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| `domain-7` | G2 owner adjudication | major | rejected | Owner approved the final reviewed v6 after the final package explicitly identified its Class-B fit-interval deferral. This accepts the documented limitation for this design: fit intervals remain outside this implementation scope, so outputs cannot distinguish surface gradients from estimator noise through intervals. The concern is not withdrawn or downgraded; empirical estimator/policy validation and uncertainty limitations remain explicit. | v6 accepted |
