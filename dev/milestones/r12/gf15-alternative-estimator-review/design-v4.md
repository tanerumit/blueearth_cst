# Method Spec — GF15 alternative GEV estimator

Status: proposed; v4 frozen for scoped verification after owner arbitration; G1 approved; G2 pending  
Date: 2026-09-12  
Authors: cst-architect author agent  
Supersedes: none; original and normalized-B qualifications remain failed  
Lifecycle: maintained-current during review; append-only revision log; after approval, frozen-with-supersession at `dev/milestones/r12/gf15-alternative-estimator-design.md`  
Revisions:
- 2026-09-12: v1 proposes a bounded L-moment qualification; no execution authorized.
- 2026-09-12: v2 resolves domain-1, domain-2 and risk-1; G1 selection recorded; estimator, criteria and no-execution scope unchanged.
- 2026-09-12: v3 resolves external/full-panel contracts and explicitly rejects unsupported causal attribution and resampling expansion; original analytical tolerances, estimator and GF15 criteria retained.
- 2026-09-12: v4 applies owner-approved ext2-1/2/3 repairs only: quantile-space CDF reference, population-gate precedence and bounded oracle bracket; scoped verification and G2 remain pending.

Reading guide: Inputs binds retained evidence; D1/D2 define the estimator; D3–D6 define readiness and qualification; Uncertainty bounds interpretation. Length exceeds the intake target because exception, join and branch contracts must be reviewable without rediscovery. Finding-by-part dispositions remain in the append-only [ledger](ledger.md).

### Context and motivation

**G1 selected range-normalized GEV estimation from unbiased sample PWMs/L-moments for proposed qualification, not adoption.** Unbiasedness does not extend generally to fitted parameters or quantiles. The rationale is a different estimation principle with inspectable numerical behavior; passing the unchanged GF15 targets is unknown and appears demanding. No source establishes those targets at n=18.

The [intake](intake.md), E1–E9, fixes authority. Original fitting passed only 3/24 baseline accuracy cells. Normalized B also passed 3/24 and 6/72 eligible relative cells, despite accepting every n=18 baseline fit. B retained 30 accepted-pair translation failures, 70 both-refused pairs and one changed-acceptance pair. Its 974 accepted fits with nonfinite physically mapped log-density have unresolved causes. These observations support investigating both sampling variability and computation; they do not prove that optimizer error explains the accuracy deficit. Sources are the original and B `scientific-handoff.md` files under `dev/milestones/r12/implementation/evidence/gf15/` and `gf15-normalized-qualification/`.

### Scope

Request type: **improve**, limited to a method proposal. Roadmap: unstable/insufficiently accurate return levels → qualify one explicit alternative → determine bounded adequacy or preserve failure → affect WF4 performance analysis only if separately approved.

| Capability slot | Binding and immutable revision | Owner / validation authority |
|---|---|---|
| `performance_analysis` | Proposed L-moment GEV below; lmoments3 1.0.8 wheel digest below; future adapter revision not yet created | python-engineer implementation / model-validator scientific acceptance |
| `orchestrator` | Proposed qualification stage sequence below, frozen design-v4; no Snakemake modification | cst-architect / independent evidence reviewer |

`decision_framing` is not applicable: no assessment decision or criterion changes. `data_adapter`, `ensemble_generator`, `simulation_engine`, and `system_model_validation` are not applicable: no input preparation, weather generation, system simulation, or system-model validation occurs. `impact_model`, `robustness_evaluation`, and `projection_overlay` are not applicable: none of their outputs is declared. Projections remain a terminal plausibility overlay; stochastic perturbations remain stress-test forcing.

This document authorizes no fits, benchmark execution, installation, production change, screening change, or seal. No managed-project run is entering `ready`; namespace claim and run-control conformance are **not applicable to this authoring stage**. A future managed-root run must satisfy cst-run-control before readiness. Current production remains pinned by repository commit `85d0b8b6b4f8b078d96f645b1049ecd477c30998`.

### Assumptions

The fixed fixtures are IID stationary GEV blocks with known generating parameters. They represent neither discharge observations nor independent evidence of pooled-bundle stationarity. Negative fixture values are legitimate and remain untruncated. Population L-moments require finite mean, hence SciPy shape c > −1; all generating shapes satisfy this. Dependence, mixtures, structural misspecification, and nonstationarity remain outside qualification.

The low-flow estimand stays the median (p=.5) of annual seven-day minima, with no sign reversal; peak flow uses p=.9. Existing per-member rolling/block extraction, missingness, calendars, warm-up and pooling remain unchanged. Operational screening remains provisional ratio 1.0 and floor 10; passing a numerical fit or IID cell cannot validate them.

### Inputs and provenance

Authoritative criteria: `dev/milestones/r12/implementation/evidence/gf15/results/criteria.json`. Preserve that file byte-exact; its estimator description identifies the predecessor. A separate candidate manifest declares D1 as the proposed replacement without modifying numerical criteria. Reuse all original retained draws and offsets: c={−.2,0,.2}, n={10,18,30,60}, 1,000 draws/cell, generating location zero and scale one; p={.9,.5}; translated locations μ=ρ−zₚ for ρ={0,.05,.5,2,10}. Provenance identifies PCG64 with `SeedSequence([20260912, shape_index, count])`; do not redraw, clip or substitute observations. These synthetic, dimensionless inputs have no basin, calendar coverage, or external data licence dependency.

**Input inventory binding.** Paths here are relative to `dev/milestones/r12/implementation/evidence/`. Freeze these inventory anchors and select their `files[].path`, `bytes`, `sha256` entries as the complete file-level manifest:

| Anchor | SHA-256 |
|---|---|
| `gf15/artifact-inventory.json` (A) | `3f13145d29c377a1c92042b3845695ce293edd58e565e17a67851767026719e1` |
| `gf15-normalized-qualification/artifact-inventory.json` (B) | `07148bb3b165ef2d1785fb49fc57908d260009cd7885c7b39712e1148aefdf4a` |
| `gf15/results/criteria.json` | `988611b6d239b95f7542f8d67470395e4997ac61dd97198fd075fdcba552d643` |
| `gf15/results/provenance.json` | `0d85d2a63c6be70683e2a1485de7bfc6266a265fd383cf96b124b6facdc67db2` |
| `gf15-normalized-qualification/results/provenance.json` | `a1ea18808890a1c270e75a35441b1bb4eecdb901551c6fafce0565be822686ae` |

Select all 12 A `results/samples-c{i}-n{n}.npy`, all 240 A `results/draws-c{i}-n{n}-{start:04d}.jsonl.gz`, all 240 B `results/fits-c{i}-n{n}-{start:04d}.jsonl.gz` and their 240 `.json` receipts: i=0,1,2 maps to c=−.2,0,.2; n=10,18,30,60; start=0,50,…,950. Receipt chunk hashes, fit/quantile counts and binding digests must agree. Original arrays are loaded with `allow_pickle=False`; the supplied physical sample is `samples[draw] + A.mu`, as in retained `gf15-normalized-qualification/qualify-normalized.py`. Seed regeneration is not permitted as an input or a new check in this scope; the recipe is provenance only.

The E6 correction in intake applies: B's criteria serialization has hash `0181b4a1044c66ab4c819fded1ae39d35608ea375ddc46bdaf2d87587714ddca`; its `criteria_exact` audit means decoded JSON equality, not byte equality. Never substitute it for A's bytes. These anchors and representative schemas were checked in [repo-fit review](internal-review-repo-fit.md); complete file integrity and key coverage remain D4 readiness work, not completed evidence.

Pin Python 3.12.13, NumPy 2.4.6 and SciPy 1.18.0 from the predecessor environment; retain its full provenance and lock digest. Proposed additional package: `lmoments3-1.0.8-py3-none-any.whl`, SHA-256 `984d1f1b0c3feefd57afc7a270931c1d28e4face2df1c2293559faf47681ef5e`, from [PyPI release metadata](https://pypi.org/project/lmoments3/1.0.8/). Installation is a later approval dependency. The lock currently contains only an xclim constraint, not an installed lmoments3 binding (E9). The downloaded wheel, installed source digests, adapter commit, effective environment and input/criteria hashes must be frozen before execution; documentation labelled “stable” alone is insufficient provenance.

B records xclim 0.60.0 and predecessor lock SHA-256 `ca888ef1c4547ee0624fc4da319de87fd9284418b2fbe16171118bc8f7b64041`. Record the candidate's effective environment separately; adding its declared wheel cannot be represented as an unchanged predecessor lock.

### Method

**D1: Complete candidate.** Fit one three-parameter GEV to each supplied finite float64 sample; repeat independently for every translated sample. The future adapter has the proposed interface `fit_case(sample, probabilities) -> record`, not an existing command. It receives no generating shape, location, scale, truth, ratio or draw identifier. The evaluator owns those, preventing oracle leakage.

1. Refuse invalid dimensionality, any nonfinite supplied value, fewer than four values, or a constant sample. Missing-block filtering remains upstream; never silently filter these fixture inputs. Set a=min(sample), s=max(sample)−a and y=(sample−a)/s. Refuse nonfinite/nonpositive s or nonfinite y. Keep all ties. This positive affine transform is numerical conditioning, not a fitted error denominator.
2. Obtain `lmoments3.lmom_ratios(y, nmom=3)`, yielding l₁, l₂, t₃. The underlying unbiased order-statistic PWM definition is bᵣ=n⁻¹Σᵢ[C(i−1,r)/C(n−1,r)]y₍ᵢ₎, r=0,1,2; l₁=b₀, l₂=2b₁−b₀, t₃=(6b₂−6b₁+b₀)/l₂. This is not a plotting-position variant. For IID samples with existing expectations, raw sample PWMs and linear sample L-moments are unbiased for their population counterparts; t₃, fitted GEV parameters and return levels are not generally unbiased. Sample-dependent normalization supplies conditioning, not an additional unbiasedness claim. [Hosking et al. (1985)](https://www.stat.cmu.edu/technometrics/80-89/VOL-27-03/v2703251.pdf), [sample API/source](https://lmoments3.readthedocs.io/stable/_modules/lmoments3.html).
3. Use `lmoments3.distr.gev.lmom_fit(lmom_ratios=[l1,l2,t3])`; read named keys `c`, `loc`, `scale`. The theoretical shape equation is t₃=2(1−3⁻ᶜ)/(1−2⁻ᶜ)−3, with the continuous c=0 limit. For nonzero c, σᵧ=l₂c/[Γ(1+c)(1−2⁻ᶜ)] and μᵧ=l₁−σᵧ[1−Γ(1+c)]/c. These equations explain the estimator; the pinned library implementation defines its approximation. Its rational branches use t₃=0 and −.8 boundaries; t₃≤−.97 changes the starting value. The iterative branch allows 19 updates with relative shape tolerance 1e−6. The positive-t₃ branch snaps |c|<1e−5 to Gumbel, using Euler constant .57721566. Preserve these disclosed choices; do not silently replace them with a different root solver. [GEV implementation](https://lmoments3.readthedocs.io/stable/_modules/lmoments3/distr.html).
4. Refuse nonfinite L-moments, l₂≤0, |t₃|≥1, only the allowlisted exceptions below, nonfinite parameters, c≤−1, or σᵧ≤0. The c domain is the finite-mean L-moment domain, not a tuned tail prior. There is no optimizer status flag to infer; successful return alone still faces all checks. No trimming, shrinkage, empirical-quantile fallback, or retry is allowed.
5. Conventional ξ=−c. For each requested p, calculate zₚ=−expm1(c log(−log p))/c, or −log(−log p) when c=0; then qᵧ=μᵧ+σᵧzₚ and q=a+s qᵧ. Use float64 NumPy operations. Refuse p outside (0,1), nonfinite intermediate/returned quantiles, or nonfinite back-mapped μ=a+sμᵧ and σ=sσᵧ. This is the primary quantile route; physical-parameter SciPy PPF checks mapping, sign and transcription but shares the inverse expression; it is not an independent derivation or a fallback. [SciPy sign/distribution convention](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.genextreme.html).

**Exception boundary.** Catch only around each named library call, then classify or re-raise. The exhaustive exception-derived refusal allowlist is exact built-in `ValueError` with message `L-Moments Invalid`, and exact built-in `Exception` with message `Iteration has not converged`, both raised by the verified `lmoments3.distr.GenextremeGen._lmom_fit` source statement. Match the traceback's raising frame, function and installed-file digest, not the exception class module (`builtins`); an identical message from another frame is a run failure. D1 prechecks ordinarily prevent the first case. All other exceptions stop execution, including every `lmom_ratios` ValueError: numeric conversion, moment-count and length errors contradict this adapter's prevalidated float64 sample/nmom=3 contract. No blanket ValueError, arithmetic-error or adapter-wide catch is allowed. Record type, class module, raising module/function/file digest, source location and verbatim message; count exception-derived refusals separately from explicit domain refusals. Unexpected faults invalidate the attempt, never become statistical rows. The [sample source](https://lmoments3.readthedocs.io/stable/_modules/lmoments3.html) and [GEV source](https://lmoments3.readthedocs.io/stable/_modules/lmoments3/distr.html) identify these paths; Stage 1 must verify the wheel matches.

**D2: Record and validity.** Return input/range/normalization data, normalized l₁(y), l₂(y), l₃(y)=t₃l₂(y), back-mapped l₁(x)=a+s l₁(y), l₂(x)=s l₂(y), l₃(x)=s l₃(y), and invariant t₃ separately. Step 2's unbiasedness statement attaches to raw-sample/back-mapped linear moments in exact arithmetic, not to moments of the randomly normalized distribution; floating-point recovery is not an exactness claim. Include normalized and physical parameters, requested probabilities/quantiles, acceptance, all refusal reasons, warnings and source identity.

Baseline acceptance is the conjunction of common fit validity **and both** requested quantile checks. A failure at either probability refuses both baseline rows; retain each probability's diagnostic status and mark the other row's refusal as shared-baseline revocation. Both cells use that shared status for their all-1,000 valid rates and accepted-error cohorts. Each translated fit requests one scalar probability and uses its own conjunction. Translation acceptance compares that scalar status with the shared baseline status. Preserve A's original per-probability `valid` and B's shared `accepted` unchanged; never harmonize predecessor acceptance. Record normalized and physical support membership and log-density separately. A PWM endpoint excluding a sample value is a model-fit diagnostic, not automatically computational invalidity; do not retrofit a likelihood refusal into this candidate or B. No scientific suitability claim follows from computational acceptance.

### Parameters and defaults

All controls above are fixed prospectively. There is no user shape bound, prior, restart budget or tunable plotting position. The documented public xclim PWM route requires an lmoments3 distribution instance; `dist="genextreme"` alone does not supply that interface. The direct candidate API preserves exceptions and avoids silently changing the production wrapper. [Official xclim source](https://xclim.readthedocs.io/en/stable/_modules/xclim/indices/stats.html). Production integration would require a separately reviewed adapter and new immutable metric identity under R12 §8.4.

### Validation regime

**D3: Unchanged gates.** Complete coverage is 132,000 fits, 144,000 quantile rows, 144 summary cells and 120,000 translation pairs. Error is eσ=(q̂−q)/σ_generating. Every cell requires valid rate ≥.95 over **all 1,000 draws**, |median(eσ)|≤.10 and P90(|eσ|)≤.50 upper/.25 lower. Report median absolute error too. Use NumPy quantile `method="linear"`; error summaries condition on valid fits and expose that denominator.

At ρ={.5,2,10}, also require |median(e_relative)|≤.10 and P90(|e_relative|)≤.50 upper/.25 lower, where e_relative=(q̂−q)/|q|. At zero it is undefined, recorded as null; .05 is diagnostic. Every translated pair requires unchanged acceptance and |Δeσ|≤1e−6 when both accepted. Both-refused pairs are never numerical passes. No averaging, post hoc exclusion or changed threshold repairs failure.

Proposed sequential handoffs, all execution stages pending separate authorization:

| Stage | Owner and deliverable | Gate / stop rule |
|---|---|---|
| 1/3 implementation readiness | python-engineer: isolated adapter, frozen dependency, effective command, manifest/schema, D4 joins and D5/D6 controls | model-validator accepts equations, sign, source match and all readiness checks; stop on unavailable dependency, undefined/failed controls, ambiguous source behavior or incomplete joins |
| 2/3 bounded qualification | authorized executor: one complete original matrix, raw rows, warnings/refusals, paired comparisons, summaries and timing | stop on corrupted input, coverage or control failure; statistical failures remain visible and the complete matrix is reported |
| 3/3 independent validation | model-validator: independently recomputed counts/errors/criteria and signed handoff | any criterion failure leaves candidate failed and GF15 unresolved; no stage integrates without returned validation |

**D4: Comparison readiness.** Canonical quantile key is `(shape_c,n,draw,p,ratio)`: baseline `ratio=null` is distinct from translated zero. Map and flatten as follows, retaining refused rows:

| Quantity | A draw row | B fit row, flattened once per nested quantile |
|---|---|---|
| Shape/count/draw | `shape_c`, `n`, `draw` | Same top-level fields |
| Probability | `p` | `quantiles[j].p`; baseline top-level p is null |
| Case/ratio | `ratio` | top-level `ratio` |
| Acceptance | `valid` | top-level `accepted` shared by nested rows |
| Estimate/errors/truth | `estimate`, `scale_error`, `relative_error`, `true_quantile` | Same names under `quantiles[j]` |
| Physical offset | `mu` | top-level `mu`, inherited from A |

Do not join on B's `fit_id` or substitute its `original_*` copies for pinned A rows. Before any candidate matrix fit, verify every selected hash/size/receipt, array shape `(1000,n)` and float64 dtype, legal draw IDs 0–999, unique complete expected 144,000-key sets for each predecessor, two baseline quantiles and one per translated fit, and consistent truth/offsets. A's two baseline rows must share the same offset. Store the canonical mapping and verification result in readiness evidence. Stop for missing, duplicate or inconsistent keys, mismatched inputs, or any requirement to refit predecessors or change cohorts. Representative records support feasibility; no complete check has run here.

**D5: Analytical and branch readiness.** Controls remain planned, not executed. Retain population parameter recovery at c={−.2,0,.2}, μ=0, σ=1 using independently derived population moments, including c=0 limits l₁=Euler's constant, l₂=ln2, t₃=2ln3/ln2−3. Do not use the package's forward-moment routine as truth. Original pass tolerances stay **1e−5** absolute in shape and **1e−5** in location/scale error divided by supplied population scale. Their adequacy is unmeasured; they are prospective requirements, not a published approximation-accuracy claim.

Add these fixed inversion controls with l₁=0, l₂=1; define τ(c) by D1's theoretical relation and its continuous zero limit. Decimal literals specify exact inputs before recorded float64 conversion.

| Branch/control family | Fixed t₃ inputs | Reference and disposition |
|---|---|---|
| Positive rational | .50, τ(.2) | Independent root/parameter oracle below; characterize discrepancy subject to retained population-gate precedence below |
| Gumbel snap and both sides | τ(0), τ(−.000005), τ(.000005), τ(−.00002), τ(.00002) | Oracle plus pinned pre-snap G predicate; record snapped/unsnapped result |
| Zero branch boundary | −.000001, 0, .000001 | Oracle; first two use nonpositive rational branch |
| Nonpositive rational | −.30, −.799999, −.8 | Oracle; characterize discrepancy |
| Newton boundary/interior | −.800001, −.85, −.969999 | Oracle; Newton from rational start |
| Alternate-start boundary/interior | −.97, −.970001, −.98 | Oracle; Newton with alternate start |

The validation-only shape oracle solves τ(c)=t₃ by bracketed bisection at 80 decimal digits using Python's standard-library Decimal logarithm/exponential. Start at [−1,1], assigning τ(−1)=1 exactly: this closed mathematical endpoint is valid for bracketing although every fitted/returned shape must remain strictly c>−1. For g(c)=τ(c)−t₃, require g(−1)>0; if g(1)>0, double only the upper endpoint through 2,4,8,16,32,64, stopping at the first g(c_hi)≤0. An exact zero returns that endpoint. Six doublings is the hard limit: τ(64)=−1+2(2⁻⁶⁴−3⁻⁶⁴)/(1−2⁻⁶⁴)<−.98, the smallest fixed control input, so this bound covers the frozen controls without an empirical search. Bisect the sign bracket to width ≤1e−40 within 256 steps. Repeat at 120 digits and require roots to agree within 1e−35. Missing sign change, exhausted budget, nonfinite evaluation or unresolved sign stops oracle readiness with t₃, precision, endpoint values and iteration counts; never turn it into a candidate refusal or extend the search silently. This oracle serves only the fixed population/branch controls, not individual matrix fits. It introduces no package dependency and never replaces the fitted estimator. Derive reference scale/location from D1's equations with the c=0 limits. Evaluate Γ independently by log-Gamma recurrence shifting its positive argument to at least 100, then the Stirling series with exact rational Bernoulli coefficients and a first-omitted-term bound ≤1e−40; increase the shift if needed. Freeze independently sourced high-precision π/Euler constants and verify reference parameters agree within 1e−30 between precision passes. Stage 1 must freeze this oracle implementation, constants, convergence evidence, rounded inputs and expected outputs before matrix execution; a self-agreeing library forward/inverse pair is insufficient. The model-validator reviews this independent implementation. If a required reference cannot be resolved reliably using the declared environment, stop for design review; no silent dependency addition.

For every new branch row, report |Δc|, |Δloc|/σ_reference and |Δscale|/σ_reference against the independent oracle, each with the same **1e−5** reference line. This is **characterization**, not a new per-branch accuracy gate or a measured guarantee. Original population gates take precedence wherever quantities overlap: Stage 1 computes the τ(−.2) and τ(.2) population shape discrepancies and existing gate outcomes first, and completes all three population controls, including c=0, before freezing branch characterization. The τ(.2) branch row repeats the population shape inversion because shape depends only on t₃; its discrepancy is gated, not merely descriptive. This adds no τ(−.2) branch-table entry. An explained library approximation cannot override any retained population failure. Separately require exact branch selection and exact same-environment agreement with the unmodified, source-verified library call on identical inputs; this tests adapter fidelity, not mathematical accuracy. Freeze the characterization table in the signed readiness handoff. An unexplained discrepancy, unverified branch, nonfinite reference or failed original population control stops readiness. An explained library approximation discrepancy in a new branch row is reported as such, never relabelled an adapter defect or silently used to relax an original control. Any proposed change to an original analytical tolerance returns to design review before the matrix. Numerical discrepancies remain unknown now.

Record an exhaustive branch-usage histogram over 132,000 attempted fits, including pre-inversion refusals, positive snap/non-snap, nonpositive rational and both Newton starts. Infer predicates from recorded t₃ and pinned coefficients without modifying the estimator. Stage 3 checks that every reached branch has readiness evidence; finite controls establish branch coverage, not an accuracy bound throughout each branch.

**D6: Other control contracts.** Preserve explicit invalid/constant/tied samples, no RNG use, exact same-environment replay and unchanged refusal classification. At readiness, verify allowlist positives at their actual library source, and negative classification for wrong type/message/origin, conversion ValueError, MemoryError and adapter faults; synthetic classifier records may test non-convergence classification without asserting an unobserved natural sample trigger. Require those faults to stop the attempt, not lower valid rate. Include a single-probability failure control demonstrating D2's conjunction. All fixed translated pairs still test affine behavior.

Retain analytical-versus-SciPy quantile agreement at **1e−10 × max(1,|qᵧ|)** as mapping/sign/transcription control. For every analytical/branch control at p=.5,.9, independently invert the forward CDF and compare its reference with the **adapter's returned float64 q̂ᵧ**, under exactly the same recorded float64 (c,μᵧ,σᵧ,p) used to produce q̂ᵧ. Convert those floats exactly to Decimal, not through shortened decimal display strings. Do not use the D5 theoretical parameter tuple here: D5 measures fitted-parameter approximation, while this control checks the quantile calculation conditional on its actual parameters. Fix each row's bound as **T=1e−10 × max(1,|q̂ᵧ|)**, using the exact value of the returned float; this preserves the existing quantile tolerance form, not a probability-space threshold or a new density/ulp allowance.

Construct the reference using only the standard-library Decimal approach at 80 and 120 digits, independently of the adapter inverse expression and SciPy PPF. With x=(q−μ)/σ and ξ=−c, evaluate the forward CDF F=exp(−w^(−1/ξ)), w=1+ξx>0, or F=exp(−exp(−x)) when ξ=0. Bisection compares log F to log p, avoiding unnecessary underflow; it must never obtain its root by evaluating the analytical inverse. At ξ>0 the finite endpoint is x=−1/ξ, a lower endpoint with F=0 on/below it; at ξ<0 it is an upper endpoint with F=1 on/above it. At ξ=0 there is no finite endpoint. These extended CDF values supply valid bracketing signs; evaluate no logarithm of nonpositive w. Start x at [−1,1]; double each failing side's magnitude independently until F(x_lo)≤p≤F(x_hi), at most 64 doublings per side. Then bisect at most 2,048 times, mapping the retained bracket to q with the same recorded μ,σ. The finite budgets are fixed control-only engineering limits, not guarantees for arbitrary fits; exhaustion stops readiness with the tuple/bracket diagnostics and returns to review.

Enclose reference arithmetic with outward-rounded Decimal intervals: directed rounding for arithmetic, and adjacent Decimal values around correctly rounded ln/exp results for transcendental bounds. Propagate endpoint and comparison uncertainty; an unresolved bisection sign stops that precision pass unless its current bracket already meets the width target. Each precision pass must return a root enclosure of width ≤1e−30 × max(1,|q̂ᵧ|); require the two enclosures to overlap and use their enclosing hull [L,U], of width ≤2e−30 × max(1,|q̂ᵧ|). Thus construction, arithmetic and termination uncertainty together consume at most 2e−20 of T. Compare the exact float64 q̂ᵧ to this Decimal interval: pass only if max(|q̂ᵧ−L|,|q̂ᵧ−U|)≤T; fail if its minimum distance exceeds T; otherwise stop as unresolved reference precision, never widen T. The gate uses the unrounded enclosure, so rounding a reference for display consumes no hidden tolerance. Record its nearest-float64 display value and ≤half-ulp rounding allowance separately, without adding that allowance to T. No fixed absolute probability residual determines representability or acceptance; record F(q̂ᵧ)−p only as a diagnostic.

Also apply the independent CDF inversion to the adapter's affine-back-mapped float64 q under its recorded physical parameters. Express its error and reference width in normalized units by dividing by the recorded positive s, and use the same normalized T; do not compare it with a reference using unrounded physical parameters. Record endpoint membership and float64 rounding separately in both coordinate systems: an output at or just beyond a finite endpoint is a representable rounding effect only if its nearest-rounding cell (midpoints to adjacent floats, with ties-to-even) intersects the distribution's support and it passes the quantile-space gate. Otherwise an unsupported output stops readiness. A wrong endpoint direction, nonfinite output/reference or failed quantile-space control also stops readiness. Freeze each row's parameter bits, reference enclosures, T, error bounds, rounding diagnostics and gate outcome in the Stage 1 characterization table before matrix execution. The model-validator must review the independent CDF/bracketing and interval implementation; none has been executed here. These engineering controls do not add a per-fit support-refusal policy and cannot relax GF15.

Artifacts include every attempted fit/quantile, per-cell denominators, paired A/B/candidate error summaries, parameter/support diagnostics, immutable criteria copy, source/input/environment digests and independent verdict. Candidate runner and working command do not yet exist. Require an absent output namespace, atomic completion records, single writer and input-hash-checked resume; resume only incomplete fixed chunks under the identical candidate/environment, never overwrite predecessor evidence.

For paired A/B, A/candidate and B/candidate accuracy comparisons (A is the original estimator), use each pair's intersection of accepted keys: generating shape, count, retained draw ID, probability, and baseline/translated case (including ρ). Identify the retained keys and denominator per cell; report both-accepted, first-only-accepted, second-only-accepted and both-refused counts over all 1,000 draws. On each intersection, report both estimators' signed median, median absolute and P90 absolute errors in generating-scale units and relative units where defined; an empty intersection yields null summaries. Keep each estimator's full 144-cell individual GF15 summaries alongside these diagnostics, with its own accepted-error denominator, all-draw valid rate and unaltered GF15 gate decisions. Neither common-cohort comparisons nor changed acceptance cohorts replace any gate. The handoff must include the Monte Carlo uncertainty statement below.

### Uncertainty treatment

B’s n=18 P90 errors span .592–1.553 upper and .484–.558 lower. Improving median bias does not establish concentration. Moreover, at ρ=.5 the relative gate requires P90 scale errors ≤.25 upper/.125 lower and median bias ≤.05, stricter than baseline gates. This follows directly from e_relative=eσ/ρ; it is not a new criterion.

For scale intuition only, the **c=0 Gumbel location submodel** with known scale and shape has per-observation location information 1/σ²: its score is [1−exp(−(x−μ)/σ)]/σ. The regular efficient normal approximation gives P90 absolute location error about 1.645σ/√n. The [Gumbel density](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.gumbel_r.html) supports that submodel; it does not establish the same information at c=−.2 or .2. Carry this noncausal context table into the Stage 3 handoff (all numbers in generating-scale units):

| n | Rounded reference | Baseline P90 upper/lower .50/.25 below reference? | ρ=.5 implied .25/.125 below? | ρ=2 implied 1/.5 below? | ρ=10 implied 5/2.5 below? |
|---|---|---|---|---|---|
| 10 | .520 | yes / yes | yes / yes | no / yes | no / no |
| 18 | .388 | no / yes | yes / yes | no / no | no / no |
| 30 | .300 | no / yes | yes / yes | no / no | no / no |
| 60 | .212 | no / no | no / yes | no / no | no / no |

The reference concerns absolute-error P90 only, not median-bias or valid-rate thresholds; ρ=0 has no relative gate and .05 is diagnostic. It flags thresholds below this illustration only for c=0, never all three shapes. It is **not** a finite-sample bound for biased estimators or conditional-on-acceptance summaries, a candidate prediction or an attainability study. Every failed cell remains evidence that this complete candidate failed fixed qualification on retained draws; no failure is attributed to threshold severity or excused. Failure does not establish that the L-moment principle is generally unsuitable. Literature comparisons use different losses and matrices; no credible evidence currently establishes an all-cell pass.

Repeated use of retained draws makes candidate selection adaptive. Freeze this one candidate before execution and label any pass conditional on that development matrix. A separately approved holdout with fresh seeds and the same cells could assess replication; it is additional scope, neither authorized nor run, and cannot replace or erase fixed-matrix failures. No new uncertainty interval, screening floor or real-bundle applicability claim follows here.

Empirical medians, P90s and valid rates retain unquantified finite Monte Carlo uncertainty. There are 1,000 independent retained draws per generating shape/count cell; probabilities, translations and A/B/candidate comparisons share draws, producing correlated derived rows. Exact replay and 120,000 translation pairs do not provide 120,000 independent accuracy replications. GF15 decisions remain deterministic on the fixed matrix. Any later uncertainty intervals or replication require separate authorization and draw-level pairing across methods and derived rows; interval overlap cannot waive a threshold.

Report existing point summaries beside thresholds with signed passing margin: valid rate minus .95, limit minus |median error|, and limit minus P90 absolute error; null when undefined. These margins reveal numerical proximity, not sampling uncertainty. No bootstrap, standard-error proximity flag or new resampling is added. Fresh-seed stability, adaptive-selection optimism and numerical candidate performance remain unknown. The handoff's uncertainty statement is substantive about those limits; it does not claim a population-performance verdict from this deterministic conjunction.

### Alternatives considered

**L-moment/PWM (selected study).** Hosking et al. (1985), §5, use plotting positions (j−.35)/n in their reported PWM simulations, so their favorable results are indirect evidence for D1's unbiased order-statistic variant. Hosking’s L-moment framework explains reduced sensitivity relative to conventional moments. Neither is a universal efficiency guarantee or this benchmark’s qualification. Positive-affine equivariance is mathematical; floating-point implementation still must pass every pair. [Hosking (1985)](https://www.stat.cmu.edu/technometrics/80-89/VOL-27-03/v2703251.pdf), [Hosking (1990)](https://doi.org/10.1111/j.2517-6161.1990.tb01775.x).

**Maximum product of spacings.** Optimize sums of log CDF spacings, including endpoint spacings. Wong and Li report stable small-sample GEV behavior, including comparisons favorable to PWM. It is preferable if L-moment accuracy is inadequate and a reviewed optimizer/domain contract is acceptable. SciPy `stats.fit(method="mse")` supplies a route but needs explicit finite search bounds; leaving location/scale unspecified fixes them at 0/1. Its stochastic default requires a frozen RNG; repeated-value handling also belongs to the estimator. Those unresolved choices prevent treating MPS as a ready second candidate. [Wong and Li (2006/2007)](https://arxiv.org/abs/math/0702830), [SciPy fit](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.fit.html).

**Generalized maximum likelihood / regularized Bayesian GEV.** Martins and Stedinger use shape-prior information to control implausible small-sample estimates. Prefer this when an independently justified prior and its sensitivity assessment are acceptable. It changes statistical assumptions and cannot silently use the benchmark’s three generating shapes as prior information. Conventional moments also deserve consideration: the paper reports favorable RMSE in a restricted shape range, but RMSE does not establish GF15 P90 performance. Neither alternative is executed or fully specified here. [Martins and Stedinger (2000)](https://agupubs.onlinelibrary.wiley.com/doi/abs/10.1029/1999wr900330).

### Limitations and open questions

G1 approved this specific candidate on 2026-09-12, including its finite-mean domain, library approximation and diagnostic-only support policy. G2 remains pending for the converged design; execution requires separate authorization. Package compatibility and runtime properties remain untested. If one complete candidate fails, stop estimator shopping and return the evidence to the owner; considering another estimator or changing scientific scope requires a new reviewed method ruling. A qualification pass still leaves independent replication, real-bundle applicability, screening validation and production integration unresolved.

Partial/rejected external major fixes are preserved at their original severity in the ledger and require round 2 plus owner ratification at G2. No material scope expansion is selected. Readiness still owes executable oracle details (including independent Γ evaluation), verified wheel/source and exception origins, complete inventories/joins, measured branch characterization and all original controls. These are unexecuted engineering handoffs, not evidence of convergence or numerical adequacy. No run command is invented.

### References

Primary references linked above: Hosking, Wallis and Wood (1985), *Technometrics* 27(3), 251–261; Hosking (1990), *JRSS B* 52(1), 105–124; Martins and Stedinger (2000), *Water Resources Research* 36(3), 737–744, DOI 10.1029/1999WR900330; Wong and Li, *IMS Lecture Notes–Monograph Series* 52, 272–283, author preprint posted 2007. Software sources were inspected read-only on 2026-09-12. Predecessor scientific verdicts, criteria and provenance remain authoritative; this proposal supersedes none of them.


