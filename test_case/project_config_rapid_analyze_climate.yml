# analyze_climate (wf0) has no settings of its own; it reads the project file.
# With era5 and chirps both in `climate.sources`, it compares the two. chirps
# is precipitation only: temperature, radiation and pressure are taken from
# era5 and corrected onto the chirps grid.
{}
