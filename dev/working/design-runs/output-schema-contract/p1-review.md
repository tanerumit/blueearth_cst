# P1 focused code review

Verdict: **REVISE**. Review of the uncommitted archive foundation against the accepted §§10.2–10.4 and P1 brief. This is a static review; I did not rerun the parent's focused tests.

## Findings

1. **Major — recovery accepts contradictory candidate bytes.** `blueearth_cst/shared/workflow_config_snapshot.py:736–754`: `_recover_locked` chooses `committed` as soon as target matches the new digest and chooses `rolled_back` when target matches old and staged matches new, without checking every other candidate that exists. For example, after `new_installed`, corrupt `previous/run_record.yml`: the reader still reports a committed transaction. §10.3 says target/previous/staged candidates that contradict recorded digests must refuse and preserve evidence. Validate every present candidate against its assigned old/new digest before any terminal transition; add fault tests that mutate each leftover candidate and verify reader/recovery/new writer refusal.

2. **Major — rerun source resolution and relocation are not implemented.** `blueearth_cst/shared/workflow_config_snapshot.py:868–967` only constructs a resolver overlay; `validate_archive` at lines 588–683 checks its hashes but no P1 API applies `source_map`, `project_root_mapping`, external mappings or recorded overrides to produce a rerun execution config. The test `tests/test_source_archive.py:114–157` moves B to C and only calls `read_archive`, so it proves retained evidence but not the accepted A→B→relocated C rerun mapping with A absent. Add a resolver that maps original resolved source identities to copies in the relocated archive, resolves project-contained paths against the current root and external paths only through explicit byte-checked mappings, then test actual composition from C with A removed. Keep execution-config generation/launcher integration at the P2 boundary.

3. **Major — archive validation does not enforce the closed run-record/source-entry schema.** `blueearth_cst/shared/workflow_config_snapshot.py:588–683` checks version, digests and source bytes but accepts arbitrary extra or missing nested control keys if the record's `archive_id` is recomputed. It does not check `source_config`/`workflow_config` pointers, unique archive paths, source-entry key sets, `recoverable`, or generated-input FileRef shape/base. `run_record_document` also accepts arbitrary `rerun_adjustments` at lines 332–389. §10.2.1 makes these closed profiles and requires readers to reject unknown fields. Implement one shared structural validator before digest verification and use it for constructed and loaded records; exercise malformed keys and references without relying on accidental `KeyError`.

## Limits

The exact captured-buffer path in `compose_captured_config` and byte-preserving source payload staging appear directionally correct. The review did not assess P2 launcher wiring or scientific computations; neither belongs to P1.

## Resolution check (2026-09-21)

The P1 author revised all three major findings before commit. `_verify_candidates` now checks every present target, staged and previous archive against the transaction's assigned digests before recovery or terminal reads; fault tests mutate leftover candidates and require refusal. `resolve_rerun_execution` now composes from a relocated archive with the original source tree absent and exposes checked source, project and external path resolution. `_validate_run_record_shape` now closes the run-record, source-entry, generated/reference, FileRef and rerun overlay control fields and is called by both builder and reader. The author reports 143 focused archive/composition tests and 24 existing config-copy tests passing. Parent reran `git diff --check` and direct Ruff lint/format checks on the five touched Python files: all passed. This resolves the three original review findings for the P1 direct API; P2 launcher wiring and full workflow emission remain separate gates. The reviewer did not perform a second independent pass because the agent thread limit prevented dispatch.
