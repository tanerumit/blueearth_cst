# Predecessor WF4 executable pin (before P1 runtime edits)

Captured on 2026-09-21 in `session-2`, branch `refactor/workflow-layouts`, HEAD `002d1ae919deab1ed718d6d5e42ebae19825413e`. The predecessor code commit `a1a97ffeccea41eed3d1bf449d94f0487121f300` is present as a Git commit. `git diff --name-only a1a97ff HEAD -- blueearth_cst scripts config build_model.smk generate_scenarios.smk simulate_system.smk pixi.toml pixi.lock Manifest.toml` was empty. Thus the two commits have the same runtime and environment files at this pin. Do not advance the comparator checkout's runtime files during P1–P7.

## Executable environment and inputs

Use `pixi run` (without `-x`) from the repository root. The active pixi environment reports Windows 11 build 26200, Python 3.12.13, Snakemake 9.6.2, HydroMT 1.3.1, hydromt_wflow 1.0.2, NumPy 2.4.6, xarray 2026.4.0, rasterio 1.5.0, netCDF4 1.7.4, pandas 3.0.3. `pixi --version` is 0.70.2 and `julia --version` is 1.11.7. R and installed Julia package identities have **not** yet been independently recorded. `pixi run -x -- python` instead finds a bare Python 3.14.6 without these packages; it is not an equivalent predecessor environment. The installed package record must be extended to R, Julia and other executing dependencies before the comparator execution.

Byte SHA-256: `pixi.lock` 797afdad5a8f8c37dd62b2f1e2f88f3fea120c3fc53b7e7c792b860700a6782b; `pixi.toml` dc10ef7e15ced28bd4f046550bfdf2f99598627c9e2c390ae64ce4a9a833b0ec; `Manifest.toml` ce3d2721ffd9ae21197e12240d9e201504edfdf3d20cbfa07084ca8a1fcb9f1f.

The clean explicit-seed `123` WF3 collection is at `.tmp/scratchpad/2026-09-21_1541/prechange-wf3-reference/explicit-123-c-root/project/scenarios/collections/65651384a9fc/`; its `collection.json` SHA-256 is ec39b95bb853c77a548a7098d47217c13a07f69638097df25672397022d7a27f and ready identity is `65651384a9fc32d957fffed48c8af6d652e589db72a6f89f5386e7f4e55fd756`. The complete forcing and source hash list is in [prechange-reference-retry.md](prechange-reference-retry.md). The fixture project config SHA-256 is e1e06e1123bf9a0e376a4a15d6defe58c7c0744a268058544e6e57229db624; its WF3 file is 8a02209f16507032bd03902a111ce97f12e0086d516e2ce2f1dec4fb959eff03. Its catalog overlay maps ERA5 to `C:/data/wflow_global/hydromt/meteo/era5_daily.zarr`; `zarr.json` exists, with hash in the WF3 report. Base catalog `config/catalogs/deltares_data.yml` SHA-256 is 459d6135261ed5fbdf323c5146d3a17741b79afeeb3c3cea1a5750893055c6c2. Its root is `C:\data\wflow_global\hydromt`.

For the matching rapid WF1/WF4 settings: `test_case/project_config_rapid_build_model.yml` SHA-256 1c4fb9ee2c5c3f86d0d06dea006b2b95a863893f406ec8f662b55fa025f74a76; `test_case/project_config_rapid_simulate_system.yml` 82c612bc46e2f0d217feb27cc05824f3e215ea9f4f9a25e528868ea90bb33ad0; `config/defaults/wflow_build_model.yml` c7df597e1a22cb5fe4f35c801411d3521299f565683f56e0ed3b8172d3b3d89f; `config/defaults/wflow_update_waterbodies.yml` 6975ec4074fe283bbcfba4970cafc973964e2c1aa72e750448fa10bb881000c7; output locations CSV f5ba4391580b2d958ca69b889dda6322b046e8edba26adb24f05641021d11922; observation CSV bf217f797529efddc9468fe4e852bd2a0744e8ab4a43a586dae16626c34fef3a. These are source-file pins, not a complete model-input inventory.

The accumulated `test_case/test_local` has a model and metrics but uses the baseline config and a different collection. It is **not** a matched independent comparator. `test_case/test_rapid` does not exist. `C:/data/wflow_global/hydromt/topography/copdem/copdem.vrt` was absent in a direct probe, but that does not establish a WF1 blocker because the selected build configuration may not consume this catalog entry; determine the actually requested sources during WF1 dry run or build.

## Comparator execution route and remaining gate

Use an isolated scratch project (separate from the retained WF3 fixture and successor write targets), based on the frozen explicit-123 rapid fixture. Copy the pinned WF3 collection into that project's `scenarios/collections/` with a verified file inventory; supply the matching rapid WF1 and WF4 workflow files beside a copied project config, and change only the project directory and collection selector as required. Record all transformed fixture bytes and mapping to the frozen source. From an immutable checkout of `a1a97ff`, first run predecessor WF1 (`snakemake all -c 3 -s build_model.smk --configfile <isolated-project-config> --notemp`), then WF4 (`python scripts/simulate_system.py --config <isolated-project-config> --target all --cores 3`). On this machine, use the documented Snakemake `--shared-fs-usage persistence input-output software-deployment sources storage-local-copies software-deployment-cache` workaround if cold startup reaches the inaccessible source-cache directory. A dry run and source availability check should precede the actual build. Capture complete built-model inventory, settings, warm state, elevation/preparation, native response and metric artifacts as prescribed by reviewed design §10.9. The commands above are an execution plan; **they were not run here**.

P1 may start against this source and environment pin: it changes provenance infrastructure, whereas the predecessor WF4 comparator must be **executed and validated before P4** changes the collection/module boundary. This note does not claim WF4 numerical continuity or that the WF1 dataset set is complete. A fully isolated comparator checkout, actual R/Julia environment inventory, executable WF1 source check, model build and native/metric capture remain required before P4.
