---
title: Diagnose the staging-harness stall that hangs tests/test_stage_data_incremental.py
type: todo-item
status: backlog
effort: 1
area: test hygiene
origin: R7
queue: 1
created: 2026-08-07
updated: 2026-08-27
---

> [!note] Overview
> **What** — TWO defects under one title, separated 2026-08-18 by capturing
> both. The FAILURE is a Windows rename race in zarr's metadata write
> (`provenance:zarr`), diagnosed and fixed the same day. The STALL is a hang in
> the netCDF glob scenario, and it is now DIAGNOSED: `_run_glob` writes netCDF
> from several threads at once, and a worker blocks forever inside
> `netCDF4_.open` while the main thread waits in `as_completed`. Same family as
> the CHIRPS deadlock (`b03d965`). The fix is not written yet.
> **Why** — It took the windows CI leg down for 30 minutes and turned `main`
> red on a README rename. Containment makes the next occurrence legible; it
> does not stop it happening.
> **Effort** — The failure mode is fixed. The stall is still a wait — no recipe
> survived scrutiny — but the child now dumps its own thread stacks after 120 s,
> so the next occurrence should arrive already diagnosed.

## Progress

> [!note] **Reproduced on a GitHub windows runner, 2026-08-27.** First
> observation of this stall OUTSIDE a developer machine, and the cleanest
> reproducer yet — a runner is a fresh VM with no local state to blame.
>
> CI run `33047452666`, `tests (windows-latest)`, on branch
> `feat/r14-p1-loader` at `06f50db7`:
> `test_netcdf_glob_widening_is_incremental_and_value_identical` failed with
> `subprocess.TimeoutExpired`, `orig_timeout = 240` — the harness hit
> `HARNESS_TIMEOUT_S` rather than returning.
>
> **The ubuntu leg of the SAME run passed it.** That is a new fact and it
> narrows the search: whatever this is, it is windows-specific rather than
> machine-specific, since both legs ran the same commit from the same
> checkout with the same lock file.
>
> Not caused by the branch it was seen on: R14 P1/P1b touch no
> `stage_data` or netcdf path (`git diff --name-only main..HEAD` over
> `stage_data|netcdf` is empty), and `main`'s last CI run (`32493770463`,
> 2026-08-21) was green on this test — so the reproducer is the RUNNER,
> not the change.
>
> Worth knowing before the next hunt: the 2026-08-09 demotion was justified
> by 15 local runs with zero failures. CI gives a fresh windows VM per run
> at no cost, which is a materially better sampling instrument than
> repeating it locally.


**RE-OPENED 2026-08-09, having been demoted to a watch-item earlier the same
day.** The demotion was wrong, and the way it was wrong is the useful part:

- The hunt that justified it ran this test **15 times across 5 orderings** and
  counted **failures**. Zero.
- The failure mode is an **unbounded wait**. A hang never produces a failure, so
  the hunt could not have found it however long it ran. Right method, wrong
  target.

**The occurrence, in full** (CI run `31330093862`, `32c3a42`, windows-latest):

| | |
|---|---|
| suite reached 89 % | 18:56:55Z |
| further output | none, for 24 minutes |
| job killed | 19:21:16Z, by `timeout-minutes: 30` |
| ubuntu leg | passed normally |
| same code, 2 h earlier | windows leg green in **6m37s** (`4a5e2cc`) |

`32c3a42` differs from `4a5e2cc` only by renaming `README.rst` to `README.md`
and two lines of AGENTS.md prose, so the hang is unrelated to what was pushed.
The 88–92 % band of the collection contains this test, and it is the only test
there that spawns a subprocess.

**Contained, not fixed** (`ea2e430`): the child now has `timeout=600`, and the
harness prints a flushed `STEP <name>` per scenario. Verified: a deliberate 20 s
kill now captures 1193 chars including `STEP glob`, where the same probe
returned an empty string before — `capture_output=True` is a pipe, so unflushed
lines died with the killed child.

**A bounded failure finally happened — 2026-08-12, and the marker was thrown
away.** The containment worked exactly as designed: the harness **failed**
(`AssertionError: equivalence harness failed`) instead of hanging, in a local
`pixi run test-full` in `lane/pipeline`, at 91 % of collection — inside the
88–92 % band this note already names. It then passed standalone in 20 s and
passed in an immediate full re-run (2044 passed, 0 failed), so it remains
order-dependent and intermittent, and it is unrelated to that session's work
(a baseline re-record).

The `STEP` marker was **not** captured, because the suite had been run through
`... | tail -8` and pytest's failure detail scrolled past the retained lines.
For whoever catches the next one: **run the suite redirected to a file, never
piped through `tail`** — a bounded failure is rare enough that losing its output
costs another wait, and the marker is the whole point of `ea2e430`.

**Second bounded failure — 2026-08-18 — and the marker was thrown away AGAIN,
the same way.** Recorded in full because the repetition is now the finding.

| | |
|---|---|
| where | local `pixi run test-contract`, session-2, branch `fix/spatial-geoms-parity` (`69fde4e`) |
| result | **FAILED**, bounded — `test_netcdf_glob_widening_is_incremental_and_value_identical`. Containment held for the second time. |
| runtime | **17:37**, against **8:21** for the same tier on the same machine hours earlier (`df98514`) — about 9 minutes of stall before the bound cut it |
| immediately after | passed standalone in **8.88 s** |
| marker | **lost**, to a `... | tail -3` pipe |

**Unrelated to the branch it fired on, provably rather than plausibly.**
`test-contract` selects only `workflow_contract or process_isolation`; the
branch's new module (`tests/test_spatial_geoms_parity.py`) carries no marker and
was among the 2620 DESELECTED, so it could not have altered that tier's
composition or ordering. The branch's other file is a new `shared/` module that
nothing else imports.

**One genuinely new datum:** this fired in `test-contract` ALONE, not in a full
`test-full` run. The trigger therefore lives inside the marked tier's own
ordering, not in an interaction with the fast tier — which halves the search
space the 2026-08-12 occurrence left.

**And the process finding, which is the embarrassing one:** this note already
said, in bold, *run the suite redirected to a file, never piped through `tail`*.
The 2026-08-18 run piped it through `tail -3` anyway, so the second bounded
failure in six weeks produced no `STEP` marker either. Two of two occurrences
since `ea2e430` have now been lost to the same mistake. The warning is not
enough on its own; it has been lifted into AGENTS.md's validation ladder, where
someone actually decides how to invoke a gate.

**Third bounded failure — 2026-08-18 — and this time the marker SURVIVED.**
The suite was redirected to a file, as this note and AGENTS.md both demand, so
the failure carried its full traceback. The prime suspect below was right.

| | |
|---|---|
| where | local `pixi run test-full`, session-1, branch `refactor/wf3-member-catalog` (`c7ae97b`) |
| result | **FAILED**, bounded — the same test. Containment held for the third time. |
| markers | `STEP glob` · `STEP value_identity:zarr` · **`STEP provenance:zarr`** ← died here |
| immediately after | passed standalone **4/4**, and a clean-sheet `test-full` on the same commit was green (2815 passed) |

**The scenario, named at last:** `_rebuild_provenance(work / "pv_zarr", "zarr",
as_zarr=True)` — the **zarr** leg. Not the glob scenario, and not netcdf.

**The call site, verbatim:**

```
_stage_equiv_harness.py:368  main
_stage_equiv_harness.py:210  _rebuild_provenance   <- the SECOND _write_source
_stage_equiv_harness.py:81   _write_source         <- ds.to_zarr(path, consolidated=True, mode="w")
  xarray -> zarr.consolidate_metadata -> group._save_metadata
  -> asyncio.gather(*set_awaitables) -> asyncio.to_thread(_put)
  zarr/storage/_local.py:74  _atomic_write -> tmp_path.replace(path)
PermissionError: [WinError 5] Access is denied:
  ...\pv_zarr\src\meteo\prov_zarr.zarr\zarr.46ec38929b3344879779085c6ee13de2.partial
  -> ...\pv_zarr\src\meteo\prov_zarr.zarr\zarr.json
```

**Why that line and not another.** `_write_source` does `shutil.rmtree(path)`
and then recreates the SAME store path, and `_rebuild_provenance` is the one
scenario that calls it twice on one path — write the source, stage it, then
mutate the 2001 overlap and re-stage to prove 2001 is not re-read. Delete-then-
recreate-the-same-path is where Windows delete-pending handles and AV/indexer
scans bite: the fresh `zarr.json` is renamed over a path the OS may still hold.

**The finding that changes what the fix is: the retry is in the wrong place.**
`94e63cd` added a retry inside `stage_data`, and `_stage()` dodges via
`dask.config.set(scheduler="synchronous")`. **Neither covers this**, because the
failure is not in the product at all — it is in the harness's own fixture
writer, which has no retry, and it is not a dask write path but zarr's own
asyncio metadata write. The product's retry is, as a consequence, NOT exercised
by this test.

**Still unproven:** whether the original 24-minute CI *hang* shares this root
cause. This occurrence and the two before it are bounded FAILURES (nonzero rc
with a traceback), which the harness reports differently from a stall
(`TimeoutExpired`). A retry loop around a denied rename is a plausible bridge
between the two, and that is all it is — a hypothesis.

**Not verified:** whether the other session slot was running its own suite
concurrently. Two suites would raise handle/AV pressure on `%TEMP%`, which would
fit an intermittent rename denial, but nothing was recorded either way.

- [x] **Wait for one bounded failure and read its `STEP` marker.** Done
      2026-08-18: `STEP provenance:zarr`.
- [x] Prime suspect once localized: the zarr-v3 metadata rename race on Windows
      that `94e63cd` added a retry for. **Confirmed** — `_atomic_write`'s
      `os.replace` of `zarr.json`, denied.
- [x] Decide whether the retry in `stage_data` needs to cover the path the
      harness exercises, or whether the harness's synchronous-scheduler
      workaround is incomplete. **Answered: neither.** The unprotected write is
      `_stage_equiv_harness._write_source`, which is test code.
**THE STALL WAS CAPTURED — 2026-08-18, hours after the above — and it is NOT
the same defect.** This is the first time the hang has produced evidence.

| | |
|---|---|
| how | ran the test 3x back to back, each run redirected to its OWN file |
| result | bounded out at the harness's own 600 s guard, on the **unmodified** harness |
| marker | **`STEP glob`** — and nothing after it |
| progress | first `_stage` (narrow) completed: y_2000, y_2001, y_2002 written, 8.4 KB each. Second `_stage` (widen) printed its header — `6 files matching y_*.nc`, grid, extent, time, `-> time_range filter` — then stopped |
| stderr | `x:   0%\|          \| 0/6 [00:00<?, ?file/s]` — the progress bar rendered, then never advanced |

**So the two failure modes are different defects.** The bounded FAILURE dies in
`provenance:zarr` on a zarr metadata rename; the STALL hangs in `scenario_glob`,
in the **netCDF** path, with no zarr write anywhere near it. The caveat this
note recorded above — "a retry loop around a denied rename is a plausible
bridge, and that is all it is" — is now answered: it is not the bridge. The zarr
retry cannot fix the hang, and the hang was never zarr's.

**Where it hangs, precisely:** the start of the WIDEN stage, before a single one
of the 6 files is processed. Everything the widen stage needs to inspect at that
point was written seconds earlier by the narrow stage in the same run, which
points at a handle on a just-closed netCDF file rather than at anything the
staging logic decides. HDF5/netCDF file locking on Windows and an antivirus
holding a newly-created file are both consistent with the evidence; neither is
proven.

**A REPRODUCTION RECIPE — CLAIMED, THEN WITHDRAWN THE SAME EVENING.** It looked
like "run the test three times in a row": it stalled on the third run of two
separate sequences, which was the first time this defect had ever appeared
twice in an hour. **Eleven consecutive clean runs immediately afterwards (5,
then 6) killed it.** Both stalls happened while the machine was also running a
full suite and a `--notemp` WF3 run; the clean sequences ran on a quiet machine.

So the honest statement is a RATE, not a recipe: roughly **2 stalls in ~20
invocations on 2026-08-18**, against two occurrences in the preceding six weeks
— and the plausible difference is disk/AV pressure rather than run ordering.
The pytest-tmpdir-GC theory (pytest keeps the three most recent
`pytest-of-<user>/pytest-NNNN` trees and deletes older ones as a third run
starts) fitted the "third run" pattern and is NOT supported once that pattern
dissolved; it is left here as a discarded lead, not a suspect.

**What replaces the recipe: the child now self-diagnoses.** The harness arms
`faulthandler.dump_traceback_later(120, repeat=True)` at import (stdlib, no
dependency, `CST_HARNESS_DUMP_SECS` overrides, `0` disables). A stalled child
therefore dumps EVERY thread's stack every two minutes to stderr, which the
test already captures and prints under `PARTIAL STDERR` when the 600 s bound
fires. py-spy was the obvious alternative and was rejected: it is installed
nowhere on this machine, it would be a new dependency, and it has to attach
from outside a process that by then is unresponsive.

The limit, stated so nobody re-runs this expecting more: `faulthandler` shows
Python frames only. If the block is inside HDF5's own locking with nothing but
an `open_dataset` above it, the dump names that call and stops — which still
turns "somewhere in the widen stage" into one line.

- [x] Fix the harness's unprotected zarr write. Done 2026-08-18: `_write_source`
      now routes the zarr leg through the product's `stage_data._write_zarr`,
      which already clears the partial store and retries. Writing each
      generation to a FRESH path was rejected — the scenario's meaning depends
      on the source changing at the CONFIGURED path.
- [x] ~~Give the product's `stage_data` retry a test, since this one never
      reached it.~~ **Wrong when written, corrected the same day:**
      `tests/test_stage_data.py` already covers it (the case asserting the
      partial store is cleared before each retry, `calls["remove"] == 2`), and
      `_stage`'s own comment says so. The accurate statement is narrower: the
      retry existed and was tested, and the HARNESS bypassed it.
- [x] Arm the child to diagnose itself, since no recipe reproduces it on
      demand: `faulthandler.dump_traceback_later(120, repeat=True)`, landed
      2026-08-18. Verified not to fire on a passing run (12-16 s, well under the
      threshold) across 13 runs.
- [x] **DIAGNOSED 2026-08-18**, on the watchdog's first outing — it fired on the
      very next `test-full` (18:07, against a normal ~8 min) and the stacks came
      with it. Section below.
- [x] Fix applied 2026-08-19: `_NETCDF_WRITE_LOCK` in `stage_data.py`, held
      across `_clip_netcdf_to_file`'s write. Narrow by design — reading,
      clipping and the whole `raster_glob` path keep their workers, so the
      CMIP6 parallel staging is untouched.
- [ ] **The fix is REASONED, not demonstrated, and should be treated as
      provisional until an occurrence passes without it.** The hang would not
      reproduce on demand: 180 staged widen passes over three attempts (40
      plain, 40 mirroring the harness's synchronous-dask + narrow-then-widen
      shape, 100 across four concurrent processes) were all clean. If it
      recurs, the harness's watchdog will dump the threads and the stack will
      say whether the lock was held — which is the experiment that settles it.
- [ ] Consider whether the 600 s bound should drop to ~60 s. Nothing in this
      test legitimately takes a minute — the passing runs are 12–14 s — and ten
      minutes per occurrence is most of what an investigation costs.

## The stall, diagnosed (2026-08-18)

`faulthandler` did in one occurrence what six weeks of waiting could not. Two
stacks, both from the stalled child:

**The main thread — waiting on its own workers:**

```
_stage_equiv_harness.py:432  <module>
_stage_equiv_harness.py:415  main
_stage_equiv_harness.py:189  scenario_glob
_stage_equiv_harness.py:70   _stage
stage_data.py:1688  stage
stage_data.py:1638  _stage_dataset
stage_data.py:1601  _stage_netcdf_glob
stage_data.py:1495  _run_glob
concurrent/futures/_base.py:243  as_completed   <- blocked here
```

**A worker thread — blocked opening a netCDF file for writing:**

```
concurrent/futures/thread.py:93  _worker
stage_data.py:1180  _worker_result
stage_data.py:500   wrapper
stage_data.py:1142  subset_netcdf_file
stage_data.py:1083  _clip_netcdf_to_file      <- sub.to_netcdf(dst)
xarray/core/dataset.py:2134   to_netcdf
xarray/backends/writers.py:422  to_netcdf
xarray/backends/writers.py:78   get_writable_netcdf_store
xarray/backends/netCDF4_.py:529 open
xarray/backends/netCDF4_.py:429 __init__       <- blocked here
```

**So: `_run_glob` writes netCDF from SEVERAL THREADS AT ONCE.** It runs
`subset_netcdf_file` across a `ThreadPoolExecutor` (`workers`), and each worker
ends in `sub.to_netcdf(dst)`. HDF5 is not thread-safe, and netCDF4-python
serialises through a global lock — so concurrent opens contend, and on Windows
one of them stops coming back. The main thread then waits in `as_completed`
forever, which is why the process is silent rather than busy.

**This is the same family as the CHIRPS deadlock** fixed in `b03d965` ("write
the climate store synchronously to break a netCDF deadlock"). Same engine, same
remedy shape: take the concurrency out of the write.

**Why the harness's existing dodge could never have helped.** `_stage` pins
`dask.config.set(scheduler="synchronous")`, and that governs DASK's threads.
The pool that deadlocks here is `stage_data`'s OWN `ThreadPoolExecutor`, which
dask config does not touch. The comment in `_stage` claiming the synchronous
scheduler "sidesteps" the flakiness is therefore true of the zarr rename and
false of this.

**Blast radius, and why the fix is not test-only.** `dev/scripts/stage_data.py`
is what stages CMIP6 slices for fixtures and for real. Anyone running a
multi-file `netcdf_glob` stage with `workers > 1` on Windows is exposed to the
same hang; the test is just the reproducer that happens to run often. Fix
candidates, cheapest first: one module-level `threading.Lock` held across
`to_netcdf` (keeps the parallel clip, serialises only the write); force
`workers=1` for `netcdf_glob`; or hand xarray an explicit per-file lock. The
first preserves most of the parallelism the pool was added for.

## Refs

- `t2608071227` (watch) — the `sys.modules.setdefault` ordering hazard. Ruled
  out here: the subprocess exists precisely to escape it.
- `dev/reference/…` CI history: 11 red runs preceded this on unrelated causes
  (`t2608071205`), which is why nobody was watching when it first appeared.
- Everything the harness writes stays under the pytest `tmp_path` it is handed,
  so persisted state cannot explain "passes on re-run".

## Detail

**[R7-14] `tests/test_stage_data_incremental.py` fails intermittently** *(row
`t260802c`; still present and still flaky, confirmed 2026-08-02)* under
some orderings; passes in isolation and on re-run. Another workstream's
module, predates the R7 branch. Test-isolation issue, not a product defect.
