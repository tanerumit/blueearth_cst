# Task Brief — GF15 adapter and report, Stage 2/4

### Context
Follow AGENTS.md, the [master brief](gf15-production-integration-brief.md) and accepted D1-D7. Stage1 integrity/setup must pass. Fixed candidate C and the retained 8x policy are settled; E7 is still unmeasured.

### Goal
Implement the exact assessed estimator with truthful portable evidence, immutable identities and legacy reads.

Owner sequencing override, 2026-09-14: execute Windows implementation and parity
after Windows Stage1 readiness; carry Linux parity as deferred. Windows parity
can release the Windows comparison, but cannot establish cross-platform acceptance.

### Non-goals
No new estimator, fallback, threshold change, packaging backend, export product or actual-bundle validation claim.

### Allowed scope
Permitted: master-brief Stage2 paths, owning tests and user metric docs discovered by reference search. Approval-gated: material design changes; unavailable platform targets. Forbidden: shared environment mutation, upstream package edits, hand-edited locks, generation/simulation behavior and frozen evidence. Only extend shared identity code if its existing projection cannot represent D7, with the broader documented checks.

### Required changes (checklist)
- Implement typed adapter, precise refusal/fault routing, source guards and unchanged extraction, including structured constant refusal.
- Implement full closed declaration, exact probability coverage, tracked source-tree report resource and byte verification.
- Bind code/report/environment identity; observe live environment before fitting and final readiness; preserve historical reads independently of current dependencies.
- Add backward-read tests, portable resource checks and detached-CSV disclosure in user docs. Preserve four-column exports.
- Record independent frozen-control parity on each supported platform, exact refusal codes and a wrong-result mutant that demonstrably fails.

### Commit plan
After Stage1 snapshot, commit runnable dependency/consumer/report/schema groupings per master brief; no incomplete candidate writer.

### Validation
Map every accepted-design validation row through Live environment, plus legacy/export compatibility, to owning tests. Existing targets: `tests/test_metric_registry.py`, `tests/test_metric_plan.py`, `tests/test_content_identity.py`, `tests/test_export_wflow_results.py`; new adapter tests must use independent retained controls. Run owning tests with the isolated interpreter (`python -B -m pytest <owning paths>`). D1 extraction mutants, D2 numerical mutants, D3 unexpected faults, D4 uncovered probabilities, D5 stale A/observed B and corrupt/absent report, and D6 legacy readers must discriminate each claim. Run repository lint/format before Python commit. Do not substitute a self-comparison for E7.

### Acceptance criteria
Model-validator accepts exact fixed-tolerance parity, refusal and diagnostic behavior on every supported platform. All design tests pass; unexecuted platform work prevents acceptance.

### Output requirements
Scoped code/docs and evidence with exact commands, environment/source hashes and per-platform verdict. Preserve all failures and mutant outcomes.

### Task constraints
No hidden cap, fallback or wider benchmark claim. Executor does not self-approve scientific parity; driver dispatches the independent reviewer.
