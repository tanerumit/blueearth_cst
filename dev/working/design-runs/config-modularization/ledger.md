# Finding ledger — config-modularization, revision r1

One row per **original** finding ID from the stage-2 internal panel. 52 rows:
`risk-1..18`, `arch-1..21`, `repofit-1..13`. Same-defect duplicates keep separate
rows at their own filed severity (`arch-7` ≡ `risk-7`; `arch-4` ≈ `risk-2`;
`arch-2` ⊂ `risk-1`; `arch-10` ≈ `risk-12`; `arch-18` ≈ `risk-11`), per the
review index's instruction that no harmonization is performed.

Dispositions for `risk-1..18`, `arch-1..18` and `repofit-1..5, 8..13` were read
out of `design-v2.md` §§1–15, where the revising author embedded them in-text by
finding ID. Dispositions for the validation-plan findings (`risk-8`, `arch-19`,
`arch-20`, `arch-21`, `repofit-1`, `repofit-6`, `repofit-7`, `repofit-12`) are
resolved in §§16–20 by the completing author. Every one of the 52 IDs is cited
at least once in `design-v2.md`.

**Round** is `internal panel (stage 2)` for all 52 — there has been no external
round. **Doc version** is the version in which the disposition landed.

## risk & assumptions lens (18)

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| risk-1 | internal panel (stage 2) | blocking | accepted | §12.0 adds the complete raw-T1 consumer inventory — six consumers, each with a decision — and D-12.0 makes `compose_config` importable outside Snakemake through `load_composed_config`, so each of the four "compose" rows is a two-line change. The finding's verified-safe list (`prune_climate_store`, `prune_config_snapshots`, `region_bbox`, `scaffold_project_tree`) is carried in, which is what closes the inventory rather than leaving it open-ended. The gate half — §16 certifying a tool the design breaks — is resolved at §16.5(a): `tree-check` is changed so it *can* fail. **Conflict 1 of the review index is adjudicated in this finding's favour** (§12.0, §16.5a) on N11. | design-v2.md |
| risk-2 | internal panel (stage 2) | major | accepted | §10.5 registers T2 bytes on **both** `configuration_inputs_sha256` paths — each Snakefile's parse-time `CONFIG_REFERENCES` and `copy_config_files.__main__`'s record-time `other_config_files` / `reference_roles` — both derived from the one dict `compose_config` returns, so they cannot diverge by construction, and the design states they must stay identical. The parse-time half is the one that matters for retriggering (`build_model.smk:190-192`). N12 observes the two agreeing today, which both lenses had argued but not run. | design-v2.md |
| risk-3 | internal panel (stage 2) | major | accepted | First option of the suggested fix taken, not the second: D-9.3 now runs over **every** T2 file declared in T1 that resolves, not only `R(entry)`'s, closing the three unreachable pairs. A tolerance clause is added and is load-bearing — a file outside `R(entry)` that is missing, unparseable or not a mapping is skipped with a logged note, so one workflow's broken file cannot fail another workflow's parse. The check reads key names only, so §8.1's narrowing is preserved. §9's "Coverage, honestly" paragraph is rewritten and §19 Q2's value is raised as the finding argues. | design-v2.md |
| risk-4 | internal panel (stage 2) | major | accepted | Both halves. `REJECTED_IN_T2` gains `(⋃ HOISTED_SECTIONS.values()) − HOISTED_SECTIONS.get(name, ())`, **derived from the hoist map** rather than hand-maintained, so every hoisted name is rejected everywhere except in its declared owner's file. The unanswered second half — `reporting:` in T1 *and* in the WF3 T2 file — is dissolved by D-9.5 closing T1's top level: the collision cannot be constructed, so no precedence rule is defined. §10.4 states the caption-edit-refuses-your-experiment failure this prevents. | design-v2.md |
| risk-5 | internal panel (stage 2) | major | accepted | §7.4 rules sharing **in** for the wf0/wf1/wf2 T2 files and **out** for the WF3 T2 file, with the mechanism spelled out: `suggest_experiment_name.py` splices `experiment_name` in place, so a shared WF3 file makes naming one project's experiment silently re-point another's — after which a baseline target goes *missing* and §16.3's falsifier reports a missing target rather than a drift. Recorded honestly as an implementation-brief rule and a reviewer check, **not** as a parse-time check, because §8.4's duplicate check sees only within one T1. §18 books the reduced file count with that constraint attached. | design-v2.md |
| risk-6 | internal panel (stage 2) | major | accepted | §15.4 is new and adds both halves: **D-15.4a** refuses to write when the source contains `&`, `*` or `<<:` anywhere, or a block scalar inside a `workflows.<name>` subsection, naming the construct and its line — converting an unbounded class of silent corruption into a stated non-support with a manual path; **D-15.4b** runs the round trip `compose_config(split(x)) == yaml.safe_load(x)` against **the user's own file** before `--write` rewrites it, which the report-only default already computes. The shipped-file round trip stays as a permanent regression test. §18's residual-risk row is re-graded from "mangles a comment block, medium" to value corruption, high. | design-v2.md |
| risk-7 | internal panel (stage 2) | major | accepted | Same defect as `arch-7`, dispositioned separately per the index. §10.3 replaces the false rationale with a **per-rule truth table**: for the four `snapshot_config` rules `params.effective_config` already retriggers and the input declaration is belt-and-braces; for rule 1.06 the plain input **is** the trigger; for rule 3.09 the `ancient()` input is an **existence edge only**, and its real rerun trigger arrives from D-10.6's `stress_test_cfg` param. The F7 analogy is withdrawn for 3.09 (F7 was a params-only read promoted to a *plain* input). D-10.3 and D-10.6 therefore land in one commit, with the reason stated rather than implied. | design-v2.md |
| risk-8 | internal panel (stage 2) | major | accepted — fix exceeded, one part deliberately not followed | §16.2 is the inventory the finding demanded: **45 existing modules** (the measured union of six greps over `tests/`, matching N17's independently-derived count) **plus 2 new**, in three tiers, each with an expected-result line — including tier 3's fifteen "checked and found safe, here is why" rows, because *not on the list* and *checked and safe* are the two outcomes a sweep cannot tell apart. The finding said **replace** the spelling sweep; the sweep is **kept** and narrowed to prose and generated text (§16.6), which is strictly stronger: an inventory cannot see a stale path in a document and a grep cannot see a dict literal. The inventory's concrete return is N18 — `tests/test_experiment_allocation.py`, a breaking consumer named by no lens. | design-v2.md |
| risk-9 | internal panel (stage 2) | minor | accepted | §8.5(a) states the rebinding invariant with its consumer of record, and §10.7 records `sm.config` as the **fifth** access path v1's "four things that are not in-memory reads" missed. Checked rather than documented: `tests/test_config_composition.py` asserts the composed shape is visible through `workflow.config` (§16.1, §16.2). | design-v2.md |
| risk-10 | internal panel (stage 2) | minor | accepted | §1 and §11.1 corrected: there are **four** workflow-scoped config snapshots, three of them baseline targets (N10). The wf0 composed snapshot is declared *intended* — near-empty for three of four seeds, the honest record of a workflow with no settings — and it moves nothing in §16.3 because it is not a target. `_RUNS_README`'s replacement text must say **four**, and §16.6 carries that. | design-v2.md |
| risk-11 | internal panel (stage 2) | minor | accepted | §10.6 decides the non-Snakemake branch instead of asserting it away: `prepare_cst_parameters.py`'s `__main__` → `:279` branch runs the same `:162-165` read and would `KeyError` on first direct invocation after migration, because the path it indexes exists in no single file a user could pass. The branch is **kept and given a T1 path**, composing via `compose_config`. Retiring it was the alternative and is rejected at §17.4, because the `lookup_fn` fallback at `:250-251` exists to serve exactly that branch. | design-v2.md |
| risk-12 | internal panel (stage 2) | minor | accepted | Same concern as `arch-10`, dispositioned separately. §11.2 specifies `RECORD_ONLY_ROLES = {"workflow_config"}` selecting the record-only branch **before** `destination_dir` is constructed — v1's "only the destination is `None`" raises `TypeError` against `_snapshot_references` as written — plus the fourth-entry-shape disambiguation (`recoverable: false`, `archived_path: null`, `sha256` present) and one added `_RUNS_README` sentence explaining why this class of file is recorded but not archived. | design-v2.md |
| risk-13 | internal panel (stage 2) | minor | accepted (part a); **part b rejected** | **Part (a) accepted:** §1 and §10.4 are corrected on N9 — `reporting:` appears in **no** shipped config, only as a commented template block at `config/templates/snake_config.template.yml:210` — so §18 no longer books the retrigger benefit, since a benefit no user experiences is not evidence for a design. **Part (b) rejected:** adding `reporting:` to a shipped seed or the template so `test_cli` covers the hoist is *not adopted* (§10.4). Rationale: a shipped seed's key set is inside `effective_config_digest` and, for `snake_config_baseline.yml`, inside three baseline snapshot targets — so it would move a baseline target for a test-coverage reason and blunt §16.3's falsifier in the very milestone that depends on it. A **synthetic** fixture declaring `reporting:` (§15.4, §16.1) buys identical coverage at no cost to the falsifier. | design-v2.md |
| risk-14 | internal panel (stage 2) | minor | accepted | D-8.7 makes `config_path` optional, so the four content-free wf0 T2 files are never created — reconciliation could not have collapsed a file that is empty by construction, which was v1's offered mitigation. §7.4's cost is corrected from v1's "up to 20 files" to **4 T1 files plus up to 12 T2 files**, and §18's Negative states it that way. The four-versus-three snapshot count the finding raised is settled at N10 and adjudicated in §16.3 (four files, three targets). | design-v2.md |
| risk-15 | internal panel (stage 2) | minor | accepted (migration half); **preflight half declined** | **Accepted:** §15.5 step 3 is *inverted* — delete the `config_path` **key**, never leave it pointing at a path that does not exist — because under D-8.7 a dangling path is a hard parse error by design, and v1 advised precisely the dangling form. **Declined:** the `run_workflows.py` preflight stays out of scope (§12.1, §19 Q4). Rationale: the finding's argument was that the preflight is worth most at migration, when four brand-new path references have never resolved once — and D-8.7 removes that case, since the splitter emits a path only for a section with content, so a dangling path after migration is a typo rather than an expected first-run state. | design-v2.md |
| risk-16 | internal panel (stage 2) | major | accepted | §15.6 (D-15.5) is new and traces the cascade link by link with citations — T1's rewritten bytes re-fire rule 1.06 (`build_model.smk:535`, non-`ancient`), the wf1 snapshot digest moves, 3.01 re-fires and rewrites `.project_consistency_ok`, which 3.09 and 3.10 declare **bare**, and the realization cascade runs — then states the consequence plainly: **migrating a project with a completed experiment re-runs the entire stress test to produce identical results.** Booked in §18 Negative and as owner-visible **OV-2**, with a documented, explicitly opt-in `snakemake --touch` shortcut that no tool ever invokes, because its precondition is a judgement about what else the user changed that no tool can verify. | design-v2.md |
| risk-17 | internal panel (stage 2) | minor | accepted | §8.5(b) states the mechanism v1 named nowhere: `--config key=value` overrides are merged into T1 by Snakemake **before** the Snakefile body executes, hence before composition. Two consequences are recorded — an override writing a workflow setting into `workflows.<name>` is a parse-time **rejection** rather than an override (a deliberate behavior change, booked in §18 Negative), and an override replacing the whole `workflows` key is validated like any other T1. §15.2's migration error must therefore not blame the file when the offending key arrived via `--config`. | design-v2.md |
| risk-18 | internal panel (stage 2) | minor | accepted | §8.4's duplicate-`config_path` check compares `os.path.normcase(os.path.abspath(resolved))` rather than the raw string, so `./build_model.yml` vs `build_model.yml`, a trailing separator and a case-only Windows difference cannot evade it — and D-9.3 cannot substitute, because with one file on disk there is only one file to compare. It matches the in-repo precedent at `copy_config_files.py:317-325`, deliberately not `resolve()` (two symlinks to one file are a user's choice, not a typo). *Note: the review index's group F calls this "`risk-19`-class"; there is no `risk-19` — see the closing note.* | design-v2.md |

## architecture & consistency lens (21)

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| arch-1 | internal panel (stage 2) | major | accepted | D-8.5 states the composition contract implementably and adds `declared_sections` — **the channel the finding found missing**. `R(entry)` is then *derived* from the Snakefile's own `CONFIG_PROJECTION` literal (`{entry} ∪ {s.split(".")[1] for s in declared_sections if s.startswith("workflows.")}`), never restated inside the loader, so WF3's derivation from `guarded_sections` is preserved end to end. §8.2 specifies the accompanying **hoist** of `guarded_sections` / `CONFIG_PROJECTION` above the compose call, mechanical because both are string literals with no config dependency (N13 is the ordering evidence: `:41` reads a section, `:332` and `:362-366` build the literals). §14.2 records that name-scheme independence was an *intention* in v1 and is a *mechanism* only because of this fix. | design-v2.md |
| arch-2 | internal panel (stage 2) | major | accepted at its own severity | The same two tools as `risk-1`, graded major on their own behavior rather than on the gate. §12.0 gives each a decision and D-12.0 the shared entry point. **Conflict 2 of the review index is resolved exactly as the index directs** — no harmonization; each ID is dispositioned at its filed severity — and §12.0 states in the document why both rows stand: `risk-1` bundles a claim about the *gate*, `arch-2` a claim about the *tools*, and both are correct at their own level. | design-v2.md |
| arch-3 | internal panel (stage 2) | major | accepted | §12.3 corrects the read inventory from one to **three**. The already-set refusal (`suggest_experiment_name.py:283-294`) reads `doc["workflows"]["run_stress_test"]["experiment_name"]` from T1, which after the split is a closed two-key stanza — so `existing` is always `None`, the refusal **never fires**, and the one command whose whole contract is refusing to overwrite silently allocates a second name over a pinned one, minting an orphan `experiments/<id>/`. The read moves to the T2 document's top-level key, resolved **before** allocation so the nothing-is-reserved ordering at `:296-300` is preserved. | design-v2.md |
| arch-4 | internal panel (stage 2) | major | accepted; **severity unchanged** | §10.5 registers T2 bytes on both digest paths. The finding self-flagged its premise as argued-not-run and offered to drop to minor if the two paths did not already agree. **N12 observes them agreeing** — the same `build_model` run carries `4ec140e5…`/`1912b6b8…` in `run_record.yml` and an identical pair on a `journal.jsonl` line; `analyze_projections` likewise at `30dfea76…`/`401dab0d…` — so the condition did **not** trigger and the severity stands. Registering on one side only would make the same run emit two values of a field with one name, in the digest `_RUNS_README` points readers at for comparing runs. | design-v2.md |
| arch-5 | internal panel (stage 2) | major | accepted | D-9.1's scope is stated once and unambiguously: closure is validated for **every stanza present in T1**, whichever workflow it names — not only `R(entry)`'s. The narrow reading would have legalized half-split configs, which is precisely the defect §15.1 uses to reject the dual-mode loader, arriving through this design's own door. The complement is stated in the same breath — absence of a whole stanza is not this check's business — which is what keeps `tests/test_cli.py:110-131`'s additive-carve regression test green (§16.2 records the expectation explicitly, because an implementer reading "validate all four" without that sentence would break it). | design-v2.md |
| arch-6 | internal panel (stage 2) | major | accepted | **D-9.5** closes T1's top level to exactly `{project, shared, workflows}`. This fixes three things at once: §15.2's migration detector becomes genuinely complete (a config whose only unmigrated element is a top-level `reporting:` produced no extra key under any stanza, so nothing fired), D-9.2's rejection set becomes symmetric, and the hoist-collision precedence question is **dissolved rather than answered** — a leftover top-level `reporting:` is now a parse error naming the key and the migration doc. | design-v2.md |
| arch-7 | internal panel (stage 2) | major | accepted | ≡ `risk-7`, dispositioned separately per the index. §10.3's per-rule truth table replaces v1's claim that 1.06 and 3.09 "have no such params, which is why the input declaration is the mechanism" — true of 1.06, **false of 3.09**, which declares `config = ancient(config_path)` and has no `params:` block at all. An `ancient()` input is by construction not a rerun trigger, so `ancient(WF_CONFIG_PATHS)` buys an existence edge and nothing else; 3.09's real trigger arrives from D-10.6, which is why the two decisions ship in one commit. | design-v2.md |
| arch-8 | internal panel (stage 2) | major | accepted | **D-8.7** declines the tightening v1 introduced by accident: `config_path` is optional in every stanza, and an absent key means the section body is `{}`. A WF3 run therefore requires the WF2 **stanza**, never the WF2 **file**. N14 pins the invariant being protected — `run_stress_test.smk:384-391` records that the wf2 artifact is a params path rather than a mandatory input "because the projections overlay is optional per the CST method and must not be force-required" — and the finding is bounded precisely: the split does not create a WF2 requirement, it would have upgraded an existing *section* requirement into a *file* requirement. Owner-visible **OV-3**. | design-v2.md |
| arch-9 | internal panel (stage 2) | major | accepted | §7.4's real-project row changes from v1's `<project>/config/<name>.yml` to `<t1_dir>/<t1_stem>_<name>.yml`, beside T1 and **never under `project_dir`**. v1's location reads in this repo as `<project_dir>/config/` — the bin `copy_config_files.py:60-93` documents as written by the run, and `semantic_tree_diff.py:338-390` enumerates leaf by leaf — so a hand-authored source file there is an undeclared path and `pixi run tree-check` would fail, a false positive the design would have manufactured against the very gate §16 nominates to detect a leaked copy. | design-v2.md |
| arch-10 | internal panel (stage 2) | major | accepted | §11.2 replaces v1's "the registration reuses `other_config_files` / `reference_roles` unchanged; only the destination is `None`", which is not implementable against `_snapshot_references` (`copy_config_files.py:258-306`) because it executes `destination_dir = Path(dest_dir)` unconditionally in the copy branch. Specified instead: a `RECORD_ONLY_ROLES = {"workflow_config"}` role prefix selects a record-only branch **before** `destination_dir` is constructed. v1's "touches nothing else" claim is withdrawn, and the correction is carried into §15.7 commit 4. | design-v2.md |
| arch-11 | internal panel (stage 2) | major | accepted | Reconciled by removing the **move**, not the **rule**. `effective_config_document` (`provenance.py:254-262`) folds the whole `advanced_settings` mapping into the digested document *unprojected*, so relocating `batch_disk_headroom_fraction` would shift `effective_config_digest` for all four entry points — including the seeds §16.3's digest-equality property test compares. **D-13.4** keeps §13.1's placement policy and applies it to **new** entries only; no existing key moves in R13. §10.2's "unchanged by construction" now carries the explicit *holding `advanced_settings` fixed* qualification, §16.3 states it in the property test's own docstring, the Non-goals list records it, §15.7 commit 6 is emptied, and the relocation is tracked at §19 **Q6**. | design-v2.md |
| arch-12 | internal panel (stage 2) | major | accepted | §8.5(a) states the invariant: `compose_config`'s result is bound to the Snakefile-global name `config`, or the existing mapping is updated in place; any other binding is a defect. `check_project_consistency.py:225` is named as the **consumer of record** (it takes `live_cfg=sm.config`, i.e. `workflow.config`, not the Snakefile-local name), and `tests/test_config_composition.py` asserts the composed shape through `workflow.config` so the invariant is checked rather than documented. §18 carries the failure mode as a residual risk: a `composed = compose_config(...)` refactor makes every WF3 run fail at rule 3.01 **after** WF1 and WF2 have run, with a message that blames project drift. | design-v2.md |
| arch-13 | internal panel (stage 2) | minor | accepted | §13.3 tabulates **all three** override mechanisms — call-site default, resolver substitution on `None`, and the T2 optional key with an advanced-settings default that v1's closing paragraph introduced without specifying — each with its behavior on an explicit `null`. Mechanism 3 is identified as mechanism 1 with the default sourced from advanced settings, which is why it needs no new machinery: `get_config` returns a present key as-is including `None`, and the call site validates. Unifying 1 and 2 stays out of scope, because it would change what an explicit `null` does under a refactor claiming output neutrality. | design-v2.md |
| arch-14 | internal panel (stage 2) | minor | accepted | §12.5 resolves the self-contradiction (signature "unchanged" while the messages must gain resolved file paths — impossible for a function that receives two mappings) by changing the signature: `resolve_simulation_window(shared_cfg, model_cfg, *, shared_source=None, model_source=None)`. Both default to `None`, so the existing message shape survives verbatim when absent, which is what keeps the change additive; the Snakefile passes T1's path and the WF1 T2 path so a user with two files open is told which one to edit. | design-v2.md |
| arch-15 | internal panel (stage 2) | minor | accepted | N1 corrected from **five** to **six** declared-input sites. The list was already right and the count wrong; two lenses re-derived the same six independently by grep, and the repo-fit appendix confirmed all six file:line references. | design-v2.md |
| arch-16 | internal panel (stage 2) | minor | accepted | Because N9 shows `reporting:` in no shipped config, both the loader's hoist and the splitter's `reporting:`-move branch would ship **exercised by nothing but their own unit test**. §16.1 and §15.4 add a **synthetic fixture** declaring `reporting:` to the composition tests and the splitter round-trip gate. §15.4 also decides the case the finding implies: the template's *commented* `#reporting:` at `:210` will not match a block matcher, so it is rewritten by hand into the WF3 T2 template as part of derived-artifact regeneration, and the splitter **reports but never moves** a commented block in a user's file — a text splitter relocating comments it cannot parse would be guessing. | design-v2.md |
| arch-17 | internal panel (stage 2) | minor | accepted | §10.1's claim is weakened to what the mechanism supports. `write_experiment_config.py:154` dumps with `sort_keys=False`, so bytes follow insertion order and D-10.1 fixes `enabled` first by construction — which matches all four seeds and the template, but not a project config placing `enabled` elsewhere. **Restated:** `experiment.yml` is *value*-identical across migration and byte-identical only when `enabled` was the block's first key. The consequence is nil and the reason is recorded: `check_not_frozen` compares parsed mappings, `_frozen_differences` is key-based, and `experiment.yml` is a rule output only. | design-v2.md |
| arch-18 | internal panel (stage 2) | minor | accepted | ≈ `risk-11`, dispositioned separately. §10.6 keeps `prepare_cst_parameters.py`'s direct-invocation branch and gives it a T1 path plus composition, a three-line change that keeps `python -m …` working against the same file a user hands Snakemake — the only spelling anyone would guess. This is the second consumer that makes `compose_config`'s importability outside Snakemake a requirement rather than a convenience. | design-v2.md |
| arch-19 | internal panel (stage 2) | major | accepted | Both halves of the suggested fix. `tests/conftest.py` is added to the §12.0 raw-T1 inventory **with a stated decision** — the `model_build_config` fixture composes via `load_composed_config` rather than being handed a T2 path (N16) — and §12.6 covers the surrounding fixture surface. §16.5(b) adds the ordering row requiring the fixture layer to be exercised **from the primary checkout**, the same "run it where it can fail" posture the `check_baseline` row already takes, and it states that the skip count is read with `-rs` rather than predicted. | design-v2.md |
| arch-20 | internal panel (stage 2) | major | accepted | `tests/test_snapshot_config_rules.py` is named in three places rather than none: §8.2 as an obligation of the hoist, §15.7 commit 3 as a module that commit necessarily edits, and §16.2 with its expected result. The obligation is stated as the finding asks — the hoist carries the literals **verbatim**, including WF3's `tuple(sorted(` derivation expression, so the `:129-141` assertion survives; `:178-193` is updated for the added `config_workflows` input. §14.3 records the same literals as an independent reason to leave the workflow-name strings alone. | design-v2.md |
| arch-21 | internal panel (stage 2) | major | accepted | §16.4 gives `semantic_tree_diff` a prediction of exactly the falsifiable shape the finding asks for, on N15's mechanism (`compare_copied_config` at `:887-906` requires **deep structural equality**, not tree shape): the **three** copied-config comparisons FAIL on **removed `workflows.*` keys only**, each snapshot retaining exactly the sections its entry point's `R(entry)` composed; `project`, `shared` and the retained section compare clean; every other file in the tree compares clean; **any other diff is a defect.** It is a local gate like `check_baseline`, and it runs in §16.5(b)'s order. | design-v2.md |

## repo fit & conventions lens (13)

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| repofit-1 | internal panel (stage 2) | blocking | accepted | §12.6 is a new subsection for the test-fixture surface, with **D-12.6**'s `tests/conftest.py::write_config(tmp_path, cfg)` helper so the mutate-then-dump idiom stays one whole-config mapping and the diff at each site is small. §16.1 names the fixture split as a **prerequisite** of `test_cli` and §15.7 commit 3 lands it with the loader, closing "the design's own primary gate is unrunnable at the commit where the loader is wired in". The finding's parenthetical *"(and its T2 siblings)"* is adopted as **D-12.6b**, added while writing §16.2: the fixture **is** split on disk, because 7 of the 14 remaining consumers pass its path straight to a Snakemake run and never load it, so they have no helper call site to change. **Note for G2:** this acceptance incorporates **D-12.6b**, a decision minted by the completing author while writing §16.2 rather than ratified at a gate, booked as owner-visible **OV-5**. If the owner rules the other way, this blocking finding's resolution changes shape — §12.6, §16.2 tier 2 and §15.7 commit 3 only. | design-v2.md |
| repofit-2 | internal panel (stage 2) | blocking | accepted — option (b) of the two offered | §8.4 **reverses v1**: a `config_path` is `os.path.expanduser`-ed and, if still relative, resolved against **the directory containing T1**. N4 is corrected in the same move — no existing key references a sibling of the config file, so the consistency argument never reached this key. §15.3's splitter emits the **bare relative filename**, which is correct whether T1 is under the repo root or in an out-of-tree project folder, and the two sections now agree. Option (a) — CWD-relative with an absolute path emitted for an out-of-tree T1 — is kept on the record at §8.4 and §17.4 and is **owner-visible OV-1**; if ruled, only §15.3 and §16.1's path matrix change. The finding's required test cases (out-of-tree T1, plus cross-drive, where `os.path.relpath` raises on Windows) are in §16.1's matrix. | design-v2.md |
| repofit-3 | internal panel (stage 2) | blocking | accepted | §7.4 fixes **discovery, not the name**, because tracking *is* the glob and the two cannot be separated by naming: a T1 file is identified by the positive predicate *its top level contains a `workflows:` key*, and `tests/test_prepare_cst_parameters.py:364-378` then reads `stress_test` from the **composed** document via `compose_config` — which is also a better test than reading raw YAML, since it exercises the same path a run takes. Named in §15.7 commit 3 and given its expected result in §16.2. The finding's second half is adopted in §16.6: the sweep must anchor on the **directory**, because `test_case/snake_config_baseline_build_model.yml` (a seed T2 source) and `config/runs/snake_config_build_model.yml` (a composed project record) now differ by one path segment and a bare stem grep conflates two file classes with opposite meanings. | design-v2.md |
| repofit-4 | internal panel (stage 2) | major | accepted | §12.3 records what actually happens rather than v1's "the plan's depth handling changes": a T2 file has no `workflows:` key, so `_find(0, -1, "workflows")` returns `None`, `_plan_edit` takes the **append-whole-path-at-EOF** branch, and the verifier's `setdefault` chain builds an expectation the bogus nested write satisfies exactly — **so it passes**, and the command reports success while the composed config has no `experiment_name`. The restated obligation names every retirement: plan a top-level key at column zero; retire the two parent-block creation branches, `_block_end`, the flow-style `ValueError` and the `after_comment` anchoring; rewrite the verifier to check the top-level key; update the `:222` error string and the `:229` help text. | design-v2.md |
| repofit-5 | internal panel (stage 2) | major | accepted | §7.1's canonical block no longer points every `config_path` at `config/workflows/<name>.yml` — a bin AGENTS.md rules out **by name**, retired in R06, inconsistent with v1's own §7.4 and §15.3, and inside the toolbox checkout where project configuration must never live. The block now shows what the splitter actually produces, resolved as §8.4 specifies. `snake_utils.py:858-860`'s docstring carries the same stale spelling and is on §16.6's sweep list. | design-v2.md |
| repofit-6 | internal panel (stage 2) | major | accepted | §16.2 enumerates the shipped-config text readers and `["workflows"]` indexers with expected results, covering all four the finding named at minimum: `test_a1_acceptance.py:119-165` — **both** regexes redirected to the T2 files, since fixing only the template leaves the seed red; `test_prepare_cst_parameters.py:364-378` — composed discovery (see `repofit-3`); `test_gridded_outputs_removed.py` — two obligations, its `seed_config`/`_write` pair moves to the helper while `test_no_shipped_config_carries_a_gridded_key` **survives and strengthens** (the rglob now scans T1 *and* T2 files for the dead key, `>= 5` still holds because the count only grows, and the `roots` comment is updated); `test_snake_utils.py:2690-2745` — **verified no change**, because §12.4 keeps `config_path` bound to T1. The stated preference for redirecting at `compose_config` rather than at a second file path is followed throughout. | design-v2.md |
| repofit-7 | internal panel (stage 2) | major | accepted | §16.5(b) is the ordering row, with **all five clauses**: (1) after §15.7 commit 4 lands, re-run WF1 → WF2 → WF3 against `snake_config_baseline.yml` from the **primary checkout**, WF1 with `--notemp`; (2) then `pixi run test-full` there, redirected to a file; (3) then read and report `check_baseline.py check`; (4) then, and only then, re-record; (5) and state plainly that a **slot run of `test-full` is not evidence about the composed snapshot** and that the fixture-layer skip count must be read with `-rs`, never predicted — the documented 499/31/1 and 498/32/1 baselines shift under this change. `tests/test_interchange_contracts.py` is listed in §16.2 as **verify-only**: both sites survive D-11.1 by construction but cannot be exercised until the fixture tree is regenerated. Carried as a §18 residual risk. | design-v2.md |
| repofit-8 | internal panel (stage 2) | minor | accepted | §15 now requires **two** documents: the user-facing `docs/migration-config-tiers.md` (which naming.md marks Optional and which a clean break needs anyway) and the internal rename record `dev/milestones/r13/migration_config-tiers.md`, which `dev/reference/naming.md:205-266` marks **Required** — two of its entries fire here (checked-in example config keys; test-fixture paths read by `conftest.py` / `check_baseline.py`). The mandated `migration_<topic>.md` form overrides the repo's kebab-case rule, so the underscore is correct. Booked in §18 Neutral; v1 planned only the optional half, which would have left the R13 seal incomplete. | design-v2.md |
| repofit-9 | internal panel (stage 2) | minor | accepted | §8 relocates **all** composition machinery — `compose_config`, `load_composed_config`, the four seam checks, `SHARED_SEAM_KEYS`, `HOISTED_SECTIONS`, path resolution and the error surface — to `blueearth_cst/shared/config_composition.py`, imported directly by the four Snakefiles as they already import `interchange_contracts`. Nothing is re-exported from `snake_utils`, so there is one import path and no second spelling. Rationale carried from the finding: `snake_utils.py` is 4658 lines and is the seam AGENTS.md's write-set-declaration rule names, so concentrating a new contract surface there raises the cost of every concurrent slot for the life of the repo; `shared/` has topic-module precedent at this size (`interchange_contracts.py` 1338, `provenance.py` 686, `climate_window.py` 280). `get_config` is untouched. | design-v2.md |
| repofit-10 | internal panel (stage 2) | minor | accepted (first half); **deferred (second half)** | **Accepted:** `tests/test_batch_sizing.py:335,343` is named as the **fourth** coupled consumer D-13.2's original trio missed, and D-13.2's coupled edit set is corrected to four (schema, file, `test_advanced_settings.py`, `test_batch_sizing.py`). **Deferred:** the second half — the `effective_config_sha256` / `configuration_inputs_sha256` movement a user would see with no way to attribute it — **does not occur in R13**, because D-13.4 removes the relocation from scope. Deferral is permitted here (minor) and is **tracked at §19 Q6**, which carries the four-file coupled set, the attribution obligation, and the constraint that the follow-up lands only after the R13 baseline has been re-recorded. | design-v2.md |
| repofit-11 | internal panel (stage 2) | minor | accepted | §19 **Q1 is closed** in favour of `config_path`, on the finding's own reasoning: `dev/reference/naming.md` §3 makes `_path` mandatory for new code holding a file-path string; the proposed alternative `workflow_config` reads as a loaded mapping (`_cfg` territory), which is exactly what the key is not; and `model_build_config` is a grandfathered spelling naming.md itself flags for this class of rename (`naming.md:486`). The prose collision with the Snakefile-local variable is resolved by renaming the **variable** to `t1_path` (§8.2), not the key. | design-v2.md |
| repofit-12 | internal panel (stage 2) | blocking | accepted | §11.1 retires `test_content_is_copied_verbatim` (`tests/test_copy_config_files.py:51-67`) **deliberately and with the argument made**, not left for an implementer to infer from a red assertion: the docstring's stated rationale is wrong on the merits, because the drift guard compares *sections* by value (`check_project_consistency.py:150-184`) and `file_digest_or_absent` is content-agnostic. The replacement invariant is `yaml.safe_load(snapshot) == composed_config`, and the verbatim-copy assertions for the **catalog and template** destinations at `:64-67` are carried forward unchanged, since D-11.2 does not touch them. §11.1 also states the `copy_config_files` **signature change** the rewrite requires. Named in §15.7 commit 4 and §16.2. The finding's closing point becomes §16.5(c): retirement and replacement land in the **same commit**, because this is the only gate in the suite that can see the snapshot's shape without the `test_local` fixture. | design-v2.md |
| repofit-13 | internal panel (stage 2) | minor | accepted | §12.5 names **both** modules that pin the message shape — `tests/test_resolve_simulation_window.py` and `tests/test_add_climate_forcing.py:447-466`, the only two in the suite that reference `simulation_window` — and both appear in §15.7 commit 5 and in §16.2 with expected results (existing messages unchanged when the new keyword sources are absent; new cases when they are passed). | design-v2.md |

## Tally

| Severity | Rows | accepted | rejected | deferred | withdrawn |
|---|---|---|---|---|---|
| blocking | 5 | 5 | 0 | 0 | 0 |
| major | 27 | 27 | 0 | 0 | 0 |
| minor | 20 | 20 | 0 | 0 | 0 |
| **total** | **52** | **52** | **0** | **0** | **0** |

Blocking: `risk-1`, `repofit-1`, `repofit-2`, `repofit-3`, `repofit-12` — **all
accepted, so the run does not halt for owner arbitration.**

**No severity changes.** `arch-4` filed a conditional drop to minor if its
argued-not-run premise failed; N12 observed the premise holding, so the
condition did not trigger and the severity stands as filed.

**Three part-level declines**, each argued in the document rather than in this
ledger alone:

- `risk-13` part (b) — **rejected**: adding `reporting:` to a shipped seed or
  the template would move a baseline snapshot target for a test-coverage reason
  and blunt §16.3's falsifier in the milestone that depends on it. A synthetic
  fixture buys identical coverage at no cost (§10.4).
- `risk-15`, preflight half — **declined**: D-8.7 removes the case the argument
  rested on; recorded as §19 Q4, unchanged in substance (§12.1).
- `repofit-10`, second half — **deferred** to §19 Q6, because D-13.4 takes the
  relocation out of R13 and the consequence therefore does not occur here.

`risk-8` is recorded as accepted with the fix **exceeded**: the demanded
inventory is added (§16.2) and the sweep the finding asked to *replace* is
**kept** and narrowed (§16.6), because an inventory cannot see a stale path in a
document and a grep cannot see a dict literal.

## Conflicts from `internal-review-index.md`

| # | Conflict | Adjudicated where | Outcome |
|---|---|---|---|
| 1 | Does the `tree-check` gate survive? | §12.0 and §16.5(a), on **N11** | **Both statements true, `risk-1`'s operative.** `snapshot_project_tree.py:219` loads raw and `:69-89` reads two workflow keys with fallbacks `"experiment"` / `"cmip6"`; `pixi.toml:185` pins the task to `test_case/snake_config_baseline.yml`, whose values are `experiment_name: experiment` (`:61`) and `clim_project: cmip6` (`:40`) — the fallbacks exactly. No path moves (repo-fit right) *and* the tool's meaning changes while the pinned gate keeps passing (risk right). The gate is changed so it can fail. |
| 2 | Severity divergence on the unlisted consumers | §12.0 | **No harmonization**, as the index directs. `risk-1` stays blocking (it bundles a claim about the *gate*), `arch-2` stays major (the tools' own behavior). Both rows stand and the document says why. |
| 3 | "Exactly three baseline targets move" | §16.3, on **N10** | **Four files, three targets.** Four workflow-scoped snapshots exist — `analyze_climate.smk:329,357`, `build_model.smk:452,486`, `analyze_projections.smk:752,848`, `run_stress_test.smk:550,636` — and `dev/baseline/manifest.json` carries exactly seven targets, three of `type: yaml`; the wf0 snapshot is **not** among them. `risk-14` counted files, repo-fit counted targets; §16.3's "exactly three" prediction **stands unchanged**, and every occurrence of the number in the document now says which it is. |

## Note on two IDs that do not exist

Neither gets a row, because neither is an original finding:

- **`repofit-14`** — cited in `design-v2.md` §15.3 as a "`repofit-14`-class
  correction raised in the repo-fit appendix". The repo-fit lens filed
  `repofit-1..13`; the point in question is the appendix clearance *"Placement —
  `split_project_config.py` in `scripts/`"* (correct, but argued from the wrong
  precedent), which is a clearance, not a numbered finding.
- **`risk-19`** — cited in `internal-review-index.md` group F as a
  "`risk-19`-class path-normalization note". The risk lens filed `risk-1..18`;
  the path-normalization finding is **`risk-18`**, which has its own row above.

# External round 1 (stage 4) — dispositions, revision r2

One row per finding in `external-review-r1.md` (`ext1-1..ext1-4`, all major).
Multi-part suggested fixes are dispositioned per part inside the row. `ext1-2`
and `ext1-3` implement owner rulings recorded as gate-clarifications in
`status.md`; their disposition is `accepted` by that authority.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| ext1-2 | external r1 (stage 4) | major | accepted | Implements the owner's **staging-emit** gate-clarification ruling (2026-08-20; recorded as S7 in §5): `split_project_config.py` is strictly report-only against user files. §15.3 is reworked concretely — the script emits the proposed T1 (under the source filename) and T2 siblings (under production names) plus `migration-report.md` into a script-owned staging directory (default `<t1_dir>/config-split-staged/`, outside the E9 tracking glob), refuses to write anywhere else, and never mutates the source; application is an explicit user step (move the files, then a dry-run parse is the check), stated in §15.3 and §15.5 steps 1–2. D-15.4b becomes a round trip of the **staged pair** against the source (`compose_config(staged_t1) == yaml.safe_load(source)` — the emitted bare-relative `config_path` values compose in place inside the staging dir), with a do-not-apply verdict and nonzero exit on mismatch. §16.1/§16.2 gain the staging-contract and **source-untouched** test cases; §18's Negative bullet and §15.7 commit 2 reworded; the `--write` mode is recorded as withdrawn in §17.4. Per part: remove `--write` — accepted (ruling); emit report + proposed contents leaving application to the user — accepted (ruling, specified). | design-v3.md |
| ext1-3 | external r1 (stage 4) | major | accepted | Implements the owner's **narrow-G4** gate-clarification ruling (2026-08-21; recorded as S8 in §5), which also faults the `risk-17` resolution's framing in §8.5b (v2 called the passthrough rejection "correct and deliberate" while G4 claimed the CLI unchanged — the status log's "risk-18" pointer is the run's known off-by-one; the faulted row is `risk-17`). §2 restates G4 as covering only the `--configfile` path contract, the wrapper invocation, and `config_path` forwarding; §4 pins criterion 3's reading to it. §8.5b is reworked: workflow-setting overrides are **withdrawn** with a five-row form-by-form migration mapping (T2 edit or command-line `config_path` repoint as the sanctioned ad-hoc path; `enabled` toggles and `project`/`shared` overrides survive; new-top-level-key overrides die on D-9.5), plus a factual correction — Snakemake's `update_config` (`utils.py:563-589`) merges mapping values recursively rather than replacing the stanzas. Design-level treatment of both named surfaces: `run_workflows.py:46,1061-1069` disclosure recording stays as-is (records, never merges); `plot_workflow_dag.py:50`'s example is a withdrawn form, updated via §16.6's sweep. §18's Negative bullet reworded from "changes behavior" to the ruled withdrawal. Per part: route overrides into composed T2 — declined by the owner (recorded in §17.4); narrow G4 explicitly — accepted; actionable migration mapping for every former form — accepted. | design-v3.md |
| ext1-1 | external r1 (stage 4) | major | accepted (part 2 with modification) | §9 gains **D-9.6**. Part 1, *inventory existing cross-workflow reads* — **accepted**: the inventory is a named deliverable in §9, measured against this tree (2026-08-21) by grep sweeps over the four `.smk` files, `blueearth_cst/`, and `scripts/`, every hit read in context. Result: exactly ONE cross-workflow value read (`run_stress_test.smk:537-540` → `workflows.build_model.wflow_outvars`; `indicator_tables.py`/`export_wflow_results.py` consume the passed value, not the config), two declared identity comparisons (guard payload `run_stress_test.smk:344-345`/`:332-333`, compared by `check_project_consistency.py:41-42`), and a recorded false-positive class (`parse_spatial_config` own-section args, `resolve_simulation_window` WF1-only, WF3 own-section scripts, wrapper stanza-level reads, single-section tool reads). Part 3, *checked manifest or equivalent test* — **accepted**: `CROSS_WORKFLOW_READS` frozen shrink-only contract in `config_composition.py` + a completeness-and-minimality static scan in `tests/test_config_composition.py`, on the in-repo `LEAVES`/`test_cross_workflow_inputs.py` precedent; §19 Q2 CLOSED. Part 2, *hoist every multiply-read key incl. `wflow_outvars` to T1* — **accepted with modification**: committed as the end state but scheduled as §19 **Q7**, immediately after the R13 baseline re-record, never in the same series — the `arch-11`/D-13.4 principle (a relocation shifts every entry point's digest exactly when §16.3's falsifier must attribute shifts) plus a hazard the hoist itself adds: `wflow_outvars` leaves the guarded `workflows.build_model` section, so `_WF1_GUARDED` must gain `("shared","wflow_outvars")` or the drift guard is weakened; the full coupled-edit set is enumerated in Q7 and D-9.6's minimality check forces the contract entry's removal in the hoist commit. §16.1/§16.2/§15.7 name the new check; §18 Positive restated. | design-v3.md |
| ext1-4 | external r1 (stage 4) | major | accepted (with the reviewer's alternative, not the removal) | Faults the `risk-16` resolution's mitigation, and the fault is conceded in §15.6: bare `--touch` moves timestamps, not contents, so snapshots and configuration records could stay monolithic-shaped under products marked current. Per part: *remove the shortcut* — **declined** in favour of the alternative the finding itself offers; *define and test a bounded sequence that regenerates snapshots and provenance first, proves equivalence, and touches only downstream products* — **accepted and specified**: (1) unchanged precondition (clean D-15.4b round trip + no other edit — the unverifiable-judgement half is why no tool invokes it); (2) records-first regeneration, grounded rule-level — the 1.01-class snapshot rules read only source files (`build_model.smk:465-489`), all FOUR snapshots covered (N10), and the experiment-snapshot target pulls rule 3.01 first (`run_stress_test.smk:617-635`), so the drift guard executes rather than being touched past; (3) only then `--touch` per workflow, reaching only computational products (reaped `temp()` files stay absent); (4) scheduled-empty final `--dry-run` as the machine-checked pre/post equivalence proof, with fallback = full re-run. Coherent with the four-snapshot fact and with §16.3/§16.5(b) (the baseline project never takes the sequence; the falsifier requires recomputation). Tested by new `tests/test_migration_touch_refresh.py` (§16.1/§16.2, commit 7; counts 45+3=48). What survives of risk-16 — cascade trace, cost booking, opt-in stance — is kept with the argument why the objection does not reach it; the bare shortcut is recorded withdrawn in §17.4 and OV-2 restated. | design-v3.md |

## Driver probe (stage 6b)

One row. The framework-feasibility probe is driver-run evidence, not a review
lens; the measurements live in `status.md` (the `[event] framework-feasibility
probe 2026-08-21` entry) and as premises N19-N20 in `design-v4.md` §6.3.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| probe-1 | driver probe (stage 6b) | blocking | accepted | The probe refutes §15.6 (r2) step 4 and step 3's no-consumer claim: on a completed experiment `snakemake all --touch` succeeds (32/32 jobs, 4 s) but the immediately-following `all -n` schedules **28 of 32 jobs**, because the four `temp()`-wrapped rules' reaped outputs (`run_stress_test.smk:736, 931, 975, 1021/1029`) can never be touched into existence and `run_wflow_batch_*` consumes the downscale/perturb outputs — so the scheduled-empty equivalence proof can never pass and the sequence's fallback (full re-run) would have fired every time, silently. Resolution: the records-first refresh sequence is **withdrawn entirely** rather than repaired. N19/N20 (§6.3) show the residual reaped chain contains the dominant Wflow cost and that the re-invocation dirtiness pre-exists migration (untouched control: 20 WF1 / 36 WF3 jobs scheduled; `run_stress_test.smk:556-560` documents the reaped state in-tree), so neither the refuted absolute proof nor a differential job-set comparison (considered and rejected, §17.4) could restore the saving OV-2 booked. The full re-run becomes the only documented path; OV-2 and §18 restated with N20's honest bound; `tests/test_migration_touch_refresh.py` retired unwritten (§16.1 row removed, §16.2 counts 45+2=47, §15.7 commit 7 reworded). No step survives as mechanism — step 3's confirmed half (`--touch` neither errors on nor resurrects reaped `temp()` outputs) is retained only inside premise N19; step 1's round trip continues as the splitter's D-15.4b check, where it already lived. No new decision IDs, no new questions. | design-v4.md |

## Owner arbitration (round-cap, 2026-08-21)

Written by the DRIVER, not an author spawn — stage 6a makes arbitration entries
the one case where the driver appends to this ledger, because they record the
owner's rulings rather than design content. Two external rounds completed = the
full-variant cap, so the owner ruled on each surviving finding and those rulings
stand in for the reviewer verdict the cap forecloses.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| ext2-1 | external r2 (stage 4) | blocking | accepted — owner arbitration 2026-08-21 | **R13 SCOPE WIDENED.** The Q7 hoist becomes a required R13 implementation phase, in the reviewer's order: validate split-only output neutrality FIRST, then hoist `wflow_outvars`, move `_WF1_GUARDED` and its pinned literals, verify the *expected* digest shift, and require `CROSS_WORKFLOW_READS` to be **empty** before R13 completes. The `arch-11`/D-13.4 objection that motivated the deferral — a relocation shifts all four entry points' digests during §16.3's falsifier series — is dissolved by the neutrality-first ordering, not overruled. Independently flagged by the driver as a G2 ratification item (an unratified `deferred` on a `major` finding's third part, which the ledger rules permit only for `minor`) BEFORE round 2 ran; two independent paths converged. | design-v5.md |
| ext2-2 | external r2 (stage 4) | blocking | accepted — owner arbitration 2026-08-21 | Both fix parts. `split_project_config.py` recursively replaces only an exact tool-created directory carrying an ownership marker; otherwise the target must be absent or empty. The source directory and its ancestors are refused outright, and a caller-supplied location receives a newly created tool-named child rather than being replaced. Refusal tests for the unmarked, non-empty, source and ancestor cases. Rationale: a mistyped `--staging` argument could recursively delete user files, which is the exact harm S7's report-only posture was chosen to prevent. | design-v5.md |
| ext2-3 | external r2 (stage 4) | major | accepted (surface-coverage half); second half **dissolved** by the ext2-1 ruling | *Surface coverage* — accepted: the permanent scan must cover every authorized runtime-consumer surface, not only the four `.smk` files, since the D-9.6 inventory itself swept `blueearth_cst/` and `scripts/`; identity-comparison sites are enumerated separately from value reads. *Registry gaming* ("add a read plus a matching tuple and the equality test goes green") — **dissolved, not declined**: ext2-1 requires `CROSS_WORKFLOW_READS` to be empty at R13 completion, so any added tuple fails the check outright and the expandable-registry hazard cannot be constructed. Not put to the owner as a separate ruling because its disposition follows mechanically from ext2-1; recorded here so the derivation is auditable. | design-v5.md |
| ext2-4 | external r2 (stage 4) | major | accepted — owner arbitration 2026-08-21 | Migration COMPLETION is separated from output refresh. The staged round trip and the post-application dry-run are required immediately; existing records are then allowed to remain truthful history until the user's next intended execution, rather than §15.5 instructing "re-run" and §15.6 calling full recomputation the only path for every project. The full-recomputation claim is scoped to users who explicitly require freshly shaped snapshots. Consistent with N20: the reaped-`temp()` chain re-schedules on the next invocation anyway, migration or not, so forcing it *at migration time* converts pre-existing next-invocation dirtiness into an avoidable immediate cost. | design-v5.md |
| ext2-5 | external r2 (stage 4) | major | accepted — owner arbitration 2026-08-21 | §8.5(b)'s mapping gains an explicit row for every accepted pre-split syntax, including the dotted `--config workflows.<name>.<setting>=value` form that S8 withdrew and the dotted `project`/`shared` leaves where applicable. Each row states the parsed mapping, the exact post-split outcome (which closure check rejects it and what the user sees), and the T2-edit or `config_path`-repoint replacement. This repairs the `ext1-3` row's unsupported claim that every former form is mapped — the omitted form was the one that prompted the ruling. | design-v5.md |
| ext2-6 | external r2 (stage 4) | major | accepted with modification — owner arbitration 2026-08-21 | D-9.3 is **restricted to explicitly-declared global/hoisted identities** rather than removed. Independent same-named workflow settings (an unrelated `window` or `output_dir` in two T2 files) stop being rejected; known-shared names stay guarded at parse time. The owner declined full removal because it would place the entire shared-seam guarantee on D-9.6, whose surface coverage ext2-3 had just faulted. Actual cross-workflow *consumption* is enforced by the corrected D-9.6; D-9.2 is retained unchanged for T1-owned and hoisted names. | design-v5.md |
| risk-3 | internal panel (stage 2) | major | **partially withdrawn — owner arbitration 2026-08-21** | Severity unchanged; this is a scope withdrawal, not a re-grade. risk-3's accepted resolution had two components: (a) D-9.3 reaches **every pair** of declared T2 files rather than only `R(entry)`'s, and (b) the check reads **all top-level key names**. Component (a) SURVIVES intact. Component (b) is withdrawn by the ext2-6 ruling above, because name-spelling is not consumption and the all-spellings scope rejected legitimately independent settings. Recorded against risk-3's own ID, not only against ext2-6, so the structural check at G2 sees a consistent ledger — `withdrawn` is settable only by the issuing reviewer or by user arbitration, and this is the latter. | design-v5.md |

