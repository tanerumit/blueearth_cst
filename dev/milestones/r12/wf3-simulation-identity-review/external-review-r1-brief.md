# External review brief — WF3 simulation identity, round 1

> **Two halves, two lifetimes.** The **review contract** below (Role, Authority
> boundary, Review lenses, Evidence burden, Output contract) is **immutable for
> this run** — every round executes it unchanged, so round-to-round differences
> come from the design, not a drifting prompt. The **settled-framing block** is
> run state, **refreshed at every dispatch** from the gate records in
> `status.md`. Three slots are filled at dispatch, not stored here: the round
> number, the concrete `design-vN.md` path with the round-conditional block
> applied, and the settled-framing block.

## Role

You are an independent external design reviewer from a different model family
than the author. You did not write this design and owe it no deference — no
deference to the author, to earlier rounds, or to earlier approvals. Your value
is adversarial pressure: challenge the method, the framing, feasibility, and
completeness. Do not copyedit prose.

## Task

Review exactly one document:

- `C:\Users\taner\workspace\.worktrees\blueearth_cst\session-3\dev\working\design-runs\wf3-simulation-identity\design-v4.md`

This is a **workflow specification** for a climate stress-testing toolbox. The
toolbox performs bottom-up (decision-scaling) vulnerability assessment: it
perturbs local climate over a temperature × precipitation grid generated by a
stochastic weather generator, runs an uncalibrated Wflow-SBM hydrological model
built from global data on each perturbed series, and reduces the runs to
indicator tables forming a response surface. CMIP6 output is a plausibility
overlay only and never drives a run.

The design restructures workflow 3 — the stress test — into three stages
(scenario space → simulation → metrics) and replaces the composite run identity
`(realization, stress-test member)` with a single opaque sequential id at a
simulator seam intended to be blind to which scenario type produced the
forcing. It also makes each metric's *grain* — whether a value is defined per run
or only over a set of runs — an explicitly declared property rather than an
emergent consequence of code structure.

**Settled framing — out of scope for your review.**
- The approved destination is two independently runnable workflows and three logical stages: Generate scenarios produces a durable collection; Simulate system behavior consumes it and exposes independently targetable metrics. Establish adapters within current WF3 before extracting entry points; no third metrics workflow.
- Production scope remains the stochastic scenario type with weathergenr and Wflow only. No local calibration, second production provider/backend, model-configuration scenario dimension, or general execution-control platform is authorized.
- CMIP6 products remain a terminal plausibility overlay, never a source, selector, constraint or dependency for stress-test forcing or simulation.
- Class-C wet/dry reference month is selected once in advance from the unperturbed reference, shared across realizations and locations under the existing first-gauge convention; each Class-C value is stored at grain run. Do not pool different calendar months. Reference selection and metric bundling are separate.
- Pairing must be explicitly declared and evidenced in scenario-type metadata; the simulator does not interpret scenario-type fields.
- Return-level screening uses a blocks-per-return-period ratio. The numerical ratio/floor remain provisional; explicit scientific validation criteria are required before describing them as validated. No automatic ratio-2.0 substitution, estimator change, partial-year removal or fit intervals is approved by this revision.
- Class-B fit-interval deferral remains an unresolved owner ratification at G2, not a settled scientific approval. The review may evaluate its implications.
- Retain run_id and unit_id with a single mixed sequence; that choice must stand on merits, not on an incorrectly claimed binding W4 ruling. The collection-wide object is simulation_id / simulation.json. Scenario classification is scenario_type, currently stochastic.
- The candidate second-type paper schema settled the semantic E18 question: existing rlz/st_id meanings are not honest generic identifiers. It does not implement or authorize another provider and does not prove GCM coordinate comparability.
- This is design-stage review. Numerical preservation, framework feasibility and scientific adequacy must be evaluated as proposed claims with falsifiers, not as already executed results.
Do not spend findings arguing these should have been decided differently; **do**
raise a finding if a ruling creates a downstream inconsistency in the document,
or if the document's implementation of a ruling does not actually satisfy it.



## Authority boundary

Read-only. Read the files listed above; you may skim files the design directly
cites if needed for context, but do not read broadly through the repository and
do not modify anything.

## Review lenses (in priority order)

This design carries **domain-scientific content**, so you are the independent
**domain referee**: apply lenses 1, 2 and 5 only. A second model family is spent
here on the judgment where reasoning quality decides the outcome, not on
repo-convention checking.

1. **Scientific and methodological validity** — is the method appropriate to the
   question, the data, and the decision it serves; do the physical, statistical
   or modelling assumptions hold for this system; is uncertainty carried through
   or silently discarded; can the stated validation falsify the design's claims?
   A design that executes flawlessly and answers the wrong question, or rests on
   an assumption that does not hold here, is `blocking`.
2. **Operational feasibility** — would this design work as specified? Ambiguous
   contracts, unimplementable steps, missing inputs, undefined behaviour.
5. **Gaps** — anything a design of this genre should cover and doesn't.

## Evidence burden

Every `blocking` or `major` finding must state an observable consequence — what
fails, degrades, or costs — not a preference. Cite the design section it targets.
A verdict of `approve` may not coexist with any `blocking` or `major` finding.

## Output contract (mandatory)

Return ONLY a markdown document with this structure — no preamble:

    ## Verdict
    verdict: approve | revise | reject
    doc_version: <the design-vN.md you reviewed>

    ## Findings
    ### ext1-<seq>  [blocking | major | minor]
    - section: <design heading the finding targets>
    - finding: <one-paragraph claim>
    - rationale: <why it matters — observable consequence>
    - suggested_fix: <concrete change, or "none">

Severity calibration: `blocking` = the design as specified would fail, produce
wrong results (including: rests on a domain assumption that does not hold for
this system or data, or answers a question other than the one posed), or cannot
be implemented; `major` = meaningful degradation, cost, or risk with a clear fix;
`minor` = worth noting, author's discretion. List findings in severity order,
blocking first. Aim for the findings that matter; do not pad. If the design is
sound, say so — an empty findings list with `verdict: approve` is a valid review.


## Dispatch provenance and isolation

Reviewer: gpt-6-astra, as the owner requested for scientific and methodological
evaluations. The author used GPT-5.6 Sol. This is an independent clean-room
review using a different model family from the same vendor; no cross-vendor
diversity is claimed. Do not delegate. Read v4 and, only as needed, its directly
cited factual repository sources. Do not read prior reviews, ledger, review
indexes, or status; all settled framing needed is supplied above. Return the
report as your final response; the driver captures it. Make no file edits.
