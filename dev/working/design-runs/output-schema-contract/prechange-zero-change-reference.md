# Pre-change zero-change perturbation reference

Captured 2026-09-21 from immutable predecessor source `a1a97ffeccea41eed3d1bf449d94f0487121f300`, using an isolated scratch project under `.tmp/scratchpad/2026-09-21_1541/prechange-zero-change-reference/`. This extends the clean explicit-seed reference in `prechange-reference-retry.md`: the only scientific config change is precipitation `n_levels: 2` to `3`, which adds a `(delta temperature, delta precipitation) = (0, 0)` perturbed member. The seed remains explicit `123`. Repository source, catalogs, and standing project outputs were not edited.

The immutable checkout is `.tmp/scratchpad/2026-09-21_1541/prechange-wf4-reference/checkout/`. A Snakemake dry-run planned seven initial jobs; the subsequent `all` run exited 0. Both logs are in the scratch reference root (`dry-run.log`, `run.log`). The run used the checkout's `generate_scenarios.smk`, the absolute scratch `config/project_config.yml`, `--directory` pointing to the checkout, `--workflow-profile none`, the documented `--shared-fs-usage` set, and `--nolock`. The resulting `scenario-collection/1` is ready at `project/scenarios/collections/4ca4e0d41582/`; its full collection ID is `4ca4e0d41582429fe7dea373de2d1026a69761684b171be1d75a342fa485a763`.

The stress grid records `st_id=2` at exactly zero change for both temperature and precipitation, including precipitation variance, in all twelve months. The 14 retained runs comprise two realizations, six design points each, and two roots. `scenario_table.csv` maps root `01` to zero-change perturbed `03`, and root `08` to zero-change perturbed `10`. The predecessor's zero-change `precip` series differs from its root series, while its other six time-varying variables are exactly equal. Thus the scientific gate compares each matched predecessor and successor run, including `03` and `10`; root-to-perturbed equality is not an acceptance criterion.

The scratch `fingerprint.py` produced `fingerprint.log` with SHA-256 for both config files, every retained forcing file, every collection record, and both date CSVs, plus run mapping and decoded root/zero comparisons. Config hashes are `1883dd667691103e59c0b88cf2b308bb755794a2c8a9d2db3178f92753ae675e` (`project_config.yml`) and `08bf39b74a34cb380cb34e98b187b87c1b82d9951132ca2b70ec305c74e4d7e8` (`generate_scenarios.yml`). The collection marker hash is `50f5d1505285efc7da9b028d4a6207f4dd1e9f0d48ce858bf408fb4a507d11f9`. The fingerprint log is the detailed reproducibility record; it remains in scratch beside the fixture.
