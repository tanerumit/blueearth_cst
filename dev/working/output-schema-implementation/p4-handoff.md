# P4 handoff — additive collection contract

P4 adds `generation-seed-material/2`, `scenario-collection-intent/2` and `scenario-collection/2` computations and a checked reader. It **does not emit** a v2 collection in the default workflow. The interim `generate_scenarios.smk` still emits and reads `scenario-collection/1`; its source-time resolver and provider script now live in `legacy_generation_plan.py` and `legacy_scenario_provider.py`. Those files preserve the existing rule targets and R argument path until P5's coordinated switch.

## P5 producer handoff

- Replace the interim imports and two `script:` paths in `generate_scenarios.smk`, then retire the two `legacy_*` modules and v1 producer route. The current new-schema code seams are `generation_plan.py` (WF3 settings/classification), `scenario_provider.py` (R generation), `content_identity.py` (`generation_seed_material_v2`, `automatic_seed_v2`, `collection_id_v2`, `collection_revision_v2`), `scenario_collection_v2.py` (`read_collection_v2`), and `collection_resolution.py` (v2 discovery/explicit selection). P5 owns the static request/plan/receipt and new path publication.
- The frozen WF3 provider-code root set is `generation_plan.py`, `scenario_provider.py`, `prepare_weathergen_config.py`, `prepare_cst_parameters.py`, plus `weathergen/{generate_weather.R,impose_climate_change.R,global.R,read_member_grid.R}` and their static Python import closure. `tests/test_scenario_collection_v2.py` proves this closure excludes climate catalog preparation, WF4 downscaling, simulation records and metric grouping; it mutates those excluded files in a copied closure and checks inventory invariance. New scientific WF3 modules must enter the closure. Dynamic repository code loading is refused by the accepted contract.
- Build the v2 embedded `documents` and scientific `identity_projections` before writing `collection_intent.json`. Populate the complete scenario/perturbation lookups, retained series descriptors and date products, creator archive FileRef, then publish `collection.json` last. Re-open it with `read_collection_v2` before claiming readiness. The reader validates full-ID/short-path binding, creator owner and archive, file hashes, row order/cross-product, embedded H digests and scientific identity/revision. It is fixture-tested; P5 must exercise it against real generated netCDF, installed YAML and a retained archive.
- Execute the accepted §10.6 excluded-field matrix with fixed source bytes and explicit seed before first v2 production. The mutation tests in P4 prove code-closure and projection independence only; they do **not** establish empirical parallel/RNG, compression, plotting, output-naming or date-selection equivalence. Check exact decoded values and member/date associations under the pinned environment. Promote an excluded field or refuse the execution choice if it affects science; that is a material contract amendment before production.
- Compare all explicit-seed-123 successor series with the clean P0 reference, including zero-change perturbed members and both date CSVs. Automatic-seed numeric parity is not required. Current v1 production remains runnable but its code-inventory digest can change from the module split, so do not reuse a v1 automatic output as the successor comparator.

## P6 consumer and elevation handoff

- `simulation_runner.py` retains an interim v1 resolver import. The v1 consumer paths are `collection_resolution.resolve_project_collection`/`resolve_explicit_collection`, `downscale_climate_forcing.py:read_collection`, and `metric_plan.py:read_collection`; replace them together with v2 selection and references at P6. A new v2 marker must be refused by WF4 until that switch, not routed through the v1 forcing reader. `metric_groups.py` is WF4-owned and absent from the WF3 code inventory.
- `shared_orography.py` supplies project-owned content-addressed copy/reuse, FileRef/descriptor verification, and a HydroMT-native relative URI bound to the retained catalog directory. P6 owns actual elevation discovery, source-specific descriptors, catalog construction, preparation identity, and validation through the supported HydroMT reader. Its current ERA5 target is `data/climate/ancillary/era5/<full-sha256>/era5_orography_2018.nc`; the relative URI from `experiments/<name>/hydrology/wflow/run_settings/forcing_elevation_catalog.yml` starts `../../../../../data/climate/ancillary/era5/`. Missing WF4-only elevation must refuse WF4, not an already verified WF3 collection. Cold CHIRPS source preparation still retains its separate lapse-correction dependency.
- P6 and P7 must compare against the independent predecessor WF1/WF4/metrics capture in `dev/working/design-runs/output-schema-contract/predecessor-wf4-comparator.md`, preserving native response and metric joins. P4 made no scientific output claim.

## Verification and file ownership

- Focused regression: `.pixi/envs/default/python.exe -m pytest tests/test_generation_seed_projection.py tests/test_scenario_provider.py tests/test_collection_preparation.py tests/test_metric_registry.py tests/test_scenario_collection_v2.py tests/test_shared_orography.py -q` — **60 passed, 1 GDAL-data warning** in 38.18 s after the final reader edit. Output: `.tmp/scratchpad/2026-09-21_1541/p4-focused-tests.log`.
- Interim Snakefile CLI: `pixi run -x -- python -m pytest tests/test_cli.py -q` — **20 passed** in 39.68 s; Snakemake's Windows source-cache ACL required outer sandbox escalation. Output: `.tmp/scratchpad/2026-09-21_1541/p4-test-cli.log`.
- Interim WF3 DAG: `pixi run -x -- snakemake all -c 3 -s generate_scenarios.smk --configfile test_case/project_config_rapid.yml --dry-run --workflow-profile none --shared-fs-usage persistence input-output software-deployment sources storage-local-copies software-deployment-cache --nolock` — **exit 0**. Output: `.tmp/scratchpad/2026-09-21_1541/p4-wf3-dry-run.log`.
- Direct `.pixi/envs/default/Scripts/ruff.exe check` and `ruff.exe format --check` on the P4 Python/test paths — **passed**. `pixi run lint` itself could not locate `ruff` in this shell; the direct pinned executable was used. Python compilation of the interim and identity modules passed.
- P4-owned changed tracked files: `blueearth_cst/experiment/{collection_resolution.py,content_identity.py,generation_plan.py,metric_plan.py,scenario_provider.py,simulation_runner.py}`, `generate_scenarios.smk`, `tests/{test_collection_preparation.py,test_metric_registry.py}`. P4-owned new files: `blueearth_cst/experiment/{legacy_generation_plan.py,legacy_scenario_provider.py,metric_groups.py,scenario_collection_v2.py,shared_orography.py}`, `tests/{test_scenario_collection_v2.py,test_shared_orography.py}`, and this handoff. Stage only these exact paths; P2/P3 edit other files concurrently.

The fixture reader checks the closed descriptor envelope and retained file bytes; P5 must rederive physical descriptors from actual netCDF files before publication and refuse any mismatch. Provider installed-YAML/argument equivalence, complete excluded-field matrix, real HydroMT relative-URI behavior, and new-schema end-to-end publication remain unexecuted gates. The new reader does check the creator archive's generated-input FileRef against the installed YAML product, but P5 must populate and exercise that edge with actual generated bytes.
