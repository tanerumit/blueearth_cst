# Bounded accuracy-policy verification

Date: 2026-09-13. Scope: release decision / numerical affected, bounded by the
explicit evidence-only assignment and dev-only repository ladder. No production
caller was found by `rg -n 'gf15-accuracy-8x|check-rescore' blueearth_cst tests scripts`.
No estimator, fit, new sample, production code or old evidence was changed.

Executed from the worktree root:

```powershell
$Assessment = 'dev/milestones/r12/implementation/evidence/gf15-accuracy-8x'
$StagePython = '.tmp/scratchpad/2026-09-11_0010/gf15-lmoments-readiness/venv/Scripts/python.exe'
$Scratch = '.tmp/scratchpad/2026-09-11_0010/gf15-accuracy-8x'
& $StagePython -B "$Assessment/check-rescore.py" *> "$Assessment/verification/check-before.log"
# Above ran before rescore.py existed: expected exit 1, FileNotFoundError.
& $StagePython -B "$Assessment/check-rescore.py" *> "$Assessment/verification/boundary.log"
& $StagePython -B "$Assessment/verification/run-mutation.py" --output "$Scratch/final-mutant" *> "$Assessment/verification/mutation.log"
& .pixi/envs/default/python.exe -B -m ruff check "$Assessment/rescore.py" "$Assessment/check-rescore.py" "$Assessment/verification/run-mutation.py" *> "$Assessment/verification/lint.log"
& .pixi/envs/default/python.exe -B -m ruff format --check "$Assessment/rescore.py" "$Assessment/check-rescore.py" "$Assessment/verification/run-mutation.py" *> "$Assessment/verification/format.log"
```

All subsequent commands exit 0. The mutation runner requires its disposable
half-P90 reducer to fail with AssertionError; its log retains the actual failing
traceback. Inclusive and excessive median/P90 values, valid counts 950/949,
eligible ratios and null conventions pass on the correct reducer.

Compilation used the same StagePython `-B -` with the following stdin;
stdout/stderr went to `verification/compile.log` (exit 0):

```python
from pathlib import Path
root=Path('dev/milestones/r12/implementation/evidence/gf15-accuracy-8x')
for path in (root/'rescore.py',root/'check-rescore.py',root/'verification/run-mutation.py'):
 compile(path.read_text(encoding='utf-8'),str(path),'exec')
print('PASS: three sources compiled in memory; no bytecode')
```

One development rescore to `$Scratch/development-results` checked counts before
finalizing the handoff. Its disposable results/log are not copied into evidence.
The final command is:

```powershell
& $StagePython -B "$Assessment/rescore.py" --output "$Assessment/results" *> "$Scratch/final-rescore.log"
```

The final source freeze precedes result computation, and the signed verdict and
results inventory bind exact sources/inputs/results. The parent integration
envelope retains the final transcript and verifies final inventory bindings.
No root inventory is generated by this worker before parent integration.
No full suite or baseline run: no production caller or estimator changed.
