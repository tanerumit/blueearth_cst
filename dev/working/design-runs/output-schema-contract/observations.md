# Review process observations

- 2026-09-21: The required G1 framing pause conflicts with the owner's explicit instruction to finish and review P0 before the single master-brief acceptance gate. The intake records the owner's supplied framing as G1 authority, while a material review challenge still returns to the owner.
- 2026-09-21: The interactive coordinator and every available reviewer model are GPT. A separate headless Codex review can be independent of the author thread, but cannot establish cross-vendor review; report this limitation at acceptance.
- 2026-09-21: The Windows PowerShell tool launcher failed at process creation. `cmd.exe` reads and workspace patching work in the coordinator; worker shells have inconsistent availability. This is an execution issue, not a design finding.
- 2026-09-21: Headless `codex exec` initially failed to initialize its app-server client under the outer filesystem sandbox. Automatic review allowed the outer process access to its own state; its internal `sandbox: read-only` and `approval: never` then blocked a deliberate scratchpad write. The probe file was absent afterward. Use this verified invocation posture for the external review and inspect its banner again.
- 2026-09-21: The clean reference first stalled in Snakemake's user source cache before Snakefile parsing, then hit a temporary-directory ACL error under the tool sandbox. A locally routed source cache with automatically reviewed execution produced the expected cold-start DAG. This is environment friction; no upstream or provider code was changed.
- 2026-09-21: The v2 author caught stale in-memory document state in its persistent filesystem tool session; some section replacements were not present on disk after a later write. It reconciled the file against disk and reapplied the missing edits before downstream review.
