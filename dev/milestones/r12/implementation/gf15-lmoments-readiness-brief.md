# Task Brief — GF15 L-moment Stage 1 readiness

### Context

Canonical rules: [AGENTS.md](../../../../AGENTS.md). This is one bounded implementation unit, delivered as an artifact; it is not the three-stage qualification program.

- Governing contract: [accepted design](../gf15-alternative-estimator-design.md), D1–D6, derived from archived v4 SHA-256 `90ce735af1c15afb964194c78a24f47ecc0bc7f62d315d0fe5d22f70f6792ee9`. Read its Inputs, Method, Validation regime and Uncertainty treatment before implementation.
- [G2 approval](../gf15-alternative-estimator-review/status.md#g2-approved--2026-09-12) on 2026-09-12 releases finalization and this brief only. Read the [scoped approval](../gf15-alternative-estimator-review/scoped-review-v4.md), especially Decimal rounding, and [round-2 review](../gf15-alternative-estimator-review/external-review-r2.md) for the resolved failure mechanism. No execution is authorized by this document.
- Executor: `python-engineer`; independent scientific readiness validator: `model-validator`. No nested delegation. Coordinate the validator handoff through the driver; no modeling stage is integrated before its independent return.
- A and normalized B remain failed; production is pinned by design to `85d0b8b6b4f8b078d96f645b1049ecd477c30998`. Retained dimensionless IID GEV draws are development data; computational readiness establishes neither GF15 accuracy nor basin applicability.
- Applicable slots: `performance_analysis` binds D1's prospective adapter plus lmoments3 1.0.8 (wheel hash below), executor/validator as above; `orchestrator` binds accepted v4's isolated Stage 1 sequence, cst-architect/independent evidence reviewer. All other slots are not applicable for the reasons in accepted Scope: no system modeling, forcing generation, decision change, impact, robustness or projection outputs. Projections remain a terminal plausibility overlay; stochastic perturbations are stress-test forcing.

### Goal

After explicit Stage 1 execution authorization, implement and independently validate the isolated candidate, controls and complete predecessor mapping. Freeze an auditable readiness bundle with executable commands and immutable source, environment, input and control identities before any separately authorized matrix run.

### Non-goals

Do not execute the full 132,000-fit / 144,000-quantile matrix, refit A/B, run production workflows, integrate the candidate, record a baseline, seal R12, change screening or thresholds, alter the optimizer/estimator, redraw or add holdout samples, bootstrap, or add another candidate. Matrix accuracy, exhaustive candidate translation/branch coverage and the independent Stage 3 qualification verdict remain deferred to the accepted design's later stages.

### Allowed scope

**Permitted now:** this brief's authoring and accepted-design editorial finalization only, under the driver's assigned ownership. No code, install, fit, evidence execution or shared-environment mutation now.

**Released only by explicit owner authorization to execute Stage 1:** the following PROPOSED new paths under `dev/milestones/r12/implementation/evidence/gf15-lmoments-readiness/` (abbreviated `<READINESS>`). They do not exist yet and are not working entry points:

- Authored: `candidate_adapter.py`, `reference_oracle.py`, `check-readiness.py`, `independent-review.py`, `test_readiness.py`, `README.md` and `environment/` for the isolated dependency specification and effective-environment record.
- Generated evidence in a separately claimed, initially absent `<READINESS>/results/`: candidate/input manifests, exact original criteria copy, canonical A/B mapping and integrity audit, control tables and both precision records, fault/replay/namespace evidence, logs, atomic completion receipts, artifact inventory and independent readiness handoff. Keep authored source distinct from generated results; no bulk input copies are needed.
- The authorized stage includes verifying/downloading/installing the pinned wheel into an isolated stage environment whose explicit path and recreation command the executor records. Shared Pixi environment and repository locks remain read-only. If the pins cannot be honored in isolation, stop with the actual dependency blocker; do not repair the shared environment silently.

**Forbidden:** existing `evidence/gf15*/` records, review archive, accepted design, `blueearth_cst/`, `scripts/`, Snakefiles, `config/`, root environment/lock files, production output trees, baseline records, trackers and driver-owned master brief/validation map. Existing evidence scripts are read-only examples, not permission to import a predecessor estimator as the candidate.

Boundary inspection on 2026-09-12 used `Test-Path 'dev/milestones/r12/implementation/evidence/gf15-lmoments-readiness'` (False), `Get-ChildItem 'dev/milestones/r12/implementation/evidence' -Directory -Filter 'gf15*'`, and `rg --files dev/milestones/r12/implementation/evidence/gf15-normalized-qualification`. Re-measure before starting; an existing namespace is a conflict to inspect, never overwrite. Inventory/count assertions below are accepted-design requirements, not a claim that complete inputs have been audited today.

### Required changes (checklist)

1. **Freeze inputs and environment.** Verify the complete file selections in accepted Inputs before any candidate matrix work. Bind these SHA-256 anchors, with paths relative to `implementation/evidence/`:

   | Anchor | SHA-256 |
   |---|---|
   | `gf15/artifact-inventory.json` | `3f13145d29c377a1c92042b3845695ce293edd58e565e17a67851767026719e1` |
   | `gf15-normalized-qualification/artifact-inventory.json` | `07148bb3b165ef2d1785fb49fc57908d260009cd7885c7b39712e1148aefdf4a` |
   | `gf15/results/criteria.json` | `988611b6d239b95f7542f8d67470395e4997ac61dd97198fd075fdcba552d643` |
   | `gf15/results/provenance.json` | `0d85d2a63c6be70683e2a1485de7bfc6266a265fd383cf96b124b6facdc67db2` |
   | `gf15-normalized-qualification/results/provenance.json` | `a1ea18808890a1c270e75a35441b1bb4eecdb901551c6fafce0565be822686ae` |

   Preserve A criteria bytes; B's decoded equality is not byte equality (B criteria hash `0181b4a1044c66ab4c819fded1ae39d35608ea375ddc46bdaf2d87587714ddca`). Retain predecessor lock hash `ca888ef1c4547ee0624fc4da319de87fd9284418b2fbe16171118bc8f7b64041` as predecessor provenance, not the new environment identity. Pin Python 3.12.13, NumPy 2.4.6, SciPy 1.18.0 and `lmoments3-1.0.8-py3-none-any.whl` SHA-256 `984d1f1b0c3feefd57afc7a270931c1d28e4face2df1c2293559faf47681ef5e`; record download origin, actual wheel/source hashes, all effective versions, recreation command, adapter revision and hashes. Source-verify the pinned wheel rather than assuming stable documentation is that wheel.

2. **Implement D1/D2 without a scientific choice.** Expose only `fit_case(sample, probabilities) -> record`; truth, shape/ratio labels, draw IDs and generating parameters stay with the evaluator. Use the specified finite float64 checks, range normalization, unbiased order-statistic PWMs, `lmom_ratios(..., nmom=3)`, named `gev.lmom_fit` outputs, finite-mean c > -1 domain and specified NumPy quantile route/sign. Preserve ties, library branches/constants and the no-retry/no-fallback policy. Record normalized/back-mapped moments and parameters, support/log-density diagnostics, warnings and refusal sources. Shared baseline acceptance requires both quantiles; a failure at one revokes both rows while retaining separate diagnostics. Translations request one probability. A per-p `valid` and B fit-wide `accepted` remain unchanged. Endpoint sample exclusion is diagnostic, not a new fit refusal.

3. **Make exceptions auditable and fail closed.** The only exception refusals are exact built-in `ValueError('L-Moments Invalid')` and exact built-in `Exception('Iteration has not converged')` from verified `GenextremeGen._lmom_fit` raising statements. Match traceback frame, function, location and installed-file digest, not class module. Catch narrowly around named calls. All other errors stop the attempt and invalidate completion, including every `lmom_ratios` ValueError, wrong message/type/origin, MemoryError and adapter faults. Keep explicit domain refusals separate from exception-derived refusals. Test actual-source allowlist positives; synthetic classification tests may cover nonconvergence without claiming a naturally observed trigger.

4. **Complete D4 readiness without refitting.** Select all 12 A `samples-c{i}-n{n}.npy`, 240 A `draws-c{i}-n{n}-{start:04d}.jsonl.gz`, 240 B `fits-c{i}-n{n}-{start:04d}.jsonl.gz` and 240 B `.json` receipts, for i=0,1,2, n=10,18,30,60 and start=0,50,...,950. Check every selected inventory size/hash and receipt binding/count/hash; arrays are float64 `(1000,n)` loaded with `allow_pickle=False`. Flatten B nested quantiles exactly once and retain refused rows. Verify unique complete 144,000-key sets for each predecessor `(shape_c,n,draw,p,ratio)`, legal draw IDs 0–999, baseline null distinct from translated zero, two baseline probabilities and one per translated fit, consistent truths/offsets, and the same offset in A's baseline pair. Do not join on B `fit_id` or use B `original_*` as A. A supplied sample is the retained array row plus A.mu, never seed regeneration. Freeze mapping/schema and all-key verification, including negative fixtures for dropped/duplicate/corrupted keys and receipts.

5. **Implement independent D5 references, then controls.** Derive population moments independently for c=-.2,0,.2, location 0, scale 1; retain the exact c=0 limits. First compute the nonzero population shape discrepancies and gate outcomes, complete all three population controls, then freeze branch characterization. Original tolerances remain 1e-5 absolute shape and 1e-5 location/scale errors normalized by population scale; failure stops readiness even when library approximation explains it. The tau(.2) branch overlap is gated; do not add a tau(-.2) branch row.

   Implement the accepted fixed branch table verbatim, including both snap sides and boundaries at 0, -.8 and -.97. The validation-only Decimal shape oracle starts [-1,1] with exact tau(-1)=1, doubles only the upper endpoint through 64 (six doublings maximum), bisects at most 256 times to width <=1e-40, repeats at 80/120 digits and requires roots within 1e-35. Missing/unresolved signs, nonfinite values or exhausted budgets stop readiness, never produce candidate refusals. Derive Gamma independently with recurrence to argument >=100 and Stirling exact rational Bernoulli coefficients/first-omitted-term bound <=1e-40; freeze independently sourced pi/Euler constants and require reference parameters within 1e-30 across precision passes. No package forward-moment oracle, alternate estimator or extra dependency. Record rounded inputs, exact branch predicates (including pre-snap G), exact same-environment library fidelity, discrepancy columns and 1e-5 reference lines. New nonoverlapping branch discrepancies are characterization, not extra accuracy gates; unexplained discrepancies stop.

6. **Implement D6 quantile references and controls.** For every analytical/branch control at p=.5,.9, compare the adapter's actual float64 output to independently bisected forward-CDF enclosures under its identical recorded float64 c, location, scale and p. Convert float bits exactly to Decimal, including .9; do not substitute D5 theoretical parameters. Preserve analytical-versus-SciPy mapping/sign check at 1e-10 times max(1, abs(normalized output)). SciPy shares the inverse expression and is not the independent reference.

   Implement accepted D6's forward log-CDF, support-endpoint extensions and independent [-1,1] side expansion (64 doublings per side), 2,048 bisection budget, 80/120-digit outward intervals and uncertainty propagation. Directed context rounding alone does not direct Decimal ln/exp: bound correctly rounded results with adjacent Decimal values, compose signed interval arithmetic, and do not assume general Decimal power is correctly rounded. An unresolved sign stops the precision pass unless its bracket already meets the width target. Each enclosure width is <=1e-30*M, both must overlap, hull width <=2e-30*M, where M=max(1,abs(returned normalized float)); T=1e-10*M. Pass/fail/unresolved uses exact float distance to the unrounded enclosure under D6, with no extra probability or ulp allowance. Record display rounding separately. Independently repeat for recorded rounded physical parameters/output; divide errors and enclosure widths by positive s and use the same normalized T. Endpoint rounding requires nearest ties-to-even rounding-cell intersection with support and quantile-gate passage. Freeze parameter bits, precision/enclosure diagnostics, T, errors, rounding cells and outcomes. Unknown reference precision, unsupported/nonfinite output or exhausted budgets stops readiness.

7. **Exercise other readiness behavior and freeze the handoff.** Include invalid/constant/tied samples, single-probability failure, fixed translated controls, exact replay/refusal classification, source fidelity, no RNG and truth-blind interface tests. Use finite controls only; exhaustive 120,000 candidate translation pairs and 132,000-attempt branch histogram belong to later qualification. Establish absent-namespace claim, single writer, atomic receipts and hash-checked resume using disposable fixed readiness chunks, not a matrix run: completed matching chunks are reused; only incomplete fixed chunks under identical bindings may resume; conflicting hashes, competing writers and inconsistent partial receipts are refused. Preserve predecessor bytes throughout. Freeze source/environment manifests before controls and immutable control outputs plus signed independent readiness verdict before matrix execution. Every attempt records its identity, inputs/config/command/environment/source and expected outputs. No claim of managed-root run-control conformance follows from this local evidence work; if its destination is a managed project root, the driver must validate intent and win the namespace claim under cst-run-control before `ready`.

### Validation

Current authoring: rapid, numerical behavior unaffected; structural/link/hash/diff checks only. Future Stage 1: numerical claims affected, isolated dev/evidence scope. Load `python-discipline`, `testing-policy` and environment guidance when execution is authorized. No candidate command exists now. The executor must replace every placeholder below with an implemented, scoped command and record it in `<READINESS>/README.md` before readiness signoff; placeholders are not runnable assertions.

| Contract/property | Falsifier and evidence-producing command to materialize |
|---|---|
| D1 truth blindness, estimator fidelity and no stochastic/fallback path | `<D1_ADAPTER_CONTROL_COMMAND>` fails if the adapter receives truth labels, changes output for identical sample/probabilities with evaluator labels changed, consumes RNG (instrument calls and states), invokes fallback/retry, differs from pinned source on identical inputs or mishandles invalid/tied cases. Retain instrumentation and source comparisons. |
| D1/D2 fault/refusal boundary and baseline conjunction | `<D2_FAULT_CONTROL_COMMAND>` injects actual-source positives and wrong-origin/type/message/conversion/memory/adapter faults; any unknown fault producing a statistical refusal or completion receipt falsifies fault-stop. One bad probability failing to revoke both baseline rows falsifies conjunction; changing predecessor statuses falsifies preservation. |
| D3 frozen criteria and no qualification claim | `<D3_SCHEMA_CONTROL_COMMAND>` verifies exact A criteria hash and synthetic summary semantics: all-draw valid denominators, conditional accepted errors, linear quantiles, undefined zero-ratio relative error, .05 diagnostic, unchanged thresholds, empty-cohort nulls and four acceptance categories. Any drift fails. Synthetic rows test schema/reducers only; no matrix performance verdict is permitted. |
| D4 full input and join integrity | `<D4_INVENTORY_JOIN_COMMAND>` audits all selected files and both complete key populations; missing/duplicate keys, null/zero conflation, receipt mismatch, altered truth/offset or use of B copies in place of A fails. Corrupted-copy fixtures must trigger stop before candidate work. |
| D5 independent population/branch reference and precedence | `<D5_REFERENCE_CONTROL_COMMAND>` emits both precision passes, Gamma/constants/budget evidence and library comparisons. Population error above original limits, oracle budget/sign failure, changed branch selection, reference dependence on library forward moments or characterization overriding a failed gate falsifies readiness. |
| D6 independent same-float quantile enclosure | `<D6_CDF_CONTROL_COMMAND>` records exact tuples and normalized/physical interval evidence. Wrong tuple/p, inverse-expression reuse, nonoverlap, width over budget, endpoint mistake, failed quantile bound or unresolved reference falsifies readiness. Mutate sign, tuple, rounding/enclosure or endpoint behavior in disposable controls to demonstrate detection. |
| D6 replay, immutable namespace and absence properties | `<D6_REPLAY_NAMESPACE_COMMAND>` reruns fixed controls and injects competing writer, changed source/environment/input, interrupted chunk and inconsistent receipt cases. Changed numerical/refusal records, overwrite of completed evidence, reuse under mismatched binding or valid completion after a fault falsifies the contract. Hash predecessor/protected paths before/after; any write there fails. A replay is required to report reused versus recomputed chunks; do not claim a no-network property without observing it. |

The full D3 gates remain pinned: 144 cells, valid rate >=.95 over 1,000 draws, abs median generating-scale error <=.10, P90 absolute <=.50 upper/.25 lower; corresponding relative gates only at ratios .5,2,10; unchanged acceptance and accepted-pair translation error <=1e-6, with both-refused never numerical passes. The Stage 1 schema checks preserve these gates; they do not evaluate candidate full-matrix passage. Later stages retain pairwise accepted-key intersections alongside each estimator's full individual gates, fixed-matrix margins and unquantified Monte Carlo/adaptive-selection limitations.

| Rung | Frequency and command/scope |
|---|---|
| Narrow feedback | Per relevant edit: `<STAGE_PYTHON> -m pytest <READINESS>/test_readiness.py <FOCUSED_SELECTION>` after creating the proposed file and interpreter binding. Start discriminating fault/reference tests before implementation and record the expected red result; focus tests on the D1–D6 falsifiers above. |
| Numerical/behavior controls | Per changed numerical or persistence invariant: the corresponding materialized D1–D6 command above; one complete fixed-control pass after final source/environment freeze. Re-run only affected controls during iteration; any changed final binding requires a new immutable attempt. |
| Integration/status | Once before independent readiness handoff: complete D4 inventory/key audit, all fixed controls and namespace/replay experiment; then `git diff --check`, protected-path hash comparison and `git status --short`. Re-run the audit when its code or input binding changes. |
| Documented Python checks | Once before handing off the completed Python diff: `pixi run lint` and `pixi run format-check`. Confirm their coverage of the proposed evidence scripts; if excluded, record scoped formatter/linter commands using existing tools. Keep logs in the assigned scratch directory, never pipe gates through tail. |
| Full suite/baseline | Not applicable to this isolated dev/evidence-only unit: no `test-full`, baseline record or workflow run. If an authorized scope revision actually touches shared code, script signatures or public surfaces, apply AGENTS.md's corresponding CLI/fast/full ladder then; this brief does not authorize that expansion. |
| Independent scientific acceptance | Once on the frozen final readiness bundle: `<INDEPENDENT_READINESS_REVIEW_COMMAND>` plus model-validator's signed handoff reviewing equations, source/exception origins, interval/oracle independence, all D4 keys, controls and limitations. A source/environment/control change invalidates that binding's acceptance and requires review of the changed evidence. |

Report every rung's executed command, scope, frequency, result, failures caught and skipped checks. A passing implementation test suite is insufficient for independent scientific readiness.

### Acceptance criteria

- Explicit Stage 1 execution authority is recorded before implementation/install/control execution begins; it does not release Stage 2.
- All pins, complete inventories and both canonical key sets verify; original criteria/predecessor bytes remain unchanged. Effective environment and actual commands are recorded without unresolved placeholders.
- D1/D2 adapter fidelity and fault-stop, D4 joins, all original D5 population gates, fixed branch characterization and every D6 control satisfy the accepted contract. No unknown fault or reference failure is reclassified as statistical rejection, approximation explanation or tolerance waiver.
- Independent model-validator returns a signed readiness acceptance for the frozen source/environment/input/control identities. Missing, failed or unresolved evidence means Stage 1 is not integrated; retain failed-attempt evidence and stop at the specific blocker without running the matrix.
- The handoff says readiness only. It does not qualify the candidate, reverse A/B failures, establish finite-sample accuracy, change screening, or authorize production/sealing.

### Output requirements

Deliver the isolated authored adapter/oracle/harness/tests, reproducible environment instructions, an exact command transcript, immutable manifests/inventory, complete predecessor audit, fixed-control tables with precision/rounding details, negative-test and replay evidence, and independent readiness handoff. Report hashes and exact scope; record timing as readiness timing only. Provide a concise Stage 2 handoff listing its still-unexecuted full matrix and later independent qualification requirements from accepted D3–D6.

Results delta: report what controls changed and why, including failures, independently of the unchanged production and predecessor results. If blocked, preserve recoverable evidence, name the failing contract and the decision/environment repair needed; do not invent a numerical success or silently repair the method.

### Task constraints

Human gate 1: explicit owner Stage 1 execution authorization is outstanding. G2 approved design/brief preparation only, so pause at that existing authority boundary before implementing, installing or fitting controls. Once released, complete this bounded stage without adding permission gates for routine authorized work. Any method/tolerance/dependency change outside the fixed contract returns to the owner with concrete evidence; no silent substitution. Stage 2 matrix execution remains separately unauthorized even after readiness acceptance.

Work only in the assigned proposed paths; other agents own trackers and integration documents. Do not revert their work or mutate archived records. Preserve no-production-change and no-new-data boundaries. No nested delegation or automatic expansion to the full program.
