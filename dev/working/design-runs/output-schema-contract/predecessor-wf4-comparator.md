# Independent predecessor WF1/WF4 comparator

Captured 2026-09-21, before P4 collection/module-boundary changes. **Execution passed; numerical continuity against the successor remains untested.** This is an isolated predecessor reference for the P4/P6/P7 comparison and model-validator handoff, not a claim of parity with future outputs.

## Frozen execution and inputs

- Predecessor source: `a1a97ffeccea41eed3d1bf449d94f0487121f300`, exported with `git archive` into `.tmp/scratchpad/2026-09-21_1541/prechange-wf4-reference/checkout/`. Archive SHA-256: `b4457f21fb3ba69d92c636a11f971dad5a2afe83422b75892f1d5b396a347252`. The checkout is separate from the active branch's P1 edits. No predecessor runtime file was changed; the added `run_snakemake.py` wrapper in scratch only routes Python's temporary directory for the successful WF1 dry-run.
- Runtime environment: the active session's pinned `.pixi/envs/default` from the same `pixi.toml` and `pixi.lock` pinned in [predecessor-wf4-pin.md](predecessor-wf4-pin.md). `pixi run` from the active repository root activated Python 3.12.13/Snakemake 9.6.2, R 4.4.3 with `weathergenr` 2.0.1, `ncdf4` 1.24 and `yaml` 2.3.12; Julia 1.11.7 with Wflow 1.0.2. Logs: scratch `r-environment.log` and `julia-environment.log`. Directly launching an unactivated R executable hung during startup, so it was not used. No `pixi.lock` or `Manifest.toml` was edited.
- Project root: `.tmp/scratchpad/2026-09-21_1541/prechange-wf4-reference/project/`. It is separate from successor write targets and from accumulated `test_local`/`test_rapid` outputs. The source explicit-seed-123 WF3 collection `65651384a9fc` was copied from the clean [WF3 reference](prechange-reference-retry.md); all 22 copied files were SHA-256 verified before execution and again afterward. The collection manifest SHA-256 remains `ec39b95bb853c77a548a7098d47217c13a07f69638097df25672397022d7a27f`; no copied collection byte changed.
- Fixture construction: `make_fixture.py` and `fixture-inventory.json` in scratch (inventory SHA-256 `32da1ea6974640006f450b77464ea99897baf5bea3520483cdf0cd35b7fd6ca1`). The copied project config differs from the source WF3 fixture only in isolated `project_dir`, absolute C-root ERA5 overlay path, the unused WF3 stanza path, and WF4 stanza path. The copied rapid WF4 file adds `scenario_collection.manifest_path` selecting the frozen collection. Source/transformed project config SHA-256: `e1e06e1123bf9a0e376a4a6a15d6defe58c7c0744a268058544e6e57229db624` / `41aa6fb511525c624c07576f72f1ec939b55070013931f93e7eed56cd28659a1`; WF4 config: `82c612bc46e2f0d217feb27cc05824f3e215ea9f4f9a25e528868ea90bb33ad0` / `ee3e347e9802f8c3d098b29bfbb2459af22d1c0a6620ec36944886180a7a8857`. The WF1 rapid config and default HydroMT build files retain the source hashes in the executable pin.
- The selected spatial catalog names `merit_hydro_ihu`, `merit_hydro_index`, `rivers_lin2019_v1`, `vito`, `modis_lai`, `soilgrids` all resolved against `C:/data/wflow_global/hydromt`; `source-probe.json` records patterns and match counts (14, 1, 1, 1, 12, 45). WF1 actually read these and the C-root ERA5 daily Zarr store. The large external datasets were not recursively hashed; the catalogs, Zarr root metadata and clean WF3 source pins are recorded in the earlier reports. Full independent dataset-byte proof remains bounded by that limitation.

## Commands and outcomes

From the active repository root, with `<checkout>` and `<config>` the absolute scratch paths above:

```text
git archive --format=zip --output=.tmp/scratchpad/2026-09-21_1541/prechange-wf4-reference/predecessor-a1a97ff.zip a1a97ffeccea41eed3d1bf449d94f0487121f300
# From <checkout>, using the active repository's pinned Python executable:
<active-repo>/.pixi/envs/default/python.exe run_snakemake.py all -c 3 -s build_model.smk --configfile test_case/project_config_predecessor_wf4.yml --notemp --dry-run --workflow-profile none --shared-fs-usage persistence input-output software-deployment sources storage-local-copies software-deployment-cache --nolock
# From the active repository root:
pixi run -- snakemake all -c 3 --directory <checkout> -s <checkout>/build_model.smk --configfile <config> --notemp --workflow-profile none --shared-fs-usage persistence input-output software-deployment sources storage-local-copies software-deployment-cache --nolock
pixi run -- python <checkout>/scripts/simulate_system.py --config <config> --target all --cores 3 --dry-run
pixi run -- python <checkout>/scripts/simulate_system.py --config <config> --target all --cores 3
```

WF1 was dry-run first using the same `--notemp` and Snakemake shared-FS arguments; it passed with 20 jobs. The actual WF1 run passed exit 0, 20/20 jobs, 8m21s. The historical Wflow run covered 2010–2016 and retained `run_default/output.csv`, AET/GWR tables, and `outstate/outstates.nc`. WF4 dry-run passed; its checkpoint initially exposed six jobs. The actual WF4 run passed exit 0, 21/21 jobs after DAG expansion, 8m01s. It downscaled and simulated all ten collection members, inventoried 130 native response series, and published one ready metric set. Full redirected logs are scratch `wf1-dry-run-escalated.log`, `wf1-run.log`, `wf4-dry-run.log`, and `wf4-run.log`. The first WF1 dry-run attempts were blocked before DAG parsing by Windows sandbox denial creating Snakemake's temporary source cache; the successful runs used the same outer escalation as the WF3 reference. A WF4 passthrough-option attempt was refused by the runner's allowlist before the corrected dry-run; no simulation ran in that attempt.

## Frozen artifacts and joins

- Post-WF1 complete project inventory: scratch `wf1-project-inventory.json`, SHA-256 `6f9581533999c773c3955ab677dbd308b0229ee9fcb86c30c88570a84a7b5774`. It includes all 59 `models/hydrology/wflow/` files: model maps/TOML, forcing, historical native tables, warm state, evaluation and build inputs. After WF4, all 59 had identical SHA-256 and none was absent. Model-reference digest: `d9109f81027f28ae4fe22787e5cd832718ea3f9bdac4188269e88d666b1ff952` (the model-reference record's defined six-input projection; use the full inventory for complete model-byte comparison).
- Post-WF4 complete project inventory: scratch `wf4-project-inventory.json`, SHA-256 `b7807f69184757b016bcdb37b88c2af57377fb4a7d1831c768796144c6b433ba` (234 files, 136,527,824 bytes). It records each relative path, byte count, SHA-256, and CSV header/data-row count. `wf4-inventory-summary.log` is a compact human-readable listing. The experiment has 10 `run_XX.toml`, 10 `run_XX.temporal.json`, 10 Wflow-ready forcing netCDFs, 10 retained native `run_XX.csv` (3,286 data rows each), and source/adapter/settings/environment/response records. These inventories are scratch evidence, not committed run outputs.
- `experiments/experiment_rapid/config/simulation.json` records simulation ID `526b6d78b721fe710b8c3776240a286a88e4688426fb62af321fb5c73d41abbb`, collection ID `65651384a9fc32d957fffed48c8af6d652e589db72a6f89f5386e7f4e55fd756`, resolution mode `explicit-manifest`, response inventory ID `7e8ec93f6556b441ed1f2c5765b28b3d4ec3ae945860c4d8907d052a45d13fb3`, and model digest above. `response_request.json` requests `q` (m³/s; five locations), `aet` and `gwr` (mm/day; four locations each), daily interval-end timestamps 2046-01-02 through 2054-12-31, standard calendar. Its SHA-256 is `27560425f274f28079adb63471eff5e3bad658df91ff6606e55e4f3c06d2bb62`.
- Ready metric set `0f8a8d7b1b84` is under `experiments/experiment_rapid/results/metric_sets/`. Its `metrics.json` SHA-256 is `6aa239955258d9a6b6891e107df1bf5489277a616940b20ffa9508ae9636d8a4`; indicator tables: `q` 500 rows, SHA-256 `246c2532b51ebf6426619137a1fcc08fb785c089b9a1c177b692128012a79019`, `aet` 40 rows, `f3fdade0399bf6a6a246b0563b3830c14cbf6a1334f7c62211c6075e8f5d069f`, `gwr` 40 rows, `daa034677bd3be054e1f2f5cde28f3fb0ca44b68013b8e9ede4d04ffefe2f44f`. The return-level benchmark and `unit_index.csv` are also inventoried.
- Scenario rows map run IDs `01`–`05` to realization 1 and `06`–`10` to realization 2. `01` and `06` are the root members with empty `st_id`; `02`–`05` derive from `01` with `st_id` 1–4, and `07`–`10` derive from `06` likewise. The metric unit index contains run units `01`–`10` and five bundles: `11=(01,06)`, `12=(02,07)`, `13=(03,08)`, `14=(04,09)`, `15=(05,10)`. Preserve these associations when mapping to successor IDs; do not join only on renamed paths.

## Validation handoff

`verify_capture.py` passed: 22 frozen collection files unchanged, 59 WF1 model files unchanged after WF4, 10 retained native response CSVs and 10 per-run TOMLs present. Both full runs and both DAG dry-runs passed. Model-validator must compare the successor under the matched explicit seed using the §10.9 row/member, temporal, native response and metric criteria. The comparison should read the frozen scratch artifacts and inventories directly, not regenerate the predecessor or use `test_local`. In particular, align every native variable/location/time and all 580 metric result keys, report value counts, masks and any mismatch locations/max absolute differences; require zero tolerance unless the owner approves a specific scientific difference. The predecessor reference alone cannot establish numerical continuity.
