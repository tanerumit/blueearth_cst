# LOG

Closure ledger. One row per board item closed since the board was adopted
(2026-08-07). Work closed BEFORE that lives in `followups-archive.md`, which is
the pre-board ledger — this file is deliberately not backfilled with IDs the
board never issued.

> **A row is at most three short sentences in plain language: what closed, why,
> and where anything durable now lives.** A finding worth keeping —
> a measurement, an owner ruling, a defect class, a tolerance — goes to its own
> home first (a `reference/` doc, a test, an ADR, a board note) and the row
> points there. The row is the index, not the storage; full rule and the
> retrofit procedure in `README.md` § The cap on `LOG.md` rows.
>
> Rows written before 2026-08-19 predate the cap and are shortened one at a
> time, only after grepping that the detail being dropped survives elsewhere.

| Closed | ID | Item | Area |
| ------ | -- | ---- | ---- |
| 2026-09-01 | t2608222016 | R14 config shape - naming, nesting and section policy — **shipped.** All nine phases `P0`..`P7` landed 2026-08-25..29 and merged as one bundle (`5e048c00`, PR #1) as `K3` requires; Gate 5 RELEASED 2026-08-30 on `test-full` 3199 with CI green on both legs, the four data baseline targets unchanged, and `semantic_tree_diff.py` CLEAN — **0 missing, 0 extra** after three declared renames, which is the load-bearing number. The gate record is `dev/milestones/r14/config-shape-gate5.md`: it enumerates the 23 content differences (every one read, none unexplained), the four things the gate does NOT cover, and the ordering defect worth preserving — the tree-shape half ran the day AFTER the merge it was supposed to gate. Eight follow-ups were boarded rather than absorbed (`t2608290250`, `t2608290620`, `t2608301524`, `t2608241413`, `t2608241414`, `t2608242212`, `t2608222239`, `t2608222252`) | config / schema |
| 2026-09-01 | t2608251900 | R14 - the T2 key renames, now owned by P1b — **closed on its own stated condition.** The item existed as the MEASUREMENT behind the owner's 2026-08-25 ruling for a new phase between `P1` and `P2`; `P1b` landed 2026-08-26 (four commits, all four entry points dry-running clean on v2, `V2_BLOCKED` retired, declared-red 83 -> 73) and `P1c` on 08-29. The durable part is the correction it carried, now in `dev/milestones/r14/config-shape-p1b-readers.task.md` § Executed 2026-08-26: the occurrence counts that motivated the phase were wrong in BOTH directions, and the number that mattered was config-key READS (~30), not bare-token sites (~200, which must not change) | config shape / R14 |
| 2026-08-25 | t2608220920 | Closed on commit-level evidence rather than on a green gate: `b4c58d8b` re-recorded 624 of the 630 rows of `indicator_ref/74ed83c06b2e7e6c.csv` at 10:00 on 2026-08-22, 33 minutes after `da9fa7fc` boarded this note at 09:27 — the premise was **true as written**, was fixed in the same session by the very commit that boarded it, and nobody flipped the note. The separation the note asked for was consciously forgone, not delivered: `b4c58d8b` names `cf5daa0` as the cause but also moved two config snapshots, by owner ruling at the gate, and that cost is written down in its own message and in `dev/milestones/r13/baseline-pass-1-result.md` rather than absorbed. Verified from the primary on 2026-08-25 — 7/7 green with a **zero run-path diff** between the recording commit `0d256a41` and HEAD `77e494b3`, which is what makes the pass a statement about the branch and not about an untracked fixture — and the four data targets' hashes, the environment, and the two targets the manifest does NOT hash now live in `dev/baseline/provenance.md` | baseline / wf3 |
| 2026-08-22 | t2608211256 | Split the project config into a T1 project file plus per-workflow T2 files composed by a shared loader, then hoisted `wflow_outvars` to `shared:` and retired the cross-workflow read registry. Both baseline-scale falsifier passes ran from the primary and are recorded in `dev/milestones/r13/baseline-pass-1-result.md` and `-2-`: the split moved the two narrow config snapshots, the hoist moved all three, and **zero data targets moved in either** — the hoist's neutrality tested where it could have failed, since it re-fired 1.09 `declare_wflow_outputs` and 1.14 `run_wflow` yet left `wflow_sbm.toml` and the discharge byte-identical. Pass 1 also dated a `q_indicators` drift that had been red since 2026-08-17 to `cf5daa0`'s weathergenr 2.0.0 upgrade rather than to R13, boarded with two other findings as `t2608220920`, `t2608220915` and `t2608221010` | config |
| 2026-08-21 | t2608191420 | `test_stage_data.py` installed fake geopandas/rasterio/xarray process-globally via `sys.modules.setdefault`, so the two-module staging subset a developer runs while iterating reported failures that did not exist. The stubs turned out to be unnecessary and were removed rather than scoped; a comment in the file says why they may not come back. Both orders of the pair now report 58 passed, and the affected tests EXECUTE rather than skip — measurement in `def3f37` | test hygiene |
| 2026-08-21 | t2608210029 | `RasterStyle` moved to a cartopy-free `shared/raster_style.py`, with `cartographic_map` re-exporting it so no public name moved, and `plot_spatial_maps` now defers everything heavy into the functions that use it. `build_model.smk` imports that module at PARSE time for `figure_paths` alone, which had been pulling the whole drawing stack: importing it went from 1125 modules with cartopy/geopandas/xarray/matplotlib to 127 with no heavy library at all (`c94c344`). Rule 1.12's `code` rerun trigger fires once per `project_dir`; nothing it draws changed | performance / workflow parse |
| 2026-08-21 | t2608210029a | `derive_change_factors` and `plot_climate_source`'s chirps branch both used the `.raster` accessor while relying on a sibling module's import to register it. Neither was a live defect — both were probed — but the dependency travelled through the import graph rather than being stated, so deferring an import elsewhere would have broken a file that never mentions hydromt. Each now imports hydromt beside the access (`d868075`) | projections / correctness |
| 2026-08-21 | t2608202307 | Snakefile parse imports the heavy stack via script modules — eight modules, not the five the note listed, pulled hydromt/cartopy/geopandas/xarray/pandas/matplotlib/scipy into every parse; deferred into the functions that use them, and own-parse time across the four dry-runs went 41.6s to 10.3s. WF0 now loads no heavy library at all, WF2 and WF3 only numpy; WF1 keeps cartopy because `plot_spatial_maps` builds module-level `RasterStyle` constants, which is filed as t2608210029. Two things outlived the note: both WF2 kernel hashes moved, so each project re-derives its series cache once (owner-ruled, no digest is pinned), and F821 turned out NOT to cover a `TYPE_CHECKING`-guarded module — `TC004` is now in `[tool.ruff.lint] select` carrying that measurement | performance / workflow parse |
| 2026-08-21 | t2608202351b | Phase-3 polish of the Quarto site, which now renders 10 pages instead of 7: a branded `docs/theme.scss`, a favicon, `404.qmd`, the two migration notes, and Next blocks on the two guide pages that ended with nowhere to go. Light-only by ruling — a dark base was wired and removed; the note's premise that the landing page was a stub was wrong and was corrected in place before closing. Two findings outlived it: quarto makes a 404's links root-absolute, so that page is knowably broken on a Pages subpath until `site-url` is set (now a step in [[t2608202352]]), and the three brand colours are approximations, not the Deltares guide ([[t2608210010]]) | docs / site |
| 2026-08-20 | t2608201725 | Document WF0 in rule-index.md — the file covered WF1/WF2/WF3 and did not mention `analyze_climate.smk` at all, while AGENTS.md routes an agent there when editing a rule. Added a WF0 section in the existing shape: dependency diagram, summary table, and a Does/Writes entry per rule, with Writes transcribed from each `output:` block rather than summarised. Ten numbered rules; `0.04` and `0.05` are bare `rule:` blocks generated per candidate source, each setting its own `name:`, and `0.06` is declared only when more than one source is configured, so the section says so rather than implying a fixed set. `0.07`–`0.09` are documented as RESERVED. Verified against the Snakefile rather than transcribed on trust: every id round-tripped, and `CLIMATE_LEVELS`, `REGION` and `SPATIAL_UNITS` were resolved to their real paths. The page is retitled and its gap notice retired | wf0 / dev records |
| 2026-08-20 | t2608160937 | Six console colour tests fail when the suite is launched with redirected stdout — **fixed, and the recorded cause was wrong.** Not the detached launch or the console handle: `NO_COLOR=1` is set in the PowerShell profile and inherited by `Start-Process`, while the Bash tool's shell does not set it. `_ConsoleHandler` honours `NO_COLOR` per no-color.org, so the seven painting tests were asserting a branch the environment had switched off. Proven by holding the shell fixed and varying only the variable: 270 passed unset, 7 failed with `NO_COLOR=1`. The tests were under-specified — they faked a TTY stream but left the other half of the decision ambient, and seven OTHER tests in the same module already cleared it. Fixed with a module-scoped autouse fixture, so the count cannot drift again, plus a new test pinning that `NO_COLOR` IS honoured — the branch nothing covered, which is why the variable stayed invisible. 271 passed both with and without it | testing / console |
| 2026-08-20 | t2608201134 | A catalog edit moves every raw digest with no schema bump, and the fixture check fails instead of skipping — **shipped; the check now names the component that moved, and the fixture is current again.** `b0963e9` added `tasmin`/`tasmax` to `cmip6_data.yml` 72 minutes after `test_local` was written, moving `entry_identity` and so every raw digest while `cst_schema_version` sat still, which the whole-fixture skip cannot see because it keys on schema alone. Raw slices now stamp a diagnostic `cst_entry_identity_digest` so the check skips a stale artifact while still failing a broken recipe -- no `SCHEMA_VERSION` bump owed, and `series_identity.entry_identity_digest` carries why plus the test pinning it -- and after a WF2 re-run `check_baseline.py check` reports **OK -- 7 target(s) match**, so the edit moved every digest and NO number. | wf2 projections / test fixtures |
| 2026-08-20 | t2608191613 | A source with two published versions is globbed, and both are opened — **shipped in two steps; the newest published version now wins.** A diagnostic landed first (naming every version per variable instead of letting xarray's combine raise), then the owner ruled on 2026-08-19 that a newer version is a revision and wins, taking the refusals from 221 of 2426 member combinations down to 39 and moving `SCHEMA_VERSION` 5 -> 6 so the rule change refuses cached slices loudly. The measurement, the three failure faces and the 136-of-289 evidence that per-entry catalog pinning cannot express these pins are in `dev/reference/workflows/wf2-cmip6-store-readability.md` section 2. | wf2 projections / remote store |
| 2026-08-20 | t2608191546 | Promote t2608182020's measured grid tables into dev/reference/, then close the note — **shipped, and it took t2608191613 with it.** Both notes were cited from shipped code as the thing that HOLDS a measurement -- two by path, one inside a `RuntimeError` a user sees -- so both are promoted into one document, `dev/reference/workflows/wf2-cmip6-store-readability.md`, beside the store inventory: they are the same finding, a model silently absent from a WF2 ensemble. All eleven citations repointed, and the stale claim that `UA/MCM-UA-1-0` cannot be read is corrected at `stage_cmip6._unreadable`. | wf2 projections / dev records |
| 2026-08-20 | t2608192107 | Give analyze_projections a member-selection policy and a per-model member override — **shipped; `members` is an ordered preference, not a set.** `member_selection: first_available` (the new default) resolves at most one member per model across ALL requested scenarios, `all` keeps the old union behaviour, and `member_overrides` names a realisation per model and hard-errors rather than falling back. This closes a silently wrong number -- three models complete at both `r1i1p1f1` and `r1i1p1f2` would have been weighted double in the cross-model mean -- and supersedes ruling R3', which is recorded in `resolve()`'s docstring where the contract is read -- and no migration note was written, because every tracked config is single-element and resolves identically under both policies. | wf2 projections / config contract |
| 2026-08-20 | t2608182020 | 40% of CMIP6 models are refused by hydromt's regular-grid check — **shipped; 28 models rescued, not 27.** `fetch_raw_slice` re-reads an irregular store unclipped and applies the bbox itself, verified by staging all 29 candidates and reducing each through the real reducer -- 27 wrote and reduced cleanly, and the two that did not are an unstructured ICON mesh and a 12-month store, neither a grid problem. The measured tables, the staged-readability sweep and the rejected alternatives now live in `dev/reference/workflows/wf2-cmip6-store-readability.md` section 1, which is what its six code citations point at; the note is gone and the reference doc replaced it in the same commit. | wf2 projections / remote store |
| 2026-08-19 | t2608192113 | Refresh test_local's WF2 slices to schema 6, so the digest check stops skipping — **done; the check runs again.** WF2 re-ran from the primary and rewrote all 9 raw slices and 9 series under schema 6, 24 jobs in 6:00. `tests/test_stage_cmip6.py` is 29 passed / **0 skipped**, and `check_baseline.py check` reports **OK — 7 target(s) match**, so the schema bump moved the digest and no number. The note's wider point stands and is not repeated here: this was the second bump to strand the fixture, and a third should raise whether that check ought to fail rather than skip | wf2 projections / test fixtures |
| 2026-08-19 | t2608132100 | Re-render the workflow notebooks when their banner sha falls behind — **superseded; the condition it watched cannot occur.** The watch-item's own premise was that the notebooks "commit their rendered outputs and carry a dated `rendered against <sha>` banner", stamped `ca4c9df`. Both halves stopped being true on 2026-08-14, when the 2026-08-13 rot-control ruling (option C) was reversed on measured cost — a notebook's base64 PNGs do not delta-compress, `Model building.ipynb` was 6.43 MB across 82 blob versions, and one rename sweep turned a few hundred KB of text into a 7.1 MB push. Outputs were stripped and the reader-sees-results goal moved to publishing a rendered copy as an Artifact (option D). A watch-item earns its place by naming a Trigger someone can RECOGNISE, and this one's trigger is a banner falling behind: with no banner it can never fire, and leaving it on the board costs each reader the three documents it took to establish that. Rot-control did not vanish with it — it lives in `docs/notebooks/README.md` § Re-rendering, whose check is unchanged | docs / notebooks |
| 2026-08-19 | t2608132140 | Make the notebooks' run() helper fail loudly on a nonzero exit — **shipped, and the reason it was deferred no longer exists.** `run()` in all three `docs/notebooks/*.ipynb` now takes a keyword-only `check: bool = True` and raises `RuntimeError` naming the exit code and the command; each `--unlock` cell passes `check=False` with a comment saying why that exit is expected. Verified by EXECUTING the extracted helper rather than reading it: success returns 0, a nonzero exit raises, `check=False` returns 3 without raising, and a positional `check` is refused. The item had been parked to ride along with a re-render, because editing notebook source invalidated the `rendered against <sha>` banner — but that mechanism was retired on 2026-08-14 when the rot-control ruling was reversed on measured cost, so there was nothing left to ride along with. The notebooks now carry ZERO output cells (verified) and `docs/notebooks/README.md` states there is no longer a `<sha>` banner to trust or distrust. The separate re-render check — no `error` output, no `Error in rule` / `Exiting because a job execution failed` in captured text — is unchanged and still required; raising narrows what it must catch rather than retiring it | docs / notebooks |
| 2026-08-19 | t2608191308 | Refresh test_local's WF2 raw slices to schema 5 — **done; the skipped contract check runs again.** WF2 re-run from the primary rewrote all 9 raw slices and 9 series under `cst_schema_version = 5` / `cst_buffer_cells`, 24 jobs in 6:17. The schema bump REFUSED the schema-4 copies rather than reusing them, which is t2608182238's mechanism observed rather than argued. `tests/test_stage_cmip6.py` is now 12 passed / **0 skipped** — the digest-reproduction test is the only pin on stage_cmip6's cache-compatibility claim and a Snakefile is not importable, so while it skipped that claim was unchecked, not merely untested. `check_baseline.py check` reports **OK — 7 target(s) match**, which is the end-to-end proof that the rename moved the digest and no NUMBER: a changed footprint would have shifted the change-factor CSVs. Incidental finding: 9 slices fetched in ~6 min total, against the ~19 min PER SOURCE the WF2 design doc records from 2026-07-30 — the stage-A split is still correct, but that figure is stale as a planning number | wf2 projections |
| 2026-08-19 | t2608182238 | WF2's `buffer_degrees` is spent as CELLS, not degrees — **renamed to `buffer_cells`, semantics untouched, `SCHEMA_VERSION` bumped 4->5.** hydromt reads `buffer` as resolution multiplicity, so one unit is 0.70 deg on EC-Earth3 and 2.77 deg on CanESM5; three places (the constant's own history as a bare `buffer = 1`, `bbox_index_slice`'s CELLS contract, t2608182020's irregular branch) had already committed to the cell reading, so the NAME was the defect and the footprint was never wrong. Making it truly degrees was rejected as a **sampling** change needing its own justification, not a naming fix. The rename moves every raw and series digest on its own — key names are canonicalized into the hashed JSON — but only SILENTLY, which is the mixed-provenance failure t2608182020 ruled against; the schema bump is what turns a quiet re-derive into `cache_hit`'s loud refusal. Freezing the digest key to save the cache was rejected: it would leave the provenance record and the cache key disagreeing about the field's name. Cost accepted with the measurement attached — 9+9 slices in `test_local` (632 KB) and 4+4 in `test_rapid` re-fetch, and no number moves, so the baseline must re-record identical. **Left open:** the `test_local` fixture is still schema 4, so the staging tool's digest-reproduction test SKIPS naming the refresh — skipped, not failed, because a digest is only comparable within one schema | wf2 projections |
| 2026-08-19 | t2608121258 | Propagate the post-R11 test_local fixture back to the primary checkout — **already resolved when checked; closed on evidence, not on action.** The primary's `q_indicators.csv` carries the post-R11 `metric,location,st_id,rlz_id,value` shape with an **2026-08-16** mtime, `tests/test_interchange_contracts.py` is 90 passed / 0 failed including `test_hm7_integration`, and `check_baseline.py check` reports **OK — 7 target(s) match manifest**. A WF3 re-run in the primary did it four days after the note was written and nobody closed the item, so it sat on the board reading as a live false-red for a week. **The note's measured trap did not materialise, and the distinction is worth keeping:** it showed that COPYING the worktree's tree back would fix `test_hm7_integration` while breaking the one baseline target the primary passed exactly — 104 differing entries, two different runs under different configs, not two versions of one artifact. A RE-RUN in the primary is a different operation and satisfies both. That answers the note's open question — how a regenerated fixture is meant to travel back — with: it does not travel. Regenerate it where the baseline was recorded, and let `worktree_seed` carry it outward, which `slot-start` already does while refusing to overwrite a seed that LEADS the primary | test fixtures |
| 2026-08-18 | t2608132341 | Verify heartbeat cadence on the silent Wflow rules; add backoff if it floods — **closed by removing the premise, not by touching the watchdog.** The question the item posed is answered NO, and structurally so: `Wflow/src/logging.jl:42-51` swaps the `TerminalLogger` (the only renderer of `@progress`) for a `NullLogger` under `silent = true`, and the file leg separately drops `log.group !== :ProgressLogging` — both legs destroy progress, so no configuration recovers the bar and 74a6e3b's silencing necessarily made rules 1.14 and 3.15 mute for their whole duration. Rather than back off the heartbeat, the rules were given progress to emit: `shared/wflow_progress.jl` tees a `FrameLogger` alongside Wflow's own file logger and writes a throttled `[cst-progress] <label> <fraction>` frame, which `WflowFrameRelay` re-renders as the house bar. **The seam is `Wflow.run(config)`, not `Wflow.run(tomlpath)`** — the latter installs its own logger via `with_logger`, REPLACING the ambient one, so wrapping it from outside can never see the records; building the config and calling `init_logger` ourselves is what allows a second leg. The child reports only a NUMBER because all three rendering constraints (no ANSI, not a TTY, cp1252-safe) are properties of the PARENT's stream, which the Julia process cannot observe — the same split rule 1.08 already uses for hydromt (`DaskFrameRelay`). `ProgressLogging`/`LoggingExtras` are Wflow's transitive deps and NOT resolvable under our `--project=.`, so the level is spelled as the literal `LogLevel(-1)` and `TeeLogger` is 15 hand-rolled lines — no `Project.toml` or Manifest churn. **Two defects found only by running it.** (1) A finished run reports 100% TWICE (ProgressLogging emits `i/n == 1.0` then a `"done"` sentinel), printing the completed bar as two identical rows; the relay now swallows the repeat, and `feed` distinguishes `""` (recognised, not drawn) from `None` (not ours) because conflating them put a raw `[cst-progress]` sentinel in the log. (2) A child dying mid-bar left the log's last row unterminated — the terminator must REPLACE the dangling `\r`, since `_cr_overwrite` keeps the last non-empty `\r`-segment and an appended newline would become that segment and blank the bar. Measured on `test_rapid`: model build 44.8 s vs a 53.3 s instrumented loop, so ~1 heartbeat notice per member remains, during the uninstrumented build phase — against the ~60 lines/hour/job the item was opened against. Rule 3.15 gets the same bar per member, labelled with the member tag, alongside its existing `[k/N]`. `run_default/log.txt` is preserved clause-for-clause (version banner, FEWS-conditional backtrace, `rethrow`, `close` in `finally`, try INSIDE `with_logger` so a crash's stacktrace still reaches the file) | logging / console |
| 2026-08-18 | t2608071216 | Make the WF3 batch-size default actually disk-aware — P3-3 design §6.1 names the DISK ceiling as the binding constraint on `B`, but the landed default implemented only the *parallelism* ceiling (`ceil(K / -c N)`), which scales `B` **up** with sweep size and so grows peak transient disk as the sweep grows; the constant `batch_size_max: 8` bounded the blast radius but is not a disk computation. **The part the note called hard is solved by MEASURING rather than modelling.** The note assumed the estimate had to come from grid dims × run length × variable count, because the member forcing NCs are `temp()` and absent at parse time — but WF1 leaves two ordinary persisted artifacts carrying the same per-model constants, `<basin>/forcing/inmaps_historical.nc` and `<basin>/run_default/outstate/outstates.nc`. Bytes-per-timestep is a property of the MODEL (grid, variable count, zlib level), not of the window, so it transfers: measured against a live rapid run, the historical file gives 1379 B/step and a real member forcing 1378 — **0.07 %**. A model would have had to predict complevel-4 output, which is not predictable; reading a file the same writer produced sidesteps it. **Three corrections found by implementing.** (1) The peak is `min(K, cores × B) × per_member`, not `p × B × …` — once every member is resident the batch structure has stopped mattering and the cap degenerates to the whole sweep, which is exactly the degenerate case GN-3 measured; the disk ceiling is therefore only applied when `K × per_member` really exceeds the headroom. (2) `forcing_window` is WIDER than `run_length`: `run_length: 8` at horizon 2050 spans 2046..2054, nine calendar years / 3287 days, so `run_length × 365` under-counts by 12 %. (3) The state anchor is a single snapshot and must NOT be scaled by run length. Headroom is TWO keys with deliberately different units — `defaults.batch_disk_headroom_fraction` (0.25) is a policy, `workflows.run_stress_test.disk_headroom_gb` a budget that wins outright; a constant generous on a workstation is a wrong answer on a laptop, so the default had to be a share. `shutil.disk_usage` reads free space portably, walking up to the nearest existing ancestor. **The cap only ever LOWERS `B`, never raises it, and never raises an exception**: an unavailable estimate (fresh project, WF1 not yet run, `netCDF4` absent) degrades to the previous behaviour, because a safety cap that becomes a new failure mode is worse than the defect it fixes. An explicit `batch_size` wins unclamped but its peak is still computed and warned about. `netCDF4` is imported lazily — measured, it is not preloaded at WF3 parse, so module scope would cost 0.75 s per parse against a 9 ms read. `forcing_window` moved to its own light module so the Snakefile can reach it without `hydromt_wflow`. 40 tests, one reproducing the measured fixture footprint. Ladder verified end-to-end against a real model: parallelism → batch_size_max → disk → warn at B=1 | wf3 batching |
| 2026-08-18 | t2608131847a | Split the historical-climate workflow out of WF1, with a forcing-selection evaluation layer — **closed for the SPLIT, which shipped in full; the evaluation layer continues as `t2608181139`.** The migration landed 2026-08-14: `analyze_climate.smk` exists and runs, the rename went with it (`docs/migration-workflow-names.md`), `scripts/run_workflows.py` orders WF0 first, and `AGENTS.md`'s Overview table, Background and Key Commands describe it. The separability claim was confirmed EMPIRICALLY rather than argued, and doing so corrected it. **Why it is split rather than carried:** the note bundled a finished migration with an unstarted feature, so it sat at `active` while reading as stale — and the half that was done is the half a user notices, which is exactly how a half-true `active` misleads. **The evaluation layer is genuinely unstarted, verified on 2026-08-18 from the tree and not from the checklist**: WF0 runs 0.01–0.06 then jumps to 0.10, leaving 0.07/0.08/0.09 an empty reserved gap, and nothing under `blueearth_cst/climate_analysis/` mentions Budyko or subregion sampling. The fourth notebook is absent too — `docs/notebooks/` holds three. So WF0 today CHARACTERISES the candidate sources (extract, plot, compare against each other) and does not yet EVALUATE them against observations, which is the question the workflow exists to answer. The O-2 ruling of 2026-08-15 (move `observations_timeseries` to `shared:`) is carried verbatim into the successor, with a note that its "fold into the rename migration note" clause has aged out — that migration landed, so the change now needs its own entry. Multi-forcing model runs stay deferred and are explicitly out of the successor's scope | wf1 / workflow split + workflow rename |
| 2026-08-18 | t2608131847b | Add statistics heatmap tables, ported from the fao branch [dropped] — **the port shipped and the consumer never did.** `blueearth_cst/shared/statistics_heatmap.py` (11 KB) and `tests/test_statistics_heatmap.py` (10.6 KB) landed in `3c47b31`, unit-tested over a synthetic frame including `invert_cmap_for`. The remaining two boxes were both *wire a caller*, and the first was explicitly marked NEEDS AN OWNER CALL — so what stalled was a decision about which surface should carry the tables, never engineering. Dropped on owner ruling 2026-08-18. **Consequence, stated rather than left to be discovered:** grep confirms the module has NO caller anywhere in the repo — only itself and its test — so it is now dead code that the suite nonetheless exercises. It was deliberately left on disk rather than deleted with the item: the port is the expensive half, and re-porting from the fao branch would cost more than keeping 22 KB. If it is still unused when someone next sweeps `shared/`, deleting it needs no further ruling — this row is that permission | plotting |
| 2026-08-18 | t2608090907a | Land ADR 0005 stage 2 — reformat the 51 blueearth_cst modules — **the code landed long ago; only its verification never did.** The reformat and the deletion of the `"blueearth_cst/**" = ["I001"]` per-file-ignore shipped in `73c57df` (2026-08-11), released early on an owner ruling so ONE `code` rerun-trigger invalidation absorbed both this and the ruff-`I` adoption (`t2608091116`). Confirmed at closure rather than taken from the note: `pyproject.toml`'s `[tool.ruff.lint.per-file-ignores]` carries a single entry, `dev/scripts/stage_data.py`, with no `blueearth_cst` carve-out left. **What is retired is the outstanding CHECK, not the work**: the note's own text read *"nothing is left to write"*, and it sat blocked on a `check_baseline.py check` needing a full pipeline re-run to compare against — none was ever scheduled, and eight days later none is. Retired rather than carried, because a formatting-only change cannot move a number: ruff `format` rewrites whitespace and quoting, while the baseline compares DATA. The check would have been reassurance, not evidence, which is plausibly why nobody scheduled the run. If a future re-run does surface drift attributable to this, the commit is `73c57df` | formatting |
| 2026-08-18 | t2608071206 | Make the hydro/clim/signatures figure families declarable so `--delete-all-output` can clean them — the per-station sheets now land in `evaluation/plots/stations/`, declared as a `directory()` on rule 1.15. They could never be declared as FILES: they are named for wflow_ids, a product of the model build (rule 1.07 read back through Q_outlets / the subcatchment map), so their count is unknowable at parse time and R7-5's assumed derivation from `wflow_outvars` / `output_locations` was never possible. **The note's open choice was already settled in-repo**: it offered "a checkpoint or a directory()", and WF0 had since chosen `directory()` for the identical per-subbasin problem, its comment already naming rule 1.15 as the twin case. Ported rather than re-decided — and a checkpoint would be wrong here anyway, since it re-plans the DAG to learn a set nothing downstream consumes (no rule reads these PNGs), so the only thing enumeration buys is `--delete-all-output` completeness, which declaring the bin buys outright. Both comments now say the two bins are one pattern. `STATION_PLOT_DIRNAME` lives in `shared/plot_evaluation.py` beside the drawing code, because the script writes into it and the Snakefile declares it. **Two corrections found by implementing.** (1) The note, the in-code O-24 block and the test docstring all named THREE families; there are two — `clim_{station}_{period}.png` has not existed since ADR 0006 removed it on 2026-08-09. (2) The `--delete-all-output` test's knowingly-undeclared CONTROL was itself a station figure, which this change makes reachable; leaving it would have turned the control into a tautology, so it moved to a stray directly in `plots/` and a real station sheet joined the must-be-deleted set instead — the test now pins the fix rather than merely passing. `tree-check` is unaffected and was checked rather than assumed: `semantic_tree_diff` maps `evaluation/` wholesale, so the move reports IDENTITY and the gate stays green on an un-rebuilt fixture. Gates: `test-fast` 2648 passed, `test-contract` 64 passed, `test_cli` 19, ruff clean | wf1 rule shape |
| 2026-08-18 | t2608071203 | Resolve six geojson basenames that mean different things in `data/spatial/geoms/` and the wflow `staticgeoms/` — shipped options **(a) + (b)** of the note, per owner ruling. (a) `blueearth_cst/shared/spatial_geoms_parity.py` asserts the RELATIONSHIP per layer: containment for `region`, tolerant topological equality for the three true copies, explicit NON-comparison for `basins`/`rivers` whose reasons it carries so the contract states them rather than omitting them. A sibling of `interchange_contracts` — same `list[str]` report, parsed objects only, `-O`-safe — but pinning a relationship BETWEEN two trees rather than the shape of one artifact. (b) The authority table lives in the seam contract's HM-3 section, with a validator-index note saying why it sits outside `interchange_contracts`. **Renaming was never on the table**: `staticgeoms/` is hydromt_wflow's output surface and off-limits by hard constraint, and renaming our side would touch `region_rule`/`spatial_units_rule`. **Two things the note could not know, both measured rather than assumed.** (1) `semantic_tree_diff.compare_geojson` is NOT reusable as the note proposed: it asserts equality via shapely `equals`, which is topological but not TOLERANT, and a copy round-tripping through GeoJSON returns displaced 4.7e-07 deg on every feature of all three copy layers — its own docstring explains why it never noticed, since its two trees were the same never-regenerated files. The tolerant test here is therefore a Hausdorff DISTANCE, not a symmetric-difference area, because `locations` is points and two points a kilometre apart have a difference of area 0. (2) Exposure 2 is **6 undeclared, not 8**, and appears already closed by ADR 0004 making rule 1.10 the terminal writer of the whole model root — option (c) was not implemented and looks moot, which the note's own amendment had asked someone to check. The three copy layers are 4/4 on today's fixture, not the 5/5 the 2026-08-05 table records: the fixture changed, the relationship did not. 19 tests — synthetic cases pin the semantics on every checkout, two fixture-backed cases assert against a really-built project and are armed at REBUILD time rather than upgrade time, and say so rather than implying a guard they cannot give. Gates: `test-fast` 2612 passed, `test-contract` 64 passed on re-run (the first run hit the `t2608071208` staging stall, recorded there) | naming |
| 2026-08-18 | t2608082036 | Re-derive the WF3 v2 execution design against the post-R11 tree [dropped] — owner ruling, no longer needed. Dropped rather than left to age at the front of the queue: `t2608152230` had cleared its blocker on 2026-08-16, so it was runnable and simply not wanted. **Eight references to this ID survive it, and this row is what they now resolve to**: `dev/milestones/r12/stress-test-lookup-design.md` (x3, assigning WF3 execution mechanics — rule shape, rerun triggers, log and benchmark parts — to this item), `stress-test-lookup-intake.md`, `dev/roadmap.md` (x2, still calling it front of the queue), `dev/tasks/t2608151154-st0-not-comparable-with-the-surface.md` (x2, naming it a live consumer of that item's `baseline: true` lifecycle branch), and the `t2608152230` row below, which ends by saying this item is unblocked and must key `member_hash` on the monthly lookup rows. None were edited at drop time — the ruling was to drop the ITEM, not to declare the WF3 v2 execution work abandoned, and those documents record a design position that is unchanged by whether a board item tracks it. **If the work is genuinely abandoned, `roadmap.md` and the two milestone docs need saying so; if it returns under a new ID, they need repointing.** Neither was in scope here | wf3 execution |
| 2026-08-17 | t2608172138 | Preflight the wf1 leaves when run_workflows drives wf3 without build_model — shipped as contract clause **(i)**: with `run_stress_test` enabled and `build_model` not, the wrapper checks `LEAVES` against `project_dir` before invoking anything and raises `PrerequisiteError` (exit 2, beside `ConfigError`) naming every absent leaf and `build_model` as producer. It runs **before the manifest**, so a run that cannot start mints no invocation record. **The clause does not reverse the docstring's refusal to do prerequisite checking** — that refusal is about STALENESS, which needs the DAG to judge and would drift if duplicated; this is EXISTENCE, and it cannot disagree with Snakemake about whether a rule should re-run. A stale leaf still resolves the DAG and yields an answer the user owns; an absent one leaves no run whose staleness could be owned. **The leaf list moved** to `blueearth_cst/shared/cross_workflow_leaves.py`: the note proposed importing it from `dev/scripts/cross_workflow_inputs.py`, which implementation showed to be a boundary violation — `dev/scripts/` is "never part of a run", so the user-facing wrapper would have gained a dev-only runtime dependency. The STAGING machinery stayed in `dev/` and re-exports the names, leaving all five prior consumers untouched and letting the preflight inherit `test_cross_workflow_inputs`'s complete-and-minimal proof against the real DAG. Rider, named rather than absorbed: `run_workflows.py` carried the same banner-fallback defect `32e506c` fixed in the four Snakefiles, uncaught because that commit's test enumerated `.smk` files only — fixed, and `BANNER_SOURCES` now includes the wrapper. Two console tests moved off the `build_model`-disabled + `run_stress_test`-enabled pair the preflight now refuses; both chose it incidentally, and comments record why. Gates: `test-fast` 2563 passed, `test-contract` 64 passed, ruff clean | orchestration / run_workflows |
| 2026-08-17 | t2608172214 | Rebasing refactor/lean-wf0-console must keep the banner fallback nested | wf0 / console + run banner |
| 2026-08-17 | t2608071209 | Decide real packaging (O-14 decision 2) and unblock flit (O-16) — **ruled: stay unpackaged**, recorded as `dev/decisions/0008-ship-blueearth-cst-unpackaged.md` and indexed. `blueearth_cst` is not an installable package and `pyproject.toml` stays tool-config-only; nothing here is imported as a package (the modules are executed by Snakemake `script:` directives and Rscript shell bodies), and pixi already owns the environment for all three languages. The counter-case was weighed and does not apply: `pythonpath = ["."]` in that same tool-config-only `pyproject.toml` already gives `tests/` ordinary `from blueearth_cst...` imports, with the 34 `sys.path.insert` shims removed long ago — so the usual first argument for packaging a source tree was already answered by O-14 decision 1. **O-16 therefore resolved to "drop it"**: `flit = ">=3.2"` is gone from `pixi.toml` and the lock was regenerated through pixi, never hand-edited, because CI runs `locked: true`. **The re-solve carried one rider, named rather than absorbed**: the lock also lost the conda `docutils 0.22.4`, which `flit` was what pulled in, and the solver re-satisfied `docutils` from PyPI (0.23) inside the PyPI-installed snakemake's own dependency block. A source swap for a transitive dependency, not a version bump anyone asked for, and arguably the more coherent state. Gates: `pixi run test-fast` 2475 passed / 7 skipped / 64 deselected / 1 xfailed; `snakemake --version` 9.6.2, `import docutils` 0.23, `import flit` ModuleNotFoundError — the last being the point. This completes the R7 tooling contract: O-14, O-15 and O-16 are all closed | packaging |
| 2026-08-17 | t2608121742 | `run_weather_generator` does not forward `relax_priority` — closed by the weathergenr 1.2.0 → 2.0.0 upgrade, and the upstream defect was deeper than the note could see. The note measured the wrapper (19 of 20 arguments forwarded) and concluded the argument was merely unreachable through `run_weather_generator`; weathergenr 1.3.0 records that `generate_weather` **ignored it even when called directly** — accepted, validated and documented as forwarded to `filter_warm_pool()`, then discarded for a hard-coded order. So the config's omission was right for a better reason than it gave: on 1.2.0 the lever did not exist at either entry point. 1.3.0 forwards it from both; 2.0.0 renames it `relax_order` to match `filter_warm_pool()`'s own spelling. Restored exactly as the note's option 1 prescribed — key in `config/defaults/weathergen_config.yml` (weathergenr's own default order, spelled out, so this restores a LEVER rather than changing behaviour), pinned in `_WG3_GENERATE_WEATHER_KEYS`, the three explanatory comments deleted, and a case added to the surfaced-argument tests in `test_prepare_weathergen_config.py` and `test_interchange_contracts.py`. Verified against the installed 2.0.0 rather than the release notes: a formals-vs-body scan reports `relax_order` among the 23 `config` entries the wrapper forwards. **`relax_priority` is still a formal** — 2.0.0 keeps both names with a deprecation warning and makes supplying both an error — so the config must name the new one only | upstream / weathergenr |
| 2026-08-17 | t2608071226 | The wavelet minimum surfaces as a cryptic error rather than a stated requirement — fixed upstream in weathergenr 2.0.0, in the words the note suggested. `wavelet_cwt.R` now stops with *"Historical period (N years) is below weathergenr's wavelet minimum of 16 years. Extend the historical range, or reduce the wavelet decomposition depth."* in place of the bare `length(series) < 16` failure. Nothing changes in this repo: `min_historical_years` in `config/advanced_settings.yml` is our own pre-flight guard and still fails first, at WF1 parse time, which is the point where a user can act on it. The upstream message now backs it up for anyone who reaches weathergenr another way | upstream / weathergenr |
| 2026-08-17 | t2608071225 | Generated netCDFs lose `spatial_ref` from their template — fixed upstream in weathergenr 2.0.0, and the load-bearing workaround it forced is **deleted**. `write_netcdf` now copies the template's `spatial_ref` VALUE and every atomic attribute on it, unconditionally, which is exactly the removal condition the block in `generate_weather.R` carried; the hand re-patching of `x_dim` / `y_dim` through `ncdf4::ncatt_put` after every realization write is gone with it. **Verified rather than read off the release notes, at both levels the note cared about**: a `read_netcdf` → `write_netcdf` round trip on the `era5_20000101_20161231` store returns all 13 template attributes with `hasatt=TRUE`, `x_dim` and `y_dim` among them; and the full rule 3.11 → 3.12 chain was run on that fixture with the workaround already removed, where `impose_climate_change.R` takes the generated realization as ITS OWN template — the consumer whose "attempt to select less than one element" failure the workaround existed to prevent — and produced a perturbed member that satisfies `validate_wg4` at the requested −30% precipitation | upstream / weathergenr |
| 2026-08-17 | t2608071215 | Enforce the naming convention with a linter instead of review attention [dropped] — the note's own text made it conditional ("add as an R3+ followup **if** drift becomes a real problem"), and in the eleven milestones since R2 established the convention no drift has been reported that review attention missed. Dropped rather than demoted to a watch-item because a watch-item needs an observable Trigger and this one's would be "someone notices drift", which is the status quo rather than a signal. `dev/reference/naming.md` remains the convention; git history carries the idea if the case ever arrives | tooling |
| 2026-08-17 | t2608112239 | Rename the project-dir config/templates snapshot bin to say what it holds [superseded] — by `t2608131304`, whose accepted design **deletes** the bin rather than renaming it: the current-only run record copies a referenced file into the project only when the toolbox repo cannot give it back, so the two hydromt build files this bin held are recorded by git blob id instead. That also dissolves the item's hardest unknown — `"templates"` as the snapshot `kind` string hashed into the run digest — since there is no bin left to key. The superseding item's Refs already said so; this closes the loop | naming |
| 2026-08-17 | t2608091116 | Adopt ruff's I rule family — import sorting — in two stages, like ADR 0005 — **already shipped; the note was stale.** Both halves landed: the 42 files outside `blueearth_cst/` in `b124a98` (2026-08-09), and the remaining 21 plus deletion of the `"blueearth_cst/**" = ["I001"]` per-file-ignore in `73c57df` (2026-08-11), released early on an owner ruling so one `code` rerun-trigger invalidation absorbed both this and ADR 0005 stage 2. Verified at closure: `pyproject.toml` carries no `I001` carve-out, and `I` is in `[tool.ruff.lint] select`. The note's two open boxes described exactly what `73c57df` did — it was written on 08-09 and never revised after the landing. `UP`, `B`, `SIM`, `PERF`, `RUF` stay deferred and unmeasured; this is not a precedent for them | formatting |
| 2026-08-16 | t2608132341a | Give run_workflows.py workflow-level banners and an end-of-invocation summary — shipped on `lane/pipeline` as `987ec6e` (the console) and `3dbcf12` (a defect caught reviewing it). The wrapper now opens with a block naming project, folder, config and cores plus an **ASCII sequence diagram** numbering the enabled workflows in invocation order and marking the disabled ones in place, carries one `starting` / `done in <h:mm:ss>` row per invoked workflow, and closes with a verdict, total elapsed, per-workflow durations and the project / logs / invocation-manifest paths — the last of which was written and never announced, which is the half of the ask that matched `run_summary`'s own "invisible unless you already know it exists" rationale. **The load-bearing decision is a TIER SPLIT the note did not anticipate:** the two blocks go through plain `print` and only the timeline rows through `log_row`, because `log_row` honours a `CST_LOG_LEVEL` floor — routing the blocks through it would delete the only statement of which project and which config a run was, exactly when someone asked for less noise. Pinned by a test. **Two deviations from the note.** It asked for a colour gate reused from `rule_banner`; the blocks are PLAIN, because `_console_colour` must be asked of the stream the caller writes to and `run_summary` asks `stderr` since that is where it writes — the wrapper writes `stdout`, so reusing the gate as written would paint a redirected file, and colour is a poor trade on a contract surface anyway. And the per-skip `log_row` was DELETED rather than demoted: the sequence diagram states every disabled workflow once, before anything runs, so the inline repeat was the noise the ask was about. **The defect that mattered is invisible to the entire test tier by construction.** stdout is now flushed before each `subprocess.run` — Python block-buffers a redirected stdout while the child inherits the fd directly, so without it a `run_workflows.py ... *> log.txt` interleaves every banner AFTER the output of the workflow it announces. No test can catch that: the suite fakes `subprocess.run` and the child writes nothing. It was found by running the thing redirected to a file and reading the result, which is also the only evidence the fix works. `logs` names the DIRECTORY, not the per-workflow files, because `wf3_run_stress_test_<experiment>.log` is built from a resolved experiment id and reconstructing it here would put a second definition in the one place that cannot notice when it drifts. **A self-inflicted near-miss, caught in review rather than by any gate:** `_project_name` cited `plot_workflow_dag.read_project` as its authority — the point being that one console and one render filename must not disagree about the project's name — and then added a `project.project_name` fallback that function does not have, which is precisely that defect, over a key no schema defines. Dropped; top level only, as the docstring claims. Three stale references fixed in the same sweep, two of them pre-existing: the module docstring's "three Snakefiles", the `run-workflows` pixi task description still naming the pre-`analyze_climate` order, and README's matching "(model → projections → experiment)" plus "all three subsections". 12 new tests, covering the diagram, both failure paths, the no-op invocation, the quiet level, secret redaction and cp1252-encodability | logging / console |
| 2026-08-16 | t2608152230 | Collapse the stress-test parameter artifacts into one lookup table, and make the surface axis a parameter — implemented against the accepted design (`dev/milestones/r12/stress-test-lookup-design.md`, D1–D35) in nine steps over `fb9f1ad..afc7efd`. `<exp>/config/stress_test_lookup.csv` (`12 × ST_NUM` rows, keyed `(st_id, month)`, percent, no `st_0` row) replaces the per-member `_work/st_<m>.csv` grid and absorbs `stress_test_design.csv`; the indicator header drops to five columns and the response-surface axis becomes a derivation over the lookup (`shared/surface_axes.py`, specified by HM-7). **The results delta is none, and it was measured rather than assumed**: V4 reports 55 groups / 0 failing against the step-0 reference trimmed to the new column set, and V16 reconstructs 144 multipliers at **0.0 ulps** — bit-identical. **Three design defects were found by running the validation plan, not by reading it, and all three are invisible to the shipped configs.** (1) `rtol = 1e-9` for the rectilinearity postcondition was a hundred times BELOW its own noise floor: D17 justified it against float64 noise while D7 deliberately quantizes the grid levels to `float32`, so three of eight realistic grids were refused — including `0.6–1.4` at `step_num: 3`, the exact grid V20 is built on, which the design celebrated in one decision and refused in another. Ruled to `1e-6`. Unreachable from the seeds, whose grids have two or three levels and pass trivially. (2) The caption rule (`≤ 3` months → initials) contradicted two of its own illustrative captions; the rule stands, so `OND`/`AMJ`. (3) A **pre-existing** mis-splice in `hydrological-model-seam.md` had cut the relational validator's item 3 in half around a stray copy of HM-7's `st_id` bullet since at least `efd69eb`; it surfaced because the design's own check-3 amendment could not be applied without healing it. **V21 is what caught (1)** — the notebook was rewritten as a contract-based consumer importing nothing from `surface_axes`, and its transcription was extracted from the `.ipynb` and compared against the reference implementation over five designs (uniform, two seasonal, degenerate, non-round); a uniform-only comparison would have found neither the tolerance defect nor my own abbreviated caption transcription. **The step 5b sweep found four FALSE GREENS rather than four failures** — `_wg2_good()` built in memory, `test_wg2_integration`'s segment-spelled `_work` path, and two `check_baseline` fixtures, all passing while asserting a dead contract; `test_float_key_columns_are_compared_as_written` was re-witnessed on `st_id`'s zero padding rather than deleted, since its stated subject (a float key column) no longer exists. Gates: `test-full` 2326 passed / 6 skipped (the 6 `test_snake_utils` colour failures are an artifact of launching detached with redirected stdout — 200/200 through a pty on the same commit, boarded as `t2608160937`); `check_baseline.py check` 7/7 against the step-7 re-record, whose only pre-record failure was the predicted `column mismatch: 2 only-ref`; `tree-check` classifies the lookup IDENTITY, and `_work/` plus `stress_test_design.csv` reported UNMAPPED before the migration procedure deleted them. The two fixture-dependent seam tests now RUN rather than skip, so `validate_wg2` and `validate_hm7` are verified against real artifacts for the first time. Pre-migration artifacts archived under `dev/milestones/r12/premigration/` because V4 and V16 compare against files the migration itself deletes; `naming.md` §7 record at `dev/milestones/r12/migration_stress-test-lookup.md`. **t2608082036 is unblocked** and must now key `member_hash` on the monthly lookup rows rather than the annual collapse this item abolished | wf3 / stress-test design + reduction |
| 2026-08-15 | t2608132341b | Console polish: color failure and stall verdicts, fix the batch-span context, bracket the context in no-color — all three shipped, two of them in `6943064` and the brackets in `9fc46be`. Bullet (a)'s PREMISE was stale by a day and its substance was not: it described the pre-2026-08-14 "only color is bold cyan on rule starts" scheme, which the three-tier RUN/DONE/body scheme had already replaced — but neither scheme colored the FAILED verdict or the stall notice, so both were still grey when the note was reviewed for closure and the ask stood unchanged. Bullet (b) was HALF done before that review: the `3.15` summary had been tightened to "run Wflow for one batch" while `_member_span` still claimed a rectangle it did not have, which is the half that matters — `(partial)` now fires on a batch whose members do not fill the product of the two ranges, and stays off the batches that genuinely do. Closed only after the residue was implemented; closing on the earlier reading would have buried live work | logging / console |
| 2026-08-14 | t2608141633 | Rename the project-dir config/observations bin to config/basin_data | naming |
| 2026-08-13 | t2608071223 | Long-running rules announce themselves only by rule name, not in plain language — closed by the console-output pass (`74a6e3b`, `7daff59`, `ade9fec`). **The note's own premise was obsolete and the closure is only half what it asked for.** It said "add `message:` directives to the long-running rules"; 47 already exist via `rule_banner`, added since it was parked 2026-07-19. What was actually missing was the plain-language CLAUSE and, more importantly, PER-JOB context — which the note never identified because when it was written there was no fan-out to be lost in. `rule_banner` now takes `summary=` (constant, what the rule does) and `context=` (per-job); the return value IS the `message:`, which Snakemake formats per job, so `{wildcards.*}` in the context resolves per member even though the banner is built once at parse time. `3.12  perturb_climate_realization - apply one stress-test member to one realization  rlz 1 \| st 1`. **Applied to 13 long-running rules, not all 47**, per the note's own scoping: a sentence on every rule lengthens every line to restate what the fast ones already say by name. `3.15` is the one that could not use wildcards — its rules are loop-generated per batch and carry none — so `_member_span()` bakes the range in at generation time instead. **Two traps found by running it, not by reading.** The `message:` call must stay on ONE LINE: Snakemake's rule-body parser is line-based, so a continuation line starting `summary=` is read as a rule keyword and fails at parse ("Unexpected keyword summary in rule definition"). And the separator must be ASCII — a Windows console defaults to cp1252 and raises UnicodeEncodeError on the typographic characters that would read better, the same failure hit while reading this repo's own logs back in the same session. Shipped alongside four sibling changes from the same review: Wflow's terminal logger silenced (`[logging] silent`, one key reaching both workflows since WF3 inherits WF1's TOML), `show-failed-logs` so a failure prints its log instead of naming it, an end-of-run summary naming the merged log and benchmark table, and stall markers made durable in the log file. The Julia box-folding half is deferred as `t2608132310` — `silent` emptied its target population (1410 box lines in the wf3 log, all Wflow `@info`) | ergonomics |
| 2026-08-13 | t2608071204 | Settle whether the unperturbed baseline member belongs in the response tables [superseded] — **already discharged by R11, and R11 said so**: `dev/milestones/r11/wf3-consolidation-scope.md:177` reads "Units A, B, C34 and F7 landed; `[R9-5]` closed". The board row simply outlived the work, which is the same failure shape as `t2608091028` — a note is not code, so nothing in the suite or the ruff gate can notice when a milestone discharges one. Verified rather than inferred, on both open checkboxes. "Delete the dead `st_nb == \"0\"` comparison rather than repairing it": `st_nb` now appears **nowhere** outside `dev/` records — not in `blueearth_cst/`, not in `tests/`, not in a Snakefile — so it was deleted, not repaired, as the note required. "Implement in R11 unit A": `export_wflow_results.py` seeds `axes = {0: (0.0, 0.0)}` with the comment *the baseline sits at the origin by definition*, i.e. the baseline is now STRUCTURAL rather than guarded by a comparison that could be wrong; the module header states the same in prose ("what makes the baseline rows mandatory rather than decorative"). Empirically on `test_rapid`: 90 `st_0` rows, `rlz_id` 0/1/2, both axes at 0. **The note's framing also partly dissolved and that is worth recording**: it was written as "in BOTH shapes" because `aggregate_rlz` chose between two grains, and `aggregate_rlz` has since been retired into `RETIRED_EXPERIMENT_KEYS` (t2608072234) — there is one grain now, so the asymmetry the item existed to remove cannot recur in the form it described. Zero code changed by this closure | wf3 results |
| 2026-08-13 | t2608131847 | Repair and extend the workflow notebooks onto the fao pattern — **done; the note's diagnosis was wrong in the way that mattered.** The blocker was not the config path it named but the `%%writefile` config's old flat schema, which the nested parser rejects outright, plus a hardcoded `os.chdir` that failed for every reader but the author. Ruled: print the shipped config rather than author a copy; the rendering procedure and its four-count verification live in `docs/notebooks/README.md` § Re-rendering, and the `run()` residue closed as `t2608132140`. | docs / notebooks |
| 2026-08-13 | t2608130215 | Fix the confirmed config defects found by the dual parameter review — eight parameters that were declared and did not reach, or did not control, what they claimed. Seven landed 2026-08-13 (`7421d2c`, `6caf0de`, `b31b9a3`); **H closed last** because it was the only one that could produce WRONG NUMBERS rather than missing or unchanged ones, and it needed an owner ruling on who owns each spatial source. Ruled: the project-level key owns both. `river_upa` now derives from `river_mask`'s own `upstream_area_threshold_km2` — the threshold P1 ACTUALLY delineated with, read off the artifact rather than the config, because what must agree is the model and the data it is handed. `soil_fn` derives from P1's `soil_source`. Both deleted from `config/defaults/wflow_build_model.yml`, so the loser is ABSENT rather than ignored — a popped-and-discarded key still reads as a setting to whoever edits the file, which is the defect class this bundle closes. **`soil_fn` was not the shape the note predicted, and the correction is the durable part:** hydromt reads the soil data itself, so `soil_fn` is a source NAME and nothing is injected. The coupling matters because `_resample_source` namespaces every variable of `spatial_sources.soil` with a `soil` prefix, producing the `soil_*` maps **rule 1.12 plots** — so a grep for the layer finds no reader and the figures depend on it anyway. Left free, the template could name a source the basin report never showed: the model running on one soil dataset while its own figures describe another. Value-neutral on every shipped config (32 == 32, soilgrids == soilgrids); it changes results only for a project that had already diverged, which is the bug. `1464868`. **Residue, deliberately not actioned:** two single-reviewer reports were never confirmed in-session and the note said not to action them without checking — `realizations_num`'s documented default `1` possibly unreachable because rule 3.10 re-reads the YAML with a bare subscript, and PET computed twice under two methods (already known as F16, not new). Both need verification before they are work. | config / cross-workflow |
| 2026-08-12 | t2608072234 | Changing any climate_experiment config key breaks every experiment that has already run — **fixed, and closed EARLY**: the note's own trigger was "a third milestone needs the same ruling, or a real project reports being unable to continue", and neither had happened. Two had, and doing it now is the cheap moment — there are no production frozen records to migrate, which is the same conditional R11 rejected the fix under ("new machinery for a problem that has occurred once"). The freeze compared a KEY UNION, so a key the TOOLBOX retired was indistinguishable from a setting the USER changed, and every already-run experiment refused after either. The two prior rulings went opposite ways for good reason and that is the whole evidence base: `aggregate_rlz` (R11) changed the table's GRAIN so refusing was right; `Tpeak`/`Tlow` (`e237f80`) were value-preserving so refusing was a false positive. Resolved by DECLARATION rather than inference — `RETIRED_EXPERIMENT_KEYS` entries carry `existing_results: unchanged|redefined`, because only whoever retires a key knows which. **Not the schema-version concept the note hypothesised**: a version plus a delta table is more machinery answering what one field answers, and the hypothesis predates the second instance that revealed the two classes. An UNREGISTERED key still counts as changed, so forgetting to register a retirement fails loud instead of silently unfreezing the project — the same default as the registry's own refusal, and the same failure mode as the omission fixed in `1acf79e` (which was this session's own defect: `e237f80` retired `Tpeak`/`Tlow` without registering them, handing a project declaring `Tpeak: 25` exactly the silent no-op the registry exists to prevent). A permitted rewrite MIGRATES THE RECORD FORWARD and rule 3.07 logs which keys it dropped. Verified end-to-end on the real frozen fixture record with its run marker present, and falsified one property at a time — four seeded regressions, each caught by the test that names it. `e23a74f` | wf3 experiment lifecycle |
| 2026-08-11 | t2608112047 | Cover the two declared artifacts the post-R9 tree inventory misses — `models/hydrology/wflow/config/climate_store_catalog.yml` (an ENUMERATED leaf: rule 1.10's second declared output, and the model's config/ holds a fixed set, so a new file there must still report) and `config/runs/invocations/` (a PREFIX: the wrapper's per-run manifest, filename `<utc stamp>-<uuid12>.json`, an open set). The manifest cannot ride the `config/runs/<workflow>/<digest>/` regex — that requires a digest level, and `invocations` is not a workflow but a SIBLING of those bundles. Neither artifact was new; both were invisible to every instrument, which is the point: the wrapper is not a rule so the declared tier cannot see it, and `test_local`'s WF1 predates the store catalog while all its runs were direct `snakemake` calls. They surfaced the first time a fixture was rebuilt THROUGH `run_workflows.py` (`t2608111659`). The R9 map has carried the equivalent invocations row since 2026-08-05 — this was the mirror into the post-R9 inventory that was never made. No R9 row for the store catalog, deliberately: it has no pre-R9 form so a relocation row could never fire, the argument that file already makes for `.model_final`. The discriminating gate was the pair of exit codes, not the unit tests: `test_rapid` goes 3 unmapped → MAP CLEAN exit 0, while `test_local` stays at EXACTLY 1 — a row written too broad would have dropped that pre-existing orphan to 0 and looked like success. `26548ba` | test fixtures |
| 2026-08-11 | t2608111659 | Reconcile test_rapid's WF1 snapshot with the rapid config so WF3 can run on the default config — the tree was a REAL WF1 build (all 207 files 14:45) but from an ad-hoc config: an older seed revision with `project_dir` swapped and observations at `C:/TESTS/CST/observations/gabon/`. Which side wins was never a choice: no tracked config can name that absolute path and stay reproducible, so a WF1 re-run was forced either way. The invariant is `test_local`'s — its snapshot is byte-identical to its tracked config. Rebuilt all three through `run_workflows.py` (so an invocation record exists this time; the 14:45 build had bypassed it, which is why the provenance had to be reconstructed by hand). Both snapshots now match byte-for-byte and WF3 runs 34/34. **A SECOND defect, not in the original diagnosis:** the wrapper run regenerated WF2's SNAPSHOT but not its PRODUCTS — the summary still carried 3 models / `far` / 1990-2010 against the config's 2 / `mid` / 2000-2014, because this `.snakemake` has no metadata for the 14:45 build so 2.06's params trigger could not fire and mtime called `summary/` current. Rule 3.01 cannot catch it: it compares config sections, not products. Fixed with `--forcerun derive_change_factors plot_gcm_timeseries` (network-free — raw slices stay valid). The recurring hazard is boarded as `t2608112051`; the two tree-inventory map gaps the rebuild exposed as `t2608112047`. Rationale in `f7d6c3e` | test fixtures |
| 2026-08-10 | t2608091028 | Land feat/signature-figure-redesign once the rendered figure has been judged [superseded] — the note was stale in BOTH directions and neither was visible from the board. First, the branch had already landed: `feat/signature-figure-redesign` no longer exists and `a04fcde` is an ancestor of `main`, so the "remaining step is the owner visual verdict, then merge" had been overtaken before ADR 0006 was written. Second, ADR 0006 then DELETED the figure that commit created — `func_plot_signature._plot_clim_year`, named explicitly in the ADR's removal list — so there is no longer a target for the verdict the note was blocked on. Its Theil-Sen helpers (`_sen_trend`, `_format_trend`) survive because `plot_signatures` uses them; that is all that is left of the redesign. The note's render recipe was dead on arrival at `main` for the same reason: steps 2 and 3 call `climate_forcing_by_subcatchment` and `plot_clim`, both retired. Surfaced by grepping the merged tree for the retired names rather than by any gate — a task note is not code, so nothing in the suite or the ruff gate can notice when an ADR invalidates one. Same class as the `preview_plots.py` break in `e0e3b6f`: a clean textual merge between a producer that was deleted on one side and a consumer that was added on the other | plotting |
| 2026-08-09 | t2608090826 | Convert the root README from reStructuredText to Markdown, rendering .rst only if a consumer needs it — **no consumer needs one**, which was the open question that made this a filed item rather than an inline edit: no Sphinx or mkdocs config, no `[project]` table or `readme` key in `pyproject.toml` (a tool-configuration host by design, not a packaging manifest), and no test/Snakefile/Dockerfile/CI reference. So it is a straight conversion with no render step. pandoc 3.10, verified structurally rather than by diff (a format conversion scores below 20% similarity, so git records add+delete and the diff proves nothing): 2921→2914 words, 17→17 code blocks with languages intact, 2→2 images, and 4 admonitions that pandoc emitted as native GitHub alerts — the idiom `dev/tasks/` already uses. Two live pointers followed it in `AGENTS.md`; the ~20 citations under `dev/milestones/`, `dev/reviews/`, `dev/decisions/` and `dev/roadmap.md` were deliberately left, per "never migrate a record's paths" — and they cite line numbers that stopped being accurate the first time anyone edited the README, so the rename lost nothing not already lost. `8bc55ce` | docs |
| 2026-08-09 | t2608071202 | Make a failing `check_model_reference` write its error into the log part it points at — closed by the SHARED fix below, which is what the note itself predicted ("likely applies to every `script:` rule that raises; check before scoping" — it does). Verified for this rule specifically rather than inferred: `ModelDriftError` is raised INSIDE the `with tee_to_log(...)` block and its whole payload rides in `str(exc)`, so `tests/test_model_reference.py` now drives a real drift through the manager and asserts the changed input, the experiment and the remedy all reach `logs/_parts/3.06_check_model_reference.log`. `10c82e4` | logging |
| 2026-08-09 | t2608071219 | Capture a failing `script:` rule's traceback in its log part — `tee_to_log` now catches, writes the formatted traceback into the log part, and re-raises; every `script:` rule in all three workflows improves at once. Three decisions worth keeping: written to `handle` rather than through `stderr_tee` (the interpreter prints its own traceback once the exception leaves the manager, so teeing would double it on the console) and therefore relativized explicitly; guarded by `_is_clean_exit`, because every WF2 cache-hit job exits via `SystemExit(0)` and treating that as failure is a regression this change could have caused — pinned by its own test; `SystemExit(1)` still logs, only the exit CODE decides. Non-vacuity demonstrated by restoring the pre-fix file: 4 of 5 new tests fail. `10c82e4` | ergonomics |
| 2026-08-09 | t2608082010 | Cover st_0 and drop basin_indicators in the fixture-facing test coverage — both halves done in `5cbb18d`, plus a THIRD site the note did not name and the worse of them: `test_workflow_climate_experiment` asserted `basin_indicators.csv` **exists** after a real run, which post-CR-2 nothing writes, so it would have failed a real integration run and never did — the case is `--run-integration`-gated. A stale expectation behind an opt-in gate is invisible exactly as a stale path behind an `exists()` guard is. The st_0 half only stopped LOOKING at data already there: the fixture carries `rlz_{1,2}_st_0` and `q_indicators.csv` carries 108 baseline rows at (0,0); both new cases pass. Deliberately left alone: `test_r09_path_map` and `semantic_tree_diff` still map `indicators/basin.csv` → `results/basin_indicators.csv`, which records what the R9 MIGRATION did and predates CR-2's removal — editing it would falsify a historical record, not fix a stale one | test coverage |
| 2026-08-09 | t2608071205 | Find out why a red ruff gate went unnoticed, and decide whether a pre-push hook is the answer — ANSWERED from the run log, and it reframed the item: the gate went red 2026-07-30 and sat unread for **eight days** (7 runs, both legs, `Lint (ruff)`), and the moment that was fixed a *second* failure was already underneath it (3 runs, ubuntu only, 4 tests asserting Windows path spelling). Last green was `43f8dfa`, 2026-07-29. A hook is therefore only half an answer — it cannot see a platform-specific failure that is green on the author's machine by construction. Landed both halves: `.githooks/pre-push` running the two ruff checks (~2 s, NOT the suite — a 6-min hook gets bypassed until the bypass is the habit), and an AGENTS.md ladder row plus section making "read the run after you push" standing. The four tests were marked windows-only per the owner's ruling, recorded in `t2608071221`. `4ba2147`, `e785ea5`, `a834cd4` | lint / CI |
| 2026-08-09 | t2608071211 | Convert `downscale_climate_forcing.py` off the bare `snakemake` global read at import — converted in `df37a0a`; the F821 per-file-ignore is DELETED, not extended, as the note required. Not mechanical: the whole body sat inside `with tee_to_log(...)` at module scope and moved behind the standard `__main__` guard, with `forcing_window` / `forcing_chunksize` / `pet_method_for` pulled out as the testable part. Generalised rather than fixed one-off: `tests/test_script_module_importability.py` now sweeps every module under `blueearth_cst/` for importability plus an AST check that none reads `snakemake` at module scope, verified non-vacuous against the pre-conversion file. Also dropped `Path(fn_in, resolve_path=True)`'s meaningless kwarg — ignored by pathlib, deprecated in 3.12, REMOVED in 3.14 | importability |
| 2026-08-09 | t2608071212 | Rule on adopting `ruff format`, then land it in one mechanical commit — ruled ADOPT (ADR 0005 accepted), and stage 1 landed: 122 files under `tests/`, `dev/` and `scripts/` reformatted, `ruff format --check` armed in CI. Two of the note's instructions were superseded: "one mechanical commit" by the ADR's two-stage split, and "re-record the baseline afterwards" — which was simply wrong, since formatting changes no artifact and the baseline compares equal. Stage 2 (the 51 `blueearth_cst/` modules, held out of the gate until a full re-run is already scheduled) continues as `t2608090907a` | formatting |
| 2026-08-09 | t2608081012 | Fix the pre-R9 fixture guard that has silently skipped the rebuild-cascade test since R9 — repointed; the case now EXECUTES and PASSES, so the cascade had not rotted. The sweep found a second instance, `test_workflow_model_creation.py`, stale three ways and ERRORING rather than skipping. Both in `5476960` | test hygiene |
| 2026-08-09 | t2608090806 | Write WF3 result CSVs in plain decimal notation, capped to 4 significant digits — the note's "needs a baseline re-record" was WRONG: the indicator target is compared with a tolerance, not hashed, and a 4-sig-digit cap sits 20x inside `INDICATOR_RTOL`. Landed in `4139217`; scope was the results writer only, never the `st_*.csv` inputs | wf3 results |
| 2026-08-09 | t2608080807 | Rename metrics_definition's returninterval functions to return_level, and fix the returninternval typo — closed by DELETING the three functions, which R11 had already left without callers; the typo half was discharged by R11's `03da1b7`. Rationale in `6f8e1f1` | wf3 metrics |
| 2026-08-08 | t2608071945 | Decide whether the tee_to_log traceback fix lands inside R11 or on its own | workflow ergonomics |
| 2026-08-07 | t2608071944 | Settle whether unit B's cst_ to st_ rename reaches the frozen experiment.yml | wf3 identification |
| 2026-08-07 | t2608071210 | Retire the 48 MB pre-R7 reference tree at `~/workspace/.r07-reference/` | housekeeping |
| 2026-08-07 | t2608071200 | Re-record the baseline manifest for the indicator-table axis-column rename | baseline |
