# Findings ledger — wf3-experiment-v2

Append-only. One row per original finding ID from the stage-2 internal panel:
`internal-review-risk.md` (19), `internal-review-architecture.md` (16),
`internal-review-repo-fit.md` (17) = **52**. Severity is **as filed** and is
never re-graded here; the three severity-divergence pairs flagged in
`internal-review-index.md` (arch-1/risk-9, arch-4/repo-fit-4,
arch-15+risk-2/repo-fit-6) each keep both rows at both severities, even where one
edit closes both.

Where a finding's `suggested_fix` had multiple parts, the Resolution cell
dispositions **each part**. Round 1 = the internal panel; no external round has
run.

## Counts

| Disposition | Blocking | Major | Minor | Total |
|---|---|---|---|---|
| accepted | 8 | 22 | 22 | **52** |
| rejected | 0 | 0 | 0 | 0 |
| deferred | 0 | 0 | 0 | 0 |
| withdrawn | 0 | 0 | 0 | 0 |

**Partial rejections inside accepted rows** (each stated in its Resolution cell,
each flagged for G2 because it declines a reviewer's stated remedy):
**risk-7** part 3 (the wall-clock ceiling), **risk-18** part 3 branch-b (dropping
`precip_variance` from `member_hash`), **risk-18** part 2's "rejected-not-ignored"
config error (adopted as a warning instead), **risk-11**'s "if the move stands"
branch, **risk-14**'s elevated-`realizations_num` branch (adopted as followup
R9-F2 inside the design body), **arch-2**'s stage-reordering branch,
**repo-fit-8**'s "state the invariant only" branch, **repo-fit-4**/**arch-4**'s
two-labels and shared-code branches, **arch-11**'s "just note the double-count"
branch, **repo-fit-13**'s retire-`estimate_batch_makespan` branch.

No blocking finding was rejected. No major finding was rejected in full.

---

## Rows

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| risk-1 | 1 | blocking | accepted | All three parts adopted, as one mechanism (§5.1). **(a)** sweep-time `baseline_digest`: adopted and generalised — every `succeeded` ledger row carries an `inputs` object (`baseline`, `st_csv`, `weagen_template`) and §5.5 gains a **fifth reuse condition** verifying them at sweep start (§5.2, §5.4, §7.3 step 4). **(b)** `seed_r` into `member_hash`: adopted (§5.1). **(c)** `weathergen_config.yml` content digest as a `params:` value of 3.01/3.05 on the `file_digest_or_absent` pattern: adopted, and additionally folded into `member_hash` as `weagen_template_digest`, so the invalidation happens at manifest time rather than only at sweep time (§2.4, §4.2, §5.1). **(d)** GF-12: adopted verbatim, covering both the `master_seed` and the template-edit paths, with C-18 as its claim. §7.6's `master_seed` row rewritten so it is now true of the stated hash. | design-v2.md |
| risk-2 | 1 | blocking | accepted | Both parts adopted. **(a)** cell-complete `allow_partial`: adopted — under `aggregate_rlz: true` a `cst_m` whose realization set is incomplete is **dropped entirely**, frames are sized from the complete set rather than `st_num`, and the empty-`csv_fns_i` branch raises a named `IncompleteCellError` instead of a pandas `ValueError` (§4.4, §9.4). `indicators/completeness.csv` names the cell with `state=partial_cell` \| `missing_cell` plus `n_present`/`n_expected`, and retained cells carry their `n`. **(b)** GF-7 restated at K=12 / `aggregate_rlz: true` with the one-of-two-realizations sub-case: adopted — GF-7 now has sub-cases (a) partial and (b) missing, both expecting **5 rows per statistic**. §9.6's Reduce class qualified "under full completeness"; C-10 stays full-grid, C-17 is the new partial-path falsifier. | design-v2.md |
| risk-3 | 1 | major | accepted | Both parts adopted verbatim. C-7 is redefined as a **paired same-input comparison**: generation frozen, same baseline NCs and WG-2 grid, `sweep_workers: 12` (cold-process reference) vs `sweep_workers: 1` (maximum available depth), tolerance-0 on `hydrology_runs/*/output/cst_*.csv` (§9.6, §12 C-7). The whole-tree diff is retained separately as **C-7s**, scoped to the stages step 2 did not change (WG-2 grid, historical store, wf1 config snapshot) and explicitly reported rather than gating. §9.6 now states all three reasons a whole-tree tolerance-0 diff cannot come back clean (`_state/` timestamps per §10b, the step-2 re-baseline, the risk-11 rename) instead of the one v1 identified. This is the C9 driver-note dependency: §9.6's parity table is written against the redefined C-7. | design-v2.md |
| risk-4 | 1 | major | accepted | All four parts adopted. **(a)** run identity/stability at `sweep_workers: 1` so one session carries all 12 members: adopted into C-7's operand table. **(b)** state that fixture depth ≈ batch depth and that A4 at production depth is open: adopted as a **scope limit inside A4 itself** (§2.2), not a footnote. **(c)** `worker_max_members` recycle bound: adopted as a config key defaulting to `0` (unbounded), specified in §6.3's lifecycle table with its cost and its rationale. **(d)** production-depth re-verification with a named trigger: adopted in §15.3, and R-10 added to §15.1 as a High, structurally-unclosable-on-the-fixture risk; H5 added to §11.3. | design-v2.md |
| risk-5 | 1 | major | accepted | The "minimum viable" part and the stronger part both adopted. Mechanism: an undeclared `_state/sweep_incomplete.json`, written with a **fresh `invocation_id`** on every accepted-but-incomplete termination and deleted on a complete one, whose digest is a `params:` value of rule 3.08 (§5, §7.2). A partial sweep therefore always re-triggers the rule on the next invocation, including after a repeated failure — which the reviewer's simpler "record the incomplete set" variant would not, since an unchanged failure set would produce an unchanged digest. **Rejected sub-option:** a distinct declared output for the partial case — it would make the declared-output set conditional and break the `--dry-run` honesty §9.4 rests on. Recovery documented in §8.1 F1; GF-13 added; C-19 added. Two second-order effects stated so they are not read as bugs (one extra no-op re-entry; the reduce re-running on each partial invocation). | design-v2.md |
| risk-6 | 1 | major | accepted | Adopted, with a **stronger mechanism than either suggested branch**. The reviewer offered "claim in 3.01, release in the reduce" or "a second lock at the same path" — both rejected, because a lock created by one job's process and released by another has no live holder pid between jobs, so the repo-fit-1 liveness test could not work and a crash between stages would leave an unreleasable lock. Adopted instead: **one** `_state/experiment.lock`, claimed in Snakemake's `onstart:` handler and released in `onsuccess:`/`onerror:`, which runs in the Snakemake **main** process — alive for the whole workflow, so its pid is a meaningful liveness token (§6.6). Probe **PR-7** added to §11.2 verifying the three properties this rests on. `_state/sweep.lock` is retired; §5.3's single-writer statement is amended by one sentence. GF-8 extended to the case that matters (second worktree, edited config, at any stage; require byte-unchanged manifest and baselines). C-14 and C-20 restated. | design-v2.md |
| risk-7 | 1 | major | accepted | **Parts 1 and 2 adopted; part 3 rejected — flagged for G2.** **(1)** stderr redirected to the member log file handle at spawn, never an undrained `PIPE`: adopted as a normative rule in §6.2, with the full mechanism (64 KB Windows buffer, Wflow verbosity, the driver's status lines moving to stderr) stated so it reads as a closed hazard rather than a style note. **(2)** GF-14 (stub worker emitting > 1 MB to stderr must not stall): adopted. **(3) Reconsidering R-4 with a data-derived wall-clock ceiling: REJECTED.** Rationale: the finding's actual defect is the *composition* of an undrained pipe with no ceiling, and part 1 removes the only known mechanism by which a healthy worker can block forever; adding a kill path to the milestone's riskiest new component is itself a risk, and a running-median ceiling still needs a warm-up window in which it does not apply, so it would not cover the early members. What remains uncovered — a genuinely wedged `Wflow.run` — is visible in the orchestrator log and recoverable via F5. R-4 is re-affirmed in §15.1 with this reasoning written into it, and §6.3's Health row states what the stderr rule does and does not bound. | design-v2.md |
| risk-8 | 1 | major | accepted | Both parts adopted. **(a)** finalization order stated normatively as a five-step sequence with `ledger_final.csv` written last (§5.2). **(b)** `invocation_id` + `config_digest` added to `sweep_completeness.csv` and to `indicators/completeness.csv`, with the reduce hard-failing when the completeness record's `invocation_id` differs from the one on every row of its declared `ledger_final.csv` (§5.2, §4.4). The unverified sub-case (post-kill blocking behaviour) is not asserted either way; the stated ordering makes it moot, since a kill before `ledger_final.csv` lands leaves the reduce blocked rather than reading a stale record. | design-v2.md |
| risk-9 | 1 | major | accepted | Same defect as arch-1, dispositioned separately at its own filed severity per the index's conflicts section. First branch of the fix adopted per the G1 gate-return ruling: **recompute everything at K=12** with `ST_START=1`, no `cst_0` members, `aggregate_rlz: true`. The second branch (pin a `run_historical: true` fixture variant) is rejected — the ruling is explicitly scope-preserving and the fixture is untouched. Sites corrected: header (a standing fixture-arithmetic block), §4.5 job counts, §6.4 scheduling note, §7.5 banner, §8.2 GF-6/GF-7/GF-9, §12 C-3, §13 step 0.4. The `cst_0`-asymmetry consequence risk-9 flagged is recorded in §4.4 and §6.4 rather than silently carried. | design-v2.md |
| risk-10 | 1 | major | accepted | Both parts adopted. **(a)** C-12 split into two stated comparands: the **binding** 3.08 sweep-stage makespan against P3-3's 228.1 s 3.10-stage makespan (verified at `dev/p33/batching-results.md:113-114`), and the **reported, not gated** full-workflow wall against 400.2 s, with the reason stated (v2 restructures generation and prepare, so `--forceall` is no longer like-for-like). P2's evidence row updated to carry both numbers. **(b)** the binding measurement moves to the **end of step 4**, where the pool and the batch rules coexist so the comparison is same-tree and the revert is one step; step 8 becomes the confirmation run. §14.1's reinstatement path updated to point at step 4. | design-v2.md |
| risk-11 | 1 | major | accepted | Adopted via the reviewer's **preferred** branch, not the "if the move stands" branch. The generator's scratch `out_dir` moves to the per-realization `_work/rlz_<n>/` (which is what removes the concurrent-write race), and our own R wrapper then **publishes** the two date CSVs back to `weather_generator/output/` as `sim_dates_rlz_<n>.csv` / `resampled_dates_rlz_<n>.csv` — honouring R07's ruling (`dev/r07/migration_project-layout.md:194-195`) with no upstream change. **Rejected sub-branch:** leaving them under `_work/` with a §10 row; it would reverse a recorded ruling and hide the resample provenance behind a scratch prefix. §10 gains two rows (the CSVs, and the PNGs moving to `plots/rlz_<n>/`); the seam doc's exclusion note is amended to the new names; `dev/scripts/semantic_tree_diff.py:357-360` and `tests/test_semantic_tree_diff.py:621-622` are named in §13 step 6 as same-commit work, and §9.6 records the rename as the third reason a whole-tree C-7 could not be clean. | design-v2.md |
| risk-12 | 1 | major | accepted | All three parts adopted. The reduce recomputes each `result_sha256` from `ledger_final.csv` before reading and hard-fails naming the diverged member (§4.4, change 2), with the reason stated: this is F8's **only** detection point, because §7.2 shows the sweep is not re-entered when its declared output is present and fresh, so §5.3's sweep-entry digest check never runs in that state. Added to C-10's observation set; F8's recovery column rewritten; **GF-15** added (mutate one member CSV after a clean sweep, require a named failure rather than a silent republish). | design-v2.md |
| risk-13 | 1 | minor | accepted | The **mechanical** branch adopted, not the "restate C-13 honestly" branch. The sweep runner re-reads the four `tools` versions at start (§7.3 step 5) and, per differing key, appends a `tools_drift` entry to `_state/sweep_summary.json` and a warning line to the orchestrator log naming recorded vs live. It does not block — recording, not enforcing, is the ruled posture. C-13's claim becomes "mechanically reported, not merely reconstructible" and its falsifier becomes **GF-18** ("inject a mismatch, observe no warning"), an observation that can actually fail. §9.2 states why the v1 wording could not: a human can always compare two strings by hand, so C-13 would have passed while R-5 stayed unmitigated. | design-v2.md |
| risk-14 | 1 | minor | accepted | The finding's **second** option adopted; the first tracked in-body. C-16 is restated as a **plausibility** check at named thresholds rather than a distributional-equivalence verdict: per-variable annual mean within ±2 SE of the pre-change mean, annual-sd ratio within [0.8, 1.25], with the n=2 power limitation written into the falsifier cell rather than left to the validator's judgment. §9.6's Generate row updated. **The elevated-`realizations_num` (20+) characterization is adopted as followup R9-F2** (§15.3), tracked inside the design body and opened in `dev/followups.md` at §13 step 6 — this is not a deferral of the finding, whose acceptance criterion ("name the threshold rather than leaving 'envelope' to judgment") is met in full. | design-v2.md |
| risk-15 | 1 | minor | accepted | Both parts adopted. §13 step 1's falsifier now asserts byte-identity **after removing `run.created_at`**, and states that exactly one field is excluded while every digest and member row is asserted byte-stable. The related reconciliation risk-15 asks for is written in the same paragraph: `created_at` changing on every 3.01 execution is precisely why §7.3's no-op path is load-bearing. **Rejected sub-option:** moving `created_at` into a sidecar — it would split the run record for one field and cost more than the stated exclusion. | design-v2.md |
| risk-16 | 1 | minor | accepted | All four parts adopted, plus a structural closure. **F4** → GF-16 (kill a *slot* process, not the worker). **F8** → GF-15. **F12** → GF-17 (shadow `julia` on `PATH`; expected observation is hard failure with no partial `ledger_final.csv`, the same discriminating observation GF-6 relies on). **The risk-1 seed path** → GF-12, which the finding correctly identifies as the most consequential realistic gap. **§14 `service:` rules** → §14.2a, disposed with the real reason (a `service:` job's lifetime is bound to its consumers, so it amortizes within a consumer group, not sweep-wide, and it leaves every member a declared-output job, reviving §14.11's blast-radius problem). Beyond the four: §8.2 gains an **F → GF coverage table** with an explicit "no gate, reason" cell for F2, so gate-set completeness is checkable at a glance instead of by cross-reading. Gate numbering was assigned once (GF-12..GF-18) because three reviews proposed colliding numbers. | design-v2.md |
| risk-17 | 1 | minor | accepted | Both parts adopted. §5.2 states normatively that **file order is authoritative for the fold and `ts` must never be used for ordering**, with the failure it prevents spelled out (a same-second `claimed`/`succeeded` pair folding to `claimed` ⇒ a verified output quarantined and re-run — a nondeterministic resume verdict in the component R-1 rates highest). §5.4's fold rule and §5.2's `ts` field row both carry the qualifier. §13 step 4's fold unit tests carry a same-`ts` pair as a named case. | design-v2.md |
| risk-18 | 1 | minor | accepted | Reviewed under the G1 deferral **and** the gate-return carve-out. **(1)** `verbose = TRUE` at the call site: adopted — the owner ruled it allowed as warning-only and value-neutral. §9.1 states it as the one carved-out change and, going beyond the finding, names **where the warning surfaces** (the perturb step runs in-slot, so it lands in `logs/_parts/3.08_run_sweep/<member>.log` and then in the merged log), without which the carve-out's stated purpose is not delivered. **(2)** annotate the inertness at the point of consumption: adopted at three surfaces — a `response_long.csv` header comment, the WG-2 seam-doc row, and a manifest-build **warning** when `variance.min != variance.max`. *Sub-part rejected:* the finding's "rejected-not-ignored validation message" — **rejecting** such a config is a scope change the deferral does not authorise, would break configs that legitimately declare a range today, and would have to be reversed on activation; a warning carries the same signal at zero contract cost. **(3)** dropping `precip_variance` from `member_hash`: **rejected in favour of the finding's own second branch** — the field stays, its presence is stated as forward compatibility, and the false invalidation is named and accepted (§5.1), because `st_params_digest` covers the same config keys anyway and carving them out would put a special case inside the freshness boundary to serve a temporary state. Removal is part of R9-F1. | design-v2.md |
| risk-19 | 1 | minor | accepted | All three parts adopted. The resume fold **deletes the transient set for every member it classifies RUN, before re-claiming** (§7.3 step 8); `prune_experiment_orphans.py` is extended to the transient classes — WG-4 perturbed NCs, WG-6 forcings, HM-6b states, `_state/members/<id>/` spec dirs (§7.4); and G3 is restated as a **per-invocation** peak with the cross-invocation cleanup named (§6.6). C-3's falsifier is widened to include a kill-resume-kill-resume cycle followed by a transient inventory, which is what makes the accumulation observable — v1's clean-run sampler could not see it. | design-v2.md |
| arch-1 | 1 | blocking | accepted | Same defect as risk-9, dispositioned separately at its own filed (higher) severity per the index's conflicts section — arch grades it blocking because GF-6/GF-7 are what make the ratified OQ-4 posture testable, and that reasoning is honoured: both gates are rewritten against K=12 with `aggregate_rlz: true` and are now executable. First branch adopted per the G1 gate-return ruling (recompute at K=12); the second branch (flip the fixture to `run_historical: true` and price it in §13 step 9) is rejected as explicitly out of scope. A standing fixture-arithmetic block was added to the document header so the K, the `ST_START`, the `ST_NUM` derivation and the "6 rows per statistic" consequence are stated once, with their file:line sources, rather than re-derived per gate. | design-v2.md |
| arch-2 | 1 | blocking | accepted | Adopted via the **second** suggested branch. `st_csv_digest` is replaced in `member_hash` by `st_params_digest`, a canonical digest of the `stress_test` config section, which rule 3.01 already holds — so every term of `member_hash` is computable when the manifest is written (§5.1). **Rejected branch:** moving `climate_stress_parameters` ahead of the manifest rule and declaring its `st_csv_fns` as an input of 3.01 — it would run the WG-2 producer **unguarded**, since the manifest is the guard sentinel, trading a computability defect for a guard-coverage defect. Per-column granularity is preserved because a stress-grid edit also changes each member's `tavg`/`prcp` scalars; the *file's* bytes are covered one layer down by `inputs.st_csv` (risk-1's mechanism). §7.6's "one `cst_<m>.csv` edited" row is rewritten to name the layer that actually catches each case — without which a fresh reviewer would re-file this finding. | design-v2.md |
| arch-3 | 1 | blocking | accepted | Both parts adopted, as part of the single C2 mechanism. `seed_r` — computable at manifest time — goes into `member_hash`; the baseline NC's content digest, which is **not** computable there, is recorded per member as `inputs.baseline` and verified at sweep start beside the three drift digests (§5.1, §5.2, §5.5, §7.1, §7.3). The finding's own observation that the fix is not free (the same property makes the "`realizations_num` increased ⇒ only the new members run" row true) is honoured: `seed_r` is derived as `sha256(f"{master_seed}:rlz:{r}")`, independent of `RLZ_NUM`, so adding realizations still does not perturb existing seeds. §7.6's `master_seed` row is now true of the stated hash; GF-12 is its gate; C-18 is its claim. | design-v2.md |
| arch-4 | 1 | blocking | accepted | Adopted via the **first** suggested branch: the orchestrator log moves *inside* the label's part directory as `_parts/3.08_run_sweep/_orchestrator.log`, so the label has exactly one shape and `merge_logs._members` reaches the directory walk. Verified in the tree: `_natural_key` (`merge_logs.py:64-71`) splits on digit runs, so `"_orchestrator"` compares as a plain string and sorts before `"rlz_…"` — it merges first and reads as the section header. **Rejected branches:** teaching `_members` to union both shapes (changes shared cross-workflow code and needs its own test, for a problem a path choice solves) and registering two labels (splits one rule's output across two merged sections). §6.5 no longer claims "no change to `merge_logs`" as a generalization from `merge_benchmarks`; it states the asymmetry and why the two gathers differ. | design-v2.md |
| arch-5 | 1 | major | accepted | Adopted verbatim: §6.6 states that `retain_member_artifacts: true` suppresses the deletions in **step 3 and step 5**, with the consequence named — step 3 holds the WG-4 perturbed NC, which is `validate_wg4`'s on-disk operand (`weather-generator-seam.md:312,383`), so v1's step-5-only wording would have left that validator permanently skipped and turned §13 step 7's "un-skip the three temp validators" into two. §10a and §13 step 7 both updated to match. | design-v2.md |
| arch-6 | 1 | major | accepted | Adopted via the **second** suggested branch. §4.3 specifies a `get_override(config, my_cfg, key, default)` helper with a stated precedence — top-level `--config` wins over `workflows.climate_experiment`, which wins over the default — pinned by a unit test in §13 step 4, and the three commands (§10a capture, GF-7, §5.3 recovery) are updated to rely on it. **Rejected branch:** editing the fixture config for these runs — it would make the documented capture procedure and the `allow_partial` gate depend on mutating a tracked file, and `sweep_quarantine` has no section home at all. Interaction with repo-fit-8 resolved explicitly: because 3.08 becomes a `shell:` rule whose orchestrator re-reads `config_path` **from disk**, the Snakefile resolves the overrides and passes the resolved values on a short fixed command line — an orchestrator reading the YAML could never see an in-memory `--config` value. | design-v2.md |
| arch-7 | 1 | major | accepted | The **first** suggested branch adopted, and strengthened. `weagen_template_digest` is recorded in `run.*`, threaded as a `params:` value of 3.01 and 3.05 on the `file_digest_or_absent` pattern, **and** folded into `member_hash` — so a template edit invalidates at manifest time rather than only re-running the config rules. **Rejected branch:** narrowing §2.4's provenance sentence to name the exception; the sentence is load-bearing for v2's claim that the manifest answers "what was this built from", and narrowing it would concede the gap rather than close it. §2.4 now states the gap v1 had and the layer that closes it. | design-v2.md |
| arch-8 | 1 | major | accepted | Both parts adopted. §10 gains a **producer/consumer rule identity** row enumerating every stale pin (WG-2's consumer 3.07, WG-5's 3.09, WG-6's 3.10, WG-4's producer 3.06/3.07, WG-4's pinned DAG-globbing surface at `:167-173`, and the bounded-substitution walkthrough at `:284-289`), and §13 step 6 is widened from three items to a full pass over both seam docs' producer / consumer / lifecycle / pinned-surface fields plus the naming-pattern clause and the walkthrough. The consequence is stated: without it R9 ships contracts naming four rules that no longer exist, in the artifact a future generator-swapper reads end to end. | design-v2.md |
| arch-9 | 1 | major | accepted | All three parts adopted. **(a)** The downscale entry-point refactor gets an owner, a **signature** (`run_downscale(cst_nc, catalog, toml_out, forcing_out, log_path, **params)`), and its own migration step **4a** with its own falsifier — pulled out of step 4 because it is an unscoped refactor of a hydromt-touching module inside the milestone's core step; R5's `prepare_weagen_config.py` extraction is named as the shape to copy. **(b)** `export_wflow_results.py` and `prepare_weagen_config.py` gain ownership rows in §2.5, as does `prepare_climate_data_catalog.py` (repo-fit-5). **(c)** A **test-migration** ownership row names `tests/test_climate_store_contract.py:339-347`, `tests/test_guard_invalidation.py:95-124`, `tests/test_check_baseline_scope.py`, `tests/test_workflow_climate_experiment.py:9,28` and the fate of `estimate_batch_makespan.py`, and each is assigned to a specific §13 step (1, 5, 6, 9). C-15 widened to both guard test files with the contract/plumbing distinction stated. | design-v2.md |
| arch-10 | 1 | minor | accepted | Both parts adopted. `RT_<var>.csv` gets an explicit disposition in §4.4 and a §10 row: undeclared, unchanged, obtained via the ledger, subject to the same cell-drop rule under `allow_partial`, covered by C-10's direct diff, and **not** in the baseline manifest. C-10's claim text is extended to name them. And §4.4 states explicitly that `WF3_TARGETS` **gains** `response_long.csv` while `RT_*` and `completeness.csv` stay out — G8 promises the entry contract, and the target set is its user-visible half. | design-v2.md |
| arch-11 | 1 | minor | accepted | The **second** suggested branch adopted: per-member benchmark TSVs are not written at all; the rule benchmark row is 3.08's sole benchmark part, and the ledger's `seconds` object is the per-member record. **Rejected branch:** merely noting the double-count in §6.5 — a known-wrong `TOTAL` in a reporting artifact is a defect whether or not it is documented, and the ledger already carries strictly finer data (four sub-steps vs one total) and is what §13 step 8 reads. `merge_benchmarks` is untouched, so the finding's verified premise (its recursive glob absorbs both shapes) stays true and simply has nothing extra to absorb. | design-v2.md |
| arch-12 | 1 | minor | accepted | All three parts adopted. **(a)** One row population fixed: every manifest member **plus** every orphan, with `state` as the discriminator (§5.2) — replacing the three different populations §5.2/§7.4/§9.4 described. **(b)** The trailing heterogeneous summary is replaced by a sidecar, `_state/sweep_summary.json`, which is what the reduce reads; `sweep_completeness.csv` becomes a parseable homogeneous CSV. **(c)** `indicators/completeness.csv` gets a §10 row and a §13 step-9 line, with an explicit decision: it is **conditional** (written only on the `allow_partial` path) and therefore does **not** join the baseline manifest, since `indicators/` is pinned by `check_baseline.py:187-189` and a conditional member would make the slice non-deterministic. | design-v2.md |
| arch-13 | 1 | minor | accepted | Both parts adopted. §5.3's crash-consistency table gains a **fifth row**: a row sequence violating §5.4's transitions (`succeeded` then `claimed` with no intervening `quarantined`, two `succeeded` rows, a terminal row with no preceding `claimed`) is corruption, handled exactly like a corrupt row. §13 step 4 carries all four illegal sequences as unit-test cases. §8.1's escalation clause is updated to point at the row, so "no legal member transition resolves" is now operative rather than presuming an evaluation that did not exist. F7's description extended to name illegal sequences. | design-v2.md |
| arch-14 | 1 | minor | accepted | All three parts adopted. **(a)** §4.2 and §10's WG-3 row now say **two added keys** (`rlz.index`, `work.path`) plus **two overrides of existing keys** (`realizations_num: 1`, per-realization `seed`), with the seam citation (`weather-generator-seam.md:141`) for why `seed` is not an addition — v1's wording would have sent `validate_wg3`'s extension after the wrong key set. **(b)** `config_digest`'s covered subset is enumerated in §5.1 (`project`, `shared`, `workflows.climate_experiment`, and nothing else), with its role clarified as provenance rather than a freshness lever — the freshness boundary is `member_hash`, which §7.6 is written against. **(c)** §4.2 states that `weather_generator/config/` **retires**, and §13 step 6 removes it from the R07 layout map. | design-v2.md |
| arch-15 | 1 | blocking | accepted | Same mechanism as risk-2 and repo-fit-6; dispositioned separately at its own filed severity. The finding's first option is adopted — **drop the row entirely**, with the reduce sizing its frames from the completeness/succeeded set rather than from `st_num` — and its second option (emit the row with a recorded realization count) is rejected: a populated row with an `n` column elsewhere is exactly the shape a consumer cannot detect without a join, which is the failure §9.4 exists to prevent. Per-cell `n` is nonetheless recorded in `indicators/completeness.csv` for the retained cells, so the diagnostic value of the second option is kept without the hazard. Both consequences the finding names are honoured explicitly: §4.4 states the reduce body is no longer "unchanged" (four specified plumbing changes with an owner), and §9.6's Reduce parity class is qualified **"Identical under full completeness"**. | design-v2.md |
| arch-16 | 1 | minor | accepted | Adopted verbatim as §4.4's plumbing change 1: the reduce resolves `ledger_final.csv`'s `result` column against `exp_dir` before calling `analyze_wflow_results`. The finding's precise diagnosis is preserved in the text — `endswith` matching (`:162`) and `realization_from_run_csv` (`:24`) survive relativization, the `open` does not — so an implementer does not "fix" the wrong call site. | design-v2.md |
| repo-fit-1 | 1 | blocking | accepted | Both fix directions adopted, and they compose with risk-6's `onstart` relocation. **(b)** liveness is recorded `pid` **plus** `pid_create_time` (and `invocation_id`), and a holder that is provably gone — dead pid, or live pid with a different creation time — is **auto-cleared** with a log line; the finding's own observation that pid alone is weak on Windows is why the creation-time tiebreak is unconditional. This is not "inferring liveness from a timeout", which `cst-run-control` refuses; it is a positive observation. **(a)** the lock-clearing command is **named** for the first time — `python scripts/sweep_status.py <exp_dir> --clear-lock` — and appears in §6.3, F11, and GF-2's command sequence. §6.6 carries a single three-line recovery block covering Snakemake's workdir lock (`--unlock`, which needs repo-fit-2's `_cores` fix to work without `-c`), the experiment lock, and the resume. The compounding with repo-fit-14 is honoured: GF-2's injection is corrected to a process-tree kill, which is what makes the stale-lock state reachable in that gate. | design-v2.md |
| repo-fit-2 | 1 | major | accepted | Adopted verbatim, on probe-verified evidence the reviewer supplied (Snakemake 9.6.2, `workflow.cores` raises at parse time for both `--unlock` and `--dry-run` without `-c`). Rule 3.08 uses the Snakefile's **existing guarded `_cores` binding** (`Snakefile_climate_experiment:486-489`), verified in the tree, and §4.3/§6.4 state that the sweep's parallelism **degrades to 1** rather than raising. The consequence chain is written into §4.3 rather than left implicit: `AGENTS.md` documents `--unlock` with no `-c`, and that is exactly the command F5/GF-2 depend on after a crash. | design-v2.md |
| repo-fit-3 | 1 | major | accepted | Adopted via the **first** fix direction: the `staticmaps = ancient(...)` input is dropped from rule 3.08. The freshness signal is already carried by `staticmaps_digest` as a `params:` value on the `file_digest_or_absent` pattern (§5.1), so the DAG edge added nothing while making `MissingInputException` — and two red CI legs — certain on a bare checkout, given `tests/test_cli.py:64-70`'s fixture and `:214-230`'s `returncode == 0` assertion. **Rejected branch:** keeping the edge and extending the fixture in §13 — it would grow the test fixture to serve an edge with no freshness role. | design-v2.md |
| repo-fit-4 | 1 | major | accepted | Same mechanism as arch-4; dispositioned separately at its own filed severity per the index's conflicts section. Cheapest fix direction adopted: the orchestrator log moves into the part-dir as `_orchestrator.log` so the label has one shape. Both named alternatives rejected with the reasons stated in the arch-4 row. The finding's `_remove_parts` consequence — dropped paths never enter `merged`, so `_parts/3.08_run_sweep/` grows across runs and the "one file in `logs/` after a clean run" invariant breaks — is written into §6.5 as the justification, verified against `merge_logs.py:106-108,139-141`. | design-v2.md |
| repo-fit-5 | 1 | major | accepted | Both parts adopted. The `downscale_climate_forcing.py` extraction becomes an **explicit, separately-verifiable sub-step 4a** with a stated signature, a named owner (§2.5) and its own falsifier (tolerance-0 on the WG-6 forcing and HM-4 TOML against the step-0 capture), following R5's `prepare_weagen_config.py` precedent. `prepare_climate_data_catalog.py` is named in §2.5 and in §13 step 5, and §10's WG-5 row is corrected from "only the producer's `input:` set changes" to state that the entry-list derivation at `:132-133` is a **script** change. §9.6's Downscale row now reads "Identical, once extracted" and states that the parity leg has nothing to run against until 4a lands. | design-v2.md |
| repo-fit-6 | 1 | major | accepted | Same mechanism as arch-15 and risk-2; dispositioned separately at its own filed severity. Both parts of the fix direction adopted: the reduce sizes frames from the ledger's succeeded set and skips empty groups (§4.4), and `allow_partial` gets its **own** falsifier, **C-17**, while C-10 stays a full-grid byte-identity check. The finding's zero-row sub-case (pre-allocated `np.zeros` yielding `0.0` indicators for an unrun cell) is named explicitly in §4.4 alongside the under-averaged and `ValueError` shapes, since all three are reachable and the fix must exclude all three. | design-v2.md |
| repo-fit-7 | 1 | major | accepted | Both parts adopted. `tests/test_guard_invalidation.py` is named in §13 step 1 (with `tests/test_climate_store_contract.py:339-347`, which pins the guard rule's `output:` shape and which the finding did not list but the same fusion invalidates), and `tests/test_check_baseline_scope.py` is named in §13 step 9 with the exact numbers (3 → 4, 14 → 15). C-15 is widened to include `test_guard_invalidation.py` **with the contract/plumbing distinction stated**: its verdict-and-message assertions are contract and must hold unchanged; its `.project_consistency_ok` target-path assertions are plumbing and are expected to change. §4.1 carries the same distinction at the point the sentinel retires. | design-v2.md |
| repo-fit-8 | 1 | major | accepted | The **`shell:` fix direction** adopted, and the invariant stated as well — both parts, not one. Rule 3.08 becomes a `shell:` rule through `run_logged.py` in the shape `Snakefile_climate_experiment:546` already uses for a long-lived Julia child, which sidesteps `__main__` entirely and gives the orchestrator log the repo's standard header/relativization/UTF-8/exit-code handling (`:21-25`). Additionally normative: `run_sweep.py` has no module-level side effects and a guarded entry, and the **slot body lives in a separate importable module** `sweep_slot.py`, so the hydromt import is paid once per slot deliberately rather than once per orchestrator re-execution accidentally. The finding's second payoff is recorded: import-cheapness lets §13 step 4's Julia-free unit tests avoid `sys.modules.setdefault` stubs and the collection-order pollution in `dev/followups.md` § R3+. | design-v2.md |
| repo-fit-9 | 1 | minor | accepted | Adopted verbatim: `sweep_status.py` moves to `scripts/`, and §7.5 states the reason against `AGENTS.md`'s O-23 invocation-model split rather than asserting the move. §7.4 additionally states why `prune_experiment_orphans.py` correctly stays in `dev/scripts/` (it mirrors `prune_series_cache.py`), so the two placements read as one applied rule rather than two judgements. §2.5's ownership row now names both homes. | design-v2.md |
| repo-fit-10 | 1 | minor | accepted | Adopted verbatim: `workflows.climate_experiment.master_seed` on the `get_config` optional contract, defaulting to the template's `generateWeatherSeries.seed`, recorded in the manifest with its provenance (`master_seed_source: "config" \| "template_default"`) so the record says where the effective value came from. §4.2 states the defect being fixed: v1's lever required editing a **tracked repo template** shared by every project on the checkout, so two projects could not differ and re-seeding a stress test would dirty the toolbox source. | design-v2.md |
| repo-fit-11 | 1 | minor | accepted | Both parts adopted. §4.5 specifies a parse-time `_reject_retired_keys(my_cfg, {...})` helper mirroring `_positive_batch_key`'s shape (verified in the tree at `Snakefile_climate_experiment:501-508`), pinned by a unit test in §13 step 5, and states the finding's own consequence as a **stated choice**: a parse-time raise also blocks `--unlock` and `--dry-run` until the config is edited. The rationale for accepting that (a config carrying a retired sweep-tuning key reflects a wrong mental model, and the edit is one line, and the message names it) is written down so the choice is reviewable. | design-v2.md |
| repo-fit-12 | 1 | minor | accepted | The **first** fix direction adopted: C-11 compares parsed values at the declared rounding, not bytes. §4.4 states the premise (`dtype="str"` frames filled from `np.concatenate([..., df.values.round(2)])`, `export_wflow_results.py:103-119,205-239`) and the consequence (a byte check would fire as a false regression on trailing zeros and exponent forms). **Rejected branch:** declaring the string formatting itself part of the HM-7 contract — it would pin numpy's repr behaviour as a seam property, which is a wider commitment than the milestone needs. | design-v2.md |
| repo-fit-13 | 1 | minor | accepted | All five items adopted into §13 step 6, which becomes an inventory table. **§9.1's recommendation** — the item the finding rates most important — is not merely corrected but **rewritten in place**, so no reading of the accepted body can take "activate" as authority; the stale OQ-1 sites the finding did not list (§2.6, §4.4, §5.1, §10's WG-2 row, §11.1's P6/P9 rows, §15.2) were cleared in the same pass. `dev/conventions/naming.md:77-84`, `dev/scripts/semantic_tree_diff.py:201`+`:381`, and `tests/test_workflow_climate_experiment.py:9,28` are all named with their line references. **`estimate_batch_makespan.py` is dispositioned as RETAIN, not retire** — the finding left the fork open; §6.4 cites the estimator as the argument for why a static partition strands stragglers and §14.1 needs it if the floor check reinstates batching, so it keeps its test and gains a docstring line stating it describes a retired lever and is kept as P3-3 evidence. | design-v2.md |
| repo-fit-14 | 1 | minor | accepted | Both parts adopted. **(1)** every whole-file write gets a bounded retry (5 attempts, 200 ms apart) with a named `AtomicReplaceError` on exhaustion, and readers are specified to open/read/close rather than hold a handle — §5.3 states the asymmetry explicitly (POSIX has no equivalent failure, so the hazard lands on the dev platform, in exactly the situation `sweep_status.py` exists for). **(2)** GF-2's injection becomes a process-tree kill (`taskkill /T /F /PID`), with the reason stated: the orchestrator is a child process and Windows has no process groups, so `Stop-Process -Id <snakemake pid>` would leave it, its slots and its workers running and the surviving `finally` would tidy up — the gate would not produce the state it is written to produce. The compounding with repo-fit-1 is stated in both rows. | design-v2.md |
| repo-fit-15 | 1 | minor | accepted | The **first** fix direction adopted: a chunked digest helper (`file_digest_or_absent(path, chunk=1 << 20)`, same `ABSENT` sentinel, same return type) is used for `staticmaps_digest`, so parse-time invalidation semantics are unchanged and only the memory profile differs. §5.1 states the finding's diagnosis — `f.read()` at Snakefile parse time on **every** invocation including `--dry-run` and `--unlock`, invisible on the 152 KB fixture and scale-dependent on a real basin. **Rejected branch:** keying on `wflow_sbm.toml` + size/mtime for the grid — it would weaken content-addressing to metadata for the one input whose silent change (a rebuilt model under an unchanged config) §5.1 is specifically defending against. | design-v2.md |
| repo-fit-16 | 1 | minor | accepted | Adopted, in §3.2 rather than §4.4 so it sits beside the undeclared-**writes** justification it complements. The paragraph states the constraint the finding identified — no rule produces the member CSVs any more, so declaring them raises `MissingInputException`; declaration is *unavailable*, not merely unattractive — cites R07 B6's rationale still in the tree at `Snakefile_climate_experiment:553-557`, and names what replaces it: `ledger_final.csv` is declared and enumerates the same paths **with digests**, and §4.4 verifies each before reading, so the undeclared read is a *verified* read where the declared one was not. `st_csv_fns` stays declared. | design-v2.md |
| repo-fit-17 | 1 | minor | accepted | Adopted verbatim and extended to the second surface the finding names. §6.2 pins `encoding="utf-8", errors="replace"` on the worker's stdin/stdout/stderr **and** on the per-member `Rscript` capture, with the failure written out: a non-cp1252 character in a Wflow error string would raise `UnicodeDecodeError` in the orchestrator's reader, so the decode fails on the very message the failure path exists to carry and the member is recorded `class=worker` with a decode traceback instead of the real error. The finding's CRLF observation is carried through as stated (benign; `json.loads` tolerates a trailing `\r`). | design-v2.md |

## Round ext-1 (external review r1, stage 6 revision r2)

Six findings from `external-review-r1.md` against design-v2.md; five fault v2
resolutions of internal-panel findings (ext1-2→risk-7, ext1-3→arch-13,
ext1-4→risk-12, ext1-5→risk-3, ext1-6→risk-10). Counts: 1 blocking + 5 major,
all **accepted**; 0 rejected; within ext1-4 the author chose the reviewer's
*second* offered branch (narrow the claim + explicit command) over the first
(live digests in reduce freshness), with the first priced and recorded in
§14.14 — a selection between offered branches, not a partial rejection. The
risk-7 part-3 rejection (wall-clock ceiling) is re-examined in the ext1-2 row
and **stands**; it remains flagged for G2 as before.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| ext1-1 | ext-1 | blocking | accepted | Adopted in full, as one mechanism (§6.1 "Parent-owned assignment"). The shared pull queue is **removed**; the parent assigns members to slots over per-slot duplex `multiprocessing.Pipe`s (`ready`/`assign`/`ack`/`result`/`stop`), chosen over `mp.Queue` because `Connection.send` has no feeder thread, so a fully-sent message survives its sender's death. **Who assigns whom:** only the parent assigns, only to a `ready` slot with no open assignment. **Claim row vs dispatch:** the `claimed` ledger row is appended and fsync'd, and the in-memory assignment table (`slot_id → (member_id, attempt)`) updated, **before** `assign` is sent — claim-before-dispatch is normative. **What the parent tracks:** the assignment table, indexed by the durable claimed rows. **Dead-slot identification and requeue:** the parent's event loop blocks on `connection.wait(pipes + process sentinels)`; on a sentinel it drains the pipe (a fully-sent `result` still folds normally), then fails/requeues exactly the member named by the open table entry, and respawns the slot. **Crash windows:** W1–W4 enumerated in §6.1, with the reviewed dequeue-and-die window (W4) structurally eliminated. **Gate:** GF-19 kills a slot between `assign` and `ack` via a named test hook; `ack` is specified as diagnostic-only so GF-19 proves recovery does not depend on it. GF-16 restated to require the parent name the member from its recorded assignment; F4 rewritten; C-21 added; §6.4 scheduling restated (parent-pushed greedy, same no-static-partition property); §5.3's single-writer transport corrected; §13 step 4 gains the protocol unit tests. | design-v3.md |
| ext1-2 | ext-1 | major | accepted | Adopted via the reviewer's **first** offered mechanism (per-request log path with Julia-side redirection), plus a destination for between-member output the finding required stated (§6.2). The `run` message carries the member's `log` path; the worker opens it append-mode and redirects **stdout and stderr at the fd level** around that member's `Wflow.run` (`redirect_stdout(logio) do redirect_stderr(logio) do … end end`), restoring both before writing the `result` line — so even native-code output mid-run lands in the right member's log, and protocol stdout can carry only JSON (C-6/H1 restated to this mechanism). **Between members:** spawn-time stderr is the per-worker lifecycle log `_worker_<id>.log` (handshake, Julia warnings, out-of-member crash backtraces), merged by `merge_logs`' existing directory walk with no shared-code change (`_natural_key` orders it as front matter). **Member-log write discipline:** sequential single-writer handoff (slot closes before `run`; worker closes before `result`; slot appends verify) — no Windows sharing conflict. **Gate:** GF-20 (one worker, all 12 members at `sweep_workers=1 worker_max_members=0`; strict per-member log separation), C-22 added; GF-14 retained with its landing spot restated; §6.5's log table rewritten. The rejected alternative (continuously drained reader routing by in-flight request) adds a thread per slot to route what the worker can place directly. **Risk-7 part-3 rejection (data-derived wall-clock ceiling): STANDS under the new mechanism.** Parts 1–2's *routing* changed, but the invariant the rejection rested on is preserved and strengthened: at every point in the worker's lifetime each of its streams is a file handle or a one-line flushed JSON pipe read by a blocking `recv` — no stream is ever an undrained `PIPE`, so the only known mechanism by which a *healthy* worker could block forever remains removed, and the residual (a genuinely wedged `Wflow.run`) is unchanged R-4, still flagged for G2. | design-v3.md |
| ext1-3 | ext-1 | major | accepted | Adopted in full, per the suggestion (§5.3, §5.2, §5.4). Legality is **epoch-scoped**: evaluated per `(member_id, member_hash)` sub-sequence in file order over the whole ledger; each `quarantined` row closes an epoch, carries the `member_hash` of the rows it supersedes (on hash-change quarantines `detail` names both hashes; the full `detail` cause enumeration is now in §5.2), is legal anywhere, may repeat, and is exempt from claimed-precedence. Legal epoch shape: `claimed→failed` pairs, then at most one `claimed→succeeded`, then nothing until the epoch closes; a dangling `claimed` is the interrupted state. **Repeated success is illegal only without an intervening `quarantined`** — the v2 flat "two succeeded rows" rule condemned the design's own GF-12/F8 repair histories, and §5.3 now says so. Illegal set (4 shapes, incl. the new double-claim shape) and **both named valid histories** — same-hash digest repair and changed-hash invalidation — are §13 step 4 unit-test cases: the illegal set must raise, the valid histories must fold cleanly. | design-v3.md |
| ext1-4 | ext-1 | major | accepted | Adopted via the reviewer's **second** offered branch: the claim is narrowed to what actually executes, and an explicit integrity-verification command is added; the first branch (live result digests in the reduce's parse-time freshness) is priced and rejected in **§14.14** (K content digests at parse time on every invocation incl. `--dry-run`/`--unlock`, reintroducing K-scaling against C-2, for detection no earlier than `--verify` gives free). §4.4 change 2 rewritten: the reduce-time digest check fires **whenever the reduce executes** (partial retries, manifest-driven re-entries, `--forcerun`, rebuilt `ledger_final.csv`); after a clean sweep an ordinary re-invocation runs neither the sweep nor the reduce and detects nothing — stated as the design's own §7.2 consequence rather than wished away. `scripts/sweep_status.py --verify` specified (§7.5): recompute every recorded digest, exit nonzero naming diverged members, print the repair lever (`delete _state/ledger_final.csv and re-invoke` → reuse condition 4 fails → exactly the diverged members quarantine and re-run). F8 rewritten with the three real detection points; **GF-15 rewritten as a four-observation sequence** — (i) plain `SM` = `Nothing to be done` as the *expected* observation, (ii) `--verify` nonzero naming the member, (iii) `--forcerun export_wflow_results` hard-fails (with the PR-4 output-removal consequence stated as fail-loud), (iv) the named repair re-runs exactly the diverged member back to green — replacing a gate whose promised observation the workflow could never produce. C-10 restated to match. | design-v3.md |
| ext1-5 | ext-1 | major | accepted | Adopted per the suggested design (§9.6, §12 C-7). Both legs run at `sweep_workers: 1`; **session depth is the only variable**, set by `worker_max_members` — `1` (recycle after every member: a fresh Julia session per member through the same slot code path, the cold-process reference) vs `0` (one session, all 12 members). **Forcing is by state absence, never by hash change:** the named reset between legs (`Remove-Item <exp_dir>/_state, <exp_dir>/hydrology_runs -Recurse -Force`, no workflow running) removes the ledger and member artifacts while generation and catalog stay, so the fold classifies all 12 RUN in each leg while every `member_hash` is identical across legs — v2's operands could not execute at all (execution-policy keys are excluded from `member_hash` by design, so its two runs would have compared the same pre-existing CSVs), and if forced at `p=12` vs `p=1` would have confounded depth with concurrency. **Both captures taken before comparison** (legA/legB holding dirs), then `semantic_tree_diff --tolerance 0`. GF-11's skip semantics are explicitly intact — the §7.6 execution-policy row is untouched and GF-11 unchanged. Enabling change: `sweep_workers`/`worker_max_members` join the `--config` override set resolved by `get_override` (§4.3, §4.5). §2.2's A4 scope limit, §6.4's budget note, §11.1 P3 and H5 restated to the new legs. | design-v3.md |
| ext1-6 | ext-1 | major | accepted | Adopted per the suggestion's first branch (same scientific boundary), realized as a **same-tree paired measurement** at the end of step 4 (§13 step 8, C-12, G7). Boundary: from a tree with prepare, generation (persistent baselines), WG-2 grid and catalog complete, to all 12 member CSVs present. Batch leg: delete member-scoped artifacts, `SM <the 12 member CSV paths>` at `-c 3 --threads 4` → exactly 3.05_st+3.07+3.09+3.10. Pool leg: same deletion plus `_state/`, `SM <ledger_final path>` at `sweep_workers=3, wflow_threads=4` (3.01 rides along, ms-scale, reported). **Gate: pool span ≤ batch span**, both walls measured under the P3-3 measurement-conditions contract with `p × M` held at 12; parse overhead present in both legs equally. **Component timings reported separately** (ledger `seconds.{perturb,downscale,simulate}` vs per-rule benchmark rows), with `seconds.simulate` still the direct `S_warm` measurement. The 228.1 s simulate-only makespan and the 400.2 s full wall are demoted to reported context — the former charges the fused rule ~156 s of work absent from its comparand (`dev/p33/batching-results.md:113-114`), the latter spans stages v2 restructures. G7 and §11.1 P2 restated; §14.1's reinstatement trigger now reads on the boundary span. The step-4 placement (measure while the batch rules coexist, revert = one step) is retained from the risk-10 resolution, which this replaces only in its comparand. | design-v3.md |

## Round ext-2 (round-cap arbitration, stage 6a revision r3)

Seven findings from `external-review-r2.md` against design-v3.md; four fault
compositions of r2 fixes (ext2-2 → the risk-8/risk-6 identity check and lock,
ext2-3 → the allow_partial retry artifact, ext2-4 → the repo-fit-1/GF-3
quarantine lever, ext2-5 → the ext1-3 epoch legality × the risk-19/GF-9 orphan
pruning). Counts: 2 blocking + 5 major. The external round cap (2) was reached
at convergence-r2, so no reviewer verdict closes this round: the **owner
arbitrated all seven on 2026-08-01 as ACCEPTED, FIX REQUIRED** (arbitration
record: `status.md` stage log, `arbitration` entry — it carries reviewer
authority; the cap stands and no further external rounds run). Disposition
authority for every row below: **owner arbitration 2026-08-01**.

| ID | Round | Severity | Disposition | Resolution or rationale | Doc version |
|---|---|---|---|---|---|
| ext2-1 | ext-2 | blocking | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated: the fused lifecycle gains a **baseline branch**, specified normatively in §6.6 — for manifest members with `baseline: true` / `st_csv: null` (`cst_0`, admitted only under `run_historical: true`), steps 1–2 (spec write, perturb) are **skipped entirely**; step 3 downscales the **persistent** baseline NC (`weather_generator/output/rlz_<n>_cst_0.nc`, the manifest's `realizations[].baseline_nc`) directly, mirroring the retired rules' own split (3.07's `st_num >= 1` wildcard constraint, `Snakefile_climate_experiment:403-404`; 3.09's direct `cst_0` pass-through). The ledger records `inputs.st_csv = null` and `inputs.weagen_template = null` (§5.2), both *defined-absent* in reuse condition 5 (§5.5), and `seconds` omits `perturb`. **The baseline NC is excluded from every deletion surface checked per the ruling:** the member steps (§6.6 — a baseline member's transient set is exactly `{WG-6 forcing, HM-6b state}`), the resume fold's RUN-set cleanup (§7.3 step 8), the quarantine inventory (§5.3 step 4 — baseline NCs never move), and `retain_member_artifacts` (§6.6 — step 3 has nothing to suppress). §5.1 gains the baseline field note; §4.4's `cst_0`-asymmetry note is reconciled (the reduce consumes baseline CSVs and emits no `cst_0` row under `aggregate_rlz: true`, observed by the gate). **Gate, per the ruling's without-changing-the-tracked-fixture requirement — the config-variant option chosen:** **GF-21**, an untracked scratch copy of the fixture config (`dev/tmp/gf21_config.yml`) with exactly `run_historical: true` + a distinct `experiment_name`, K = 14 incl. two baseline members, named and executable in §8.2; claim **C-23** (§12). | design-v4.md |
| ext2-2 | ext-2 | blocking | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated, both halves. **(1) The workflow-lock's `onstart`-minted `invocation_id` is authoritative and the handoff is concrete** (§6.6, §5.2, §7.3 step 1): `onstart` mints the uuid4 once into the lock's JSON; `run_sweep`'s first action reads `_state/experiment.lock` and **adopts** that id for every ledger row, quarantine generation and finalization artifact — the runner never mints its own. Absent/unparseable lock at that read ⇒ named `ExperimentLockMissingError`, never a fallback mint (the ruling's "state what happens if absent"). **(2) `ledger_final.csv` distinguishes per-member `succeeded_invocation_id` (the invocation whose `succeeded` row the fold selected — naturally mixed after a resume or retry, and that mixture is provenance, not error) from the uniform `finalization_invocation_id`** (§5.2); the reduce's completeness identity check validates against the **latter only**, so resumed sweeps with mixed-invocation successes pass by construction. §10b's field enumeration updated. **GF-2 and GF-13 are extended through successful reduction** (§8.2), each asserting mixed `succeeded_invocation_id` values under a uniform `finalization_invocation_id` and a published `Qstats.csv`; claim **C-24** (§12). | design-v4.md |
| ext2-3 | ext-2 | major | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated: a **full-completeness reduction deletes any prior `indicators/completeness.csv`** (`Path.unlink(missing_ok=True)`, a single atomic remove) immediately before publishing its surfaces — §4.4's `allow_partial` specification gains the "Full-completeness cleanup" row, so the designed partial→retry→complete sequence (§7.2) can no longer leave a stale record from an earlier invocation beside complete surfaces or contaminate tree comparisons that claim the file absent on clean runs. Propagated to §9.4's Reduce row and §10's HM-7+ `completeness.csv` disposition (removal noted, gated by GF-13). **GF-13 extended** (§8.2): its final observation asserts `indicators/completeness.csv` is **absent** after the successful retry's full-completeness reduction; **C-17 extended** in the same edit — its falsifier now includes a surviving stale `completeness.csv` (§12). | design-v4.md |
| ext2-4 | ext-2 | major | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated: §5.3's corrupt-ledger recovery is rewritten from "everything else under `_state/` moves" — not implementable: self-nested quarantine move, removed `members.json` sentinel, persistent member outputs left for the forced rerun to overwrite — to an **explicit five-step inventory**: (1) create `_state/quarantine/<invocation_id>/` (the adopted id); (2) **preserve in place**: `experiment.lock` (held by the running workflow), `members.json` (the declared manifest sentinel), and the quarantine root itself; (3) **move** the active ledger/finalization state (`ledger.jsonl`, `ledger_final.csv`, `sweep_completeness.csv`, `sweep_summary.json`, `sweep_incomplete.json`, `_state/members/`); (4) **move every current manifest-owned member output** under `member_products/<member_id>/` with relative paths preserved (HM-4 TOMLs, HM-5 CSVs, surviving transients) — the fresh ledger implies a full rerun that would otherwise overwrite the only surviving copies; baseline NCs are generation's products and never move (composes with ext2-1); (5) start the fresh ledger last. **Gate, per the ruling: GF-22 executes the recovery command to completion** (§8.2 — inventory verified, sentinel and lock untouched in place, full rerun and reduce finish, exit 0); claim **C-25** (§12). | design-v4.md |
| ext2-5 | ext-2 | major | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated via the **decoupling branch** (the ruling's first option; no tombstones — one mechanism, no write amplification on a delete path): orphan-ledger-history legality is **independent of live artifact existence**. §5.3's crash-consistency table replaces the "not in the manifest and not a known orphan ⇒ corruption" row with two rows — schema violation ⇒ corruption; **schema-valid rows for members absent from the current manifest ⇒ legal history** (superseded generations), excluded from current-state verdicts, still epoch-checked within their own `(member_id, member_hash)` sub-sequences — and the out-of-manifest clause is removed from the illegal set with the decoupling stated ("history in an append-only record must not become corruption because a documented maintenance action ran"). §7.4 gains the "Pruning never rewrites history" bullet: `--delete` removes artifacts only; "orphan" there is a disk-reporting category, not a legality predicate. §8.1's row reference updated. **Gate, per the ruling: GF-23**, the full resize→prune→reinvoke composition (§8.2 — post-`--delete` invocation folds cleanly, rlz_1 members SKIP, zero orphan rows, exit 0); claim **C-26** (§12). | design-v4.md |
| ext2-6 | ext-2 | major | accepted (owner arbitration 2026-08-01) | Adopted as arbitrated, with both surfaces under one stated semantics (the ruling names the pre-existing unchecked `file.rename` at `generate_weather.R:68-73` as in-scope). §4.2 specifies **checked replacement** via one helper, `publish_file(src, dest)`: checked `file.remove` of an existing destination, then checked `file.rename`, each `FALSE` ⇒ a **named loud `stop()`** (`"[generate_weather] publish failed: …"`, the R-side analogue of §5.3's `AtomicReplaceError`) — the ruling's "checked rename with a named error" branch, chosen because base R has no `os.replace` equivalent and §2.3's zero-new-dependency rule forecloses one; the crash window between the two steps is closed by Snakemake's failed-job semantics (nonzero Rscript exit ⇒ declared NC deleted ⇒ full regeneration, PR-4). Applied to **both** the two per-realization date CSVs and the four diagnostic PNGs (the `:68-73` loop rewritten onto the helper; a missing *source* figure stays legitimate per its per-figure `tryCatch`). §10's two WG-3+ rows and §2.5's r-developer brief updated. **Gate, per the ruling: GF-24** — second generation with changed seeds on Windows replaces every sidecar consistently, plus a held-open-destination sub-case proving the loud-failure half (§8.2, runnable from §13 step 2); claim **C-27** (§12). | design-v4.md |
| ext2-7 | ext-2 | major | accepted (owner arbitration 2026-08-01) | Adopted exactly as the arbitration restated it: the floor protocol becomes **≥ 3 predeclared counterbalanced AB/BA pairs on the fixture** (batch-first and pool-first strictly alternating, each leg preceded by its own deletion reset; the pair count declared before the first pair runs), **all spans and per-leg dispersion (min/median/max) reported**, and the **gate on the median pool-vs-batch comparison at the same scientific boundary v3 defined** (prepare/generation/catalog-complete → all 12 member CSVs, both legs in the step-4 tree, `p × M` held at 12). C-12 restated (§12: binding = median ≤ median over the pairs; single-pair comparison named inadmissible — order hands the second leg OS-cache warming, noise can reverse a strict single observation); G7 restated (§1.2); §13 step 8 rewritten with the repetition/counterbalancing bullets and the near-tie-reported-not-silently-passed rule; §13 step 4 notes the full protocol runs at the binding measurement point; §14.1's reinstatement trigger reads on the median. **Component timings stay reported** (ledger `seconds.{perturb,downscale,simulate}` vs per-rule benchmark rows), with P3-3's 228.1 s / 400.2 s as non-gating context. | design-v4.md |
