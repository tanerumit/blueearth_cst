# P5 scientific continuity — matched explicit seed

Status: **scientific continuity accepted for the matched explicit-seed WF3 fixtures**. This report concerns generated climate series only. It does not accept the §10.6 excluded-field matrix, WF4 simulation, hydrological outputs, or P7 integration.

## Reference and successor

- Pre-change reference: isolated explicit-seed-123 P0 project at `.tmp/scratchpad/2026-09-21_1541/prechange-wf3-reference/explicit-123-c-root/project`, recorded in [prechange-reference-retry.md](prechange-reference-retry.md). Its v1 collection ID is `65651384a9fc32d957fffed48c8af6d652e589db72a6f89f5386e7f4e55fd756`.
- Successor: fresh post-DAG-fix fixture E at `.tmp/scratchpad/2026-09-21_1541/p5-successor-explicit-123-e/project`. Its v2 collection prefix is `0b17af464140`. P5 worker confirmed fresh production and unchanged 37 creator files across plain and forced reuse checks; the earlier fixture D was tainted and excluded from this verdict.
- Independent comparator: `.tmp/scratchpad/2026-09-21_1541/p5_scientific_compare.py`; completed output: `.tmp/scratchpad/2026-09-21_1541/p5-scientific-comparison-e.log`. Command: `.pixi/envs/default/python.exe .tmp/scratchpad/2026-09-21_1541/p5_scientific_compare.py <P0-project> <E-project>`. Exit code 0. The test posture is rapid / numerical affected: deterministic reference with explicit seed, plus discrimination checks.

## Findings

All ten retained `run_01.nc`–`run_10.nc` files are byte identical by SHA-256, in addition to exact decoded comparison. Each of the seven scientific variables (`kin`, `kout`, `precip`, `press_msl`, `temp`, `temp_max`, `temp_min`) contains 3,358,000 compared values across the ten members: **zero mismatches, maximum absolute difference 0, maximum relative difference 0**. Dimensions, time and spatial coordinates, variable and global attributes, calendar encodings, data types, and missing-value masks match. Both `sim_dates.csv` and `resampled_dates.csv` have 16,790 rows and are byte identical.

The ordered ten run IDs, realization IDs, perturbation IDs, member type and evaluation flags match after mapping the v1 and v2 column names. Each perturbed member points to the root member of its own realization. All 48 monthly perturbation rows match exactly. Descriptors in both collection markers match, and every marker FileRef digest agrees with the bytes it names. A deliberate +1 change to a precipitation value and a swapped run-01/run-02 member both make the comparison fail. Thus the check discriminates value and member-association errors.

The P0 rapid fixture's four perturbations are 0°C/−30%, 0°C/+30%, +3°C/−30%, and +3°C/+30%. **It has no zero-change perturbed member.** The root members are unperturbed, but cannot stand in for the requested zero-change perturbation path.

An additional isolated predecessor project at `.tmp/scratchpad/2026-09-21_1541/prechange-zero-change-reference/project` was run from the immutable pre-migration revision with explicit seed 123 and a grid including `st_id=2` at 0°C/0% precipitation/0% variance for every month. Its successor is fresh v2 fixture F at `.tmp/scratchpad/2026-09-21_1541/p5-successor-zero-change-f/project`. The same independent comparator exited 0; its complete record is `.tmp/scratchpad/2026-09-21_1541/p5-scientific-comparison-zero-f.log`. The zero-change perturbed members are `run_03` and `run_10`; each maps to the corresponding root realization (`run_01` and `run_08`). All 14 member mappings and 72 monthly perturbation rows match across revisions. For each of the seven scientific variables, 4,701,200 values were compared: **zero mismatches, maximum absolute and relative differences 0**. Dimensions, coordinates, attributes, calendar, masks and both 16,790-row date CSVs also match. The deliberately changed value and swapped-member checks fail as intended.

All 14 netCDF file SHA-256 digests differ between predecessor and successor zero-change fixtures, although decoded data and checked scientific metadata are identical; each digest matches its own collection marker. This is a byte-level serialization difference, not a decoded scientific difference. The bytes' exact cause was not isolated, so no assertion about its origin is made. The predecessor's zero-change perturbed precipitation is itself different from its root precipitation, but the successor reproduces the corresponding predecessor perturbed member exactly; the comparison checks continuity of the producer path rather than assuming a zero-change perturbation must equal the root.

The numeric automatic seed and collection ID are intentionally allowed to change under `generation-seed-material/2`; no automatic-seed parity claim is made here. The P0 comparison is a fixed-input algorithm-continuity check, not observational fit validation or a conclusion about weather-generator realism. No calibration, observational benchmark, uncertainty interval, or regime claim is inferred.
