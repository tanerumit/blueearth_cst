# R14 declared-red - ONE node, and it is a flag rather than a defect.
# Read tests/test_r14_expected_red.py first.
#
# **Re-recorded 2026-08-27 at the end of P4.** The arc:
#   83  P1 recorded it -- every shipped config refused at `schema_version`
#   73  P1b, after the T2 readers moved
#   57  P4/1, the four `test_case/` sets and the fixture, by the rewriter
#   55  P4/2, the templates
#   29  P4/3, R13's digest-equality tests and `presplit/` retired
#    1  P4/4, every remaining test fixture moved onto v2
#
# What is left is not a config at all. `C-32`'s trajectory enum is
# `transient | constant` in the RULED register row and `transient | step` in
# shipped code. The mapping states the ruled value, because it is the
# normative record; the test compares it against `TRAJECTORY_KINDS` and fails
# until one side moves. Emitting either silently would be a choice no commit
# has standing to make, so the red IS the flag.
#
# **Delete this file and its test the moment P1c Gate 1 rules `C-32`.** A
# one-entry allowance list is one step from being a permanent one.
#
#     1  tests/test_migration_mapping.py
#
tests/test_migration_mapping.py::test_the_trajectory_enum_agrees_with_the_code  # C-32 unresolved; P1c Gate 1 rules it
